"""복사합성 적합기 — 녹음을 정답으로 두고 물리 파라미터를 역추정한다.

    fitter = CopySynthFitter(eng, target, sr, init_track)
    rep = fitter.fit_staged()
    y   = fitter.render()

왜 이 방식인가
--------------
음절을 손으로 작곡하면 "지표는 맞는데 사람 소리가 아닌" 상태에서 빠져나올 길이 없다.
무엇이 틀렸는지 비교할 정답이 없기 때문이다. 복사합성에는 정답이 있다 — 원본 파형.
되합성이 원본과 다르면 **엔진이 틀린 것**이고, 어느 파라미터가 부족한지 바로 좁혀진다.

두 가지 일치율을 따로 보고한다
------------------------------
* **포락 일치**: 멜 대역에서 잰다. "포먼트·세기·잡음색이 맞는가". 조음 파라미터가
  옳은지를 재는 값이고, 사람이 듣는 음색에 대응한다.
* **정밀 일치**: 선형 다해상도 STFT 에서 잰다. 창이 길면 하모닉이 분해되므로 F0 가
  0.1 % 만 틀려도 고차 하모닉이 통째로 빗나가 값이 0 근처로 무너진다. 즉 이 값은
  **F0 궤적과 성문 펄스 위치까지 맞았는가**를 재는 훨씬 가혹한 값이다.

둘 다 `100 · (1 − ‖|S_t| − |S_p|‖_F / ‖|S_t|‖_F)` (스펙트럼 수렴도) 로 정의한다.

왜 성김에서 촘촘함으로 가는가
-----------------------------
긴 창부터 켜면 F0 에 대한 손실면이 하모닉 간격마다 골이 파인 톱니가 되어 Adam 이
가장 가까운 가짜 골에 갇힌다. 짧은 창(256)에서는 하모닉이 분해되지 않아 손실면이
매끈하다. 그래서 짧은 창으로 포락과 세기를 먼저 맞추고, 길이를 늘려 가며 하모닉을
잠근다. 각 단계는 앞 단계의 해에서 출발한다.

경계 조건
---------
* 잔차 보정(residual)은 끈다. 학습되지 않은 NN 이 물리 적합을 오염시킨다.
* 로그 파라미터에서 0 은 "끔" 이다. 시그모이드 재매개화로는 0 에 닿을 수 없으므로
  초기값이 0 인 로그 파라미터는 고정한다(켜고 끄기는 물리 결정이지 적합 대상이 아니다).
* 전역 오프셋을 먼저 맞춘다. 전체 수준이 20 dB 틀린 채로 프레임별 적합을 돌리면
  Adam 이 그 20 dB 를 프레임마다 따로 메우고, 제어열이 물리적으로 말이 안 되게 굳는다.
"""
from __future__ import annotations

import os

import math
from dataclasses import dataclass, field

import numpy as np
import torch

from . import room
from . import tract as _tract_mod
from . import glottis as _glottis_mod
from .control import INDEX, PARAMS, ControlTrack

FFT_SIZES = (256, 512, 1024, 2048, 4096)
MEL_FFT = 256              # 포락용 창 (5.3 ms @48 kHz) — 하모닉이 분해되지 않는다
HARM_FFT = 2048            # 하모닉용 창 (43 ms, 분해능 23 Hz) — F0 빗살이 보인다
DB_RANGE = 70.0            # 정점 아래 이만큼까지만 본다 (전역 바닥)
#: **밴드별 바닥.** 전역 바닥 하나(정점 −70 dB)는 조용하지만 실재하는 고역을 통째로
#: 가린다 — 실측(`yang_00000040`)에서 멜 48 밴드 중 9 개가 바닥 아래였고 그중 7 개가
#: **13 kHz 위**였다. 목표의 14 kHz 밴드는 정점보다 73.9 dB 아래인데 70 dB 에서 잘리니
#: 그 대역에서는 무슨 짓을 해도 손실이 0 이다. 목표는 거기 20 kHz 코덱 절벽까지
#: 실제 결이 있다(스펙트로그램). `--sharp 1.0`(`out/L14`)이 그 대역을 −17.7 dB 로
#: 망가뜨린 것이 이 사각지대 때문이다 — 벌점이 없으니 공짜로 망가진다.
#:
#: 그래서 각 밴드는 **자기 정점에서 BAND_FLOOR_DB 아래까지** 보이게 한다. 단 전역
#: 정점에서 BAND_FLOOR_MIN 아래로는 내리지 않는다 — 그러지 않으면 F0 아래의 진짜
#: 무음(114 Hz 밴드가 −120 dB)까지 드러나 손실이 빈 대역을 좇는다. 실측 효과
#: (바닥 위에 있는 밴드×프레임 비율, s040 / s101):
#:
#:     12 kHz 위     24.6 → 46.6 %   38.9 → 75.0 %
#:     400 Hz 아래   57.1 → 57.1 %   59.7 → 59.7 %   (무음은 계속 가린다)
#:     400~12 kHz    82.0 → 82.1 %   88.7 → 88.7 %
#:
#: 0 이면 예전처럼 전역 바닥 하나다.
BAND_FLOOR_DB = 40.0
BAND_FLOOR_MIN = 95.0
# **난류 우세 빈에서 기대 스펙트럼을 볼 시간 폭 (ms). 0 이면 끈다.**
#
# 마찰 잡음의 STFT 크기는 확률변수다 (레일리). 프레임별 실현을 맞추라고 하면 기울기의
# 대부분이 그 잡음이고, 최적해도 실현마다 다르다. 재현할 수 있는 것은 **기대값**뿐이니
# 그것을 비교한다. 목표와 합성에 똑같이 걸리므로 편향은 없고 분산만 줄어든다.
#
# 25 ms 는 마찰음 길이(이 화자 실측 70~110 ms, `profiles/yang_female.json`)의 1/4 이라
# 정상부를 뭉개지 않으면서 창 256 에서 19 프레임을 평균한다(분산 1/19). 조화 우세
# 빈에는 `1−w` 가중이 0 이라 아무 일도 일어나지 않는다.
NOISE_EXPECT_MS = 25.0
# 성김 -> 촘촘함. 각 단계에서 켜는 창 크기.
STAGES = ((256, 512), (256, 512, 1024), (256, 512, 1024, 2048), FFT_SIZES)

# 전역 오프셋을 붙일 파라미터. **수준·기울기·잡음량만**이다.
# 포먼트에 전역 오프셋을 주면 최적화가 F4 를 F1 아래로 끌어내리는 식으로 스펙트럼을
# 맞춘다(실측: F1 887->248, F2 1550->3250, F4 5153->915). 대역 에너지는 맞지만
# 조음으로는 말이 안 되는 해다. 그런 자유도는 애초에 주지 않는다.
# `tract_gain` 은 여기 없다. 전역 수준은 `log_gain` 이 이미 닫힌 형태로 잡는데
# 둘 다 열어 두면 서로를 상쇄하며 떠돌고, 그 사이에서 `p_sub` 까지 끌려간다
# (실측: 보통 세기 모음인데 p_sub 가 17.6 cmH2O — 큰 소리로 외치는 값 — 로 갔다).
# `tract_gain` 은 폐쇄 감쇠 같은 **시간 변화**로만 쓴다.
GLOBAL_PARAMS = frozenset((
    "p_sub", "adduction", "rd_offset", "tilt", "aspiration",
    "fric_gain", "back_leak", "nasal_damp", "nasal_gain",
    "jitter", "shimmer", "bw1", "bw2", "bw3", "bw4",
))

# 사전(prior) 가중. 분석이 믿을 만한 양은 세게 묶고, 관측되지 않는 양은 기본값에
# 묶어 둔다. 약하게 푸는 것은 수준·음질 계열뿐이다.
# **조음 속도 상한 (Hz/ms).** 성도는 근육이 움직이는 물건이라 포먼트가 임의로 빨리
# 뛸 수 없다. 이 값을 넘으면 그건 조음이 아니라 적합기의 프레임별 난동이다.
#
# 계측(`data/ref` 여성 3 개, 유성 프레임 쌍 10,333 개, Praat 1 ms):
#
#   |    | 중앙 | 80분위 | 95분위 | 99분위 |
#   |----|------|--------|--------|--------|
#   | F1 |  2.9 |    8.5 |   27.2 |  147.3 |
#   | F2 |  7.0 |   22.7 |   92.7 |  604.9 |
#   | F3 | 11.5 |   37.5 |  151.0 |  770.7 |
#   | F4 | 10.7 |   31.2 |  145.2 |  771.8 |
#
# 99 분위의 폭주(F2 605, 최대 2043)는 Praat 추적기가 포먼트를 맞바꾼 것이지 조음이
# 아니다. 그래서 **95 분위를 상한**으로 잡는다. 그 아래는 공짜, 넘으면 이차로 문다
# (평활 벌점처럼 전 구간을 뭉개면 설측 F2 의 실제 급전이 같이 죽는다).
#
# 이 제약이 왜 필요했나 — 없을 때 적합된 궤적을 실측과 대조하면:
#   지속 모음 F1 실제 중앙 1.1 Hz/ms  vs  적합 126.4 Hz/ms (×115)
#   설측    F2 실제 중앙 25.5        vs  적합 309.0      (×12)
# 즉 `lam_smooth` 만으로는 네 자릿수 모자랐다. 해가 심하게 비-식별이 되어, 크기만
# 맞고 위상은 틀린 해로 굴러가도 밀어낼 힘이 없었다.
VEL_LIMIT_HZ_PER_MS: dict[str, float] = {
    "f1": 27.0, "f2": 93.0, "f3": 151.0, "f4": 145.0,
}

# **힌지는 틀린 형태였다.** 95 분위를 문턱으로 두면 그 아래의 균일한 난동을 그냥
# 통과시킨다. 실측: 지속 모음의 실제 F2 속도는 1.1 Hz/ms 인데 문턱은 93 이라, 모음에서
# 적합값이 57 Hz/ms 로 널뛰어도 벌점이 0 이다 (A/B 실측: 속도 77.9→57.3 으로 26 % 만
# 줄고 조화 SNR 은 4.6 dB 잃었다 — 나쁜 거래).
#
# 조음 속도의 실측 분포는 꼬리가 중앙의 200~300 배인 희소 신호다:
#
#   |    | 중앙 | 95분위 |   최대 | 꼬리/중앙 |
#   |----|------|--------|--------|-----------|
#   | F1 |  2.9 |   27.2 |  877.8 |      303× |
#   | F2 |  7.0 |   92.7 | 2043.0 |      292× |
#   | F3 | 11.5 |  151.0 | 2540.5 |      221× |
#
# "대부분 정지, 가끔 크게 이동" — 이런 분포의 사전은 이차(가우시안)가 아니라 L1
# (라플라스) 계열이다. 그래서 후버(smooth L1)를 쓴다: 무릎 아래는 이차라 미분이
# 살아 있고, 위는 선형이라 드문 급전에 이차 벌점처럼 가혹하지 않다.
#
# 무릎은 **실측 중앙값**이다 — "여기가 보통 속도" 라는 뜻이지 상한이 아니다.
VEL_KNEE_HZ_PER_MS: dict[str, float] = {
    "f1": 2.9, "f2": 7.0, "f3": 11.5, "f4": 10.7,
}
# **가속도 무릎 (Hz/ms²).** 1 차 차분(속도)으로는 난동과 실제 조음을 못 가른다 —
# 동적 구간에서 둘은 크기가 비슷하고 **시간 구조**만 다르다. 난동은 부호가 매 프레임
# 뒤집히고, 실제 급전은 한 방향으로 지속된다. 그래서 2 차 차분을 본다: 빠르게
# *움직이는* 것은 허용하고 빠르게 *방향을 바꾸는* 것만 문다.
#
# 실측 (data/ref 여성 3 개, 유성 프레임 삼중항 10,305 개, Praat 1 ms):
#
#   |    | 속도 중앙 | 가속 중앙 | 가속 80% | 가속 95% |
#   |----|-----------|-----------|----------|----------|
#   | F1 |       2.9 |       0.7 |      2.4 |     12.6 |
#   | F2 |       7.0 |       1.9 |      8.0 |     69.4 |
#   | F3 |      11.5 |       3.4 |     15.4 |    117.4 |
#   | F4 |      10.7 |       3.5 |     12.6 |    132.5 |
#
# 가속도가 더 나은 판별자인 이유가 이 표에 있다. 우리 적합의 난동은 F2 기준
# 300~500 Hz/ms 로 부호가 매 프레임 뒤집히므로 가속도는 약 600~1000 Hz/ms² 다.
#
#   속도로 보면   실제 7.0  vs 난동 ~300   ->  43 배
#   가속도로 보면 실제 1.9  vs 난동 ~600   -> 300 배
#
# 판별 여유가 7 배 넓다. 속도로는 실제 급전(최대 628 Hz/ms)이 난동 영역과 겹쳐서
# 못 갈랐는데, 가속도로는 실제 급전이 한 방향으로 지속되므로 겹치지 않는다.
ACC_KNEE_HZ_PER_MS2: dict[str, float] = {
    "f1": 0.7, "f2": 1.9, "f3": 3.4, "f4": 3.5,
}
# "off" | "hinge"(95 분위 상한) | "huber"(속도 중앙값 무릎) | "accel"(가속도 무릎)
#
# 앞의 둘은 기각됐다 (docs/MEASUREMENTS.md §8.6). 성긴 격자도 기각됐다 (§8.9) —
# 균일한 격자든 균일한 벌점이든 모든 시점에 똑같이 자유도를 주거나 뺏는데, 조음은
# "대부분 정지, 짧은 순간에 급전" 이라 그 틀로는 두 요구를 동시에 못 맞춘다.
VEL_MODE = "accel"
# **0.001 로 켠다** (0.3.15). 오래 기본값 0 이었는데, 그 이유였던 "포락을 얼마나
# 깎는지 모른다" 가 이제 측정됐다 — **깎지 않고 오히려 올린다.**
#
# 실측 (yang_00000040 **전체**, tilt 셸프 + a_c 속도 벌점 고정, 같은 코드):
#
#   | | 포락 | 정밀 | 제어열 곡률 rms | 95 분위 |
#   |---|---|---|---|---|
#   | 벌점 없음 | 90.47 % | 83.04 % | 147.97 | 256.90 |
#   | 잔물결 0.01 | 91.23 % | 84.05 % | **87.90** | **93.81** |
#   | **가속도 0.001** | **93.17 %** | **86.96 %** | 100.30 | 139.93 |
#
# 이전 세션의 세 구간 벤치와도 일치한다 (docs/HANDOFF.md §4: 모음 4.3→3.7°,
# 씨 17.6→12.1°, 설측 33.8→32.4° — 어디서도 무너지지 않는다).
#
# 힌지 자체는 검증했다: F2 이동 10/50/93 Hz/ms 는 벌점 0, 150 은 0.97, 309 는 14.0.
VEL_W = 0.001

# **잔물결 벌점** — 제어열이 조음 대역 **위에서** 흔들리는 것만 문다.
#
# 왜 필요한가 (docs/MEASUREMENTS.md §13, §16). 제어 격자가 1 ms 라 제어열의
# 나이퀴스트가 500 Hz 인데, 조음은 0~20 Hz 에 있고(음절률 4 Hz, 음소률 10~16 Hz)
# 여성 F0 는 250~300 Hz 다. 그 사이의 자유도는 **손실의 영공간**에 있다 — STFT
# 프레임 하나 안에 제어 프레임이 다섯 개 들어가고 `_expect` 가 난류 빈을 25 ms 로
# 뭉개므로, 그 안쪽 요동은 손실이 밀지도 당기지도 않는다. 그래서 표류한다.
#
# 그 표류가 두 증상으로 들린다. 실측(yang_00000101):
#
#   * **마찰 구간**: 고역 포락이 목표보다 3~5.5 배 맥동한다 -> "지지직"
#   * **유성 구간**: 트랙의 150~350 Hz 성분(전력의 4~6 %)이 1 차 하모닉과 곱해져
#     차주파수 0 Hz 로 내려온다 -> 0~80 Hz 가 20.7 dB 과다
#
# 트랙 14 개를 전부 25 ms 로 뭉개면 0~80 Hz 가 −20.8 → −47.8 dB 로 27 dB 내려간다.
# **어느 한 트랙이 아니라 전부가 조금씩 보태고 비간섭적으로 합쳐진다.** 그래서
# 벌점도 파라미터를 가리지 않는다.
#
# **2 차 차분이 그 자다.** dt = 1 ms 에서 |2 sin(πf·dt)|² 가 20 Hz 대 264 Hz 를
# 전력 27,000 : 1 로 가른다. 진폭 A, 주파수 f 의 정현 요동은 |Δ²| = A·(2πf·dt)²
# 이므로 F0 = 264 Hz 에서 걸음 0.1 짜리 잔물결이 0.275/ms², 15 Hz 짜리 조음이
# 걸음 1 이어도 0.0089/ms² 다. 무릎 0.02 면 조음은 이차(싼) 영역에 남고 잔물결만
# 선형(비싼) 영역으로 올라간다. 파열음 해제 같은 급전도 2 차 차분은 **양 끝
# 두 점**에만 크게 나와 평균 기여가 무시할 만하다 (VEL_MODE="accel" 과 같은 논리).
#
# 단위는 적합기의 **걸음**(`STEP`)이다 — `u = u0 + w·scale` 이므로 `w` 는 이미
# 파라미터마다 정규화돼 있다. 그래서 하나의 무릎이 28 개 파라미터에 다 통한다.
#
# 실측으로 무릎을 확인했다 (`out/sib/w0_track.npz`, 벌점 없이 적합한 350 프레임을
# raw 로 되돌려 |Δ²w|/dt² 를 낸 것):
#
#   조음이 내야 할 값 (15 Hz · 걸음 1)     0.0089
#   **실제 적합 트랙의 중앙값**            0.4746      <- 53 배
#   95 분위                                3.3604
#
# 즉 1 ms 격자에서는 **중앙값 자체가 이미 잔물결**이다. 무릎 0.02 는 조음을 이차
# 영역(0.4 배)에, 잔물결을 선형 영역(24 배)에 놓는다. 선형 영역이 L1 이라는 것도
# 맞는 성질이다 — 곡률의 L1 은 **희소한 곡률**, 즉 "움직이고 멈추는" 조각별 선형
# 궤적을 선호한다. 조음이 실제로 그 모양이다.
#
# 그래서 **세기는 작아야 한다.** 위 트랙에서 의사후버 평균이 44.66 이므로
# RIPPLE_W = 0.01 이면 벌점이 0.45 — 그 구간 손실 1.06 과 같은 자릿수다.
RIPPLE_KNEE = 0.02
# **0.01 로 켠다** (0.3.15). §23 의 A/B 가 "판정 불가" 였던 것은 자가 틀렸기
# 때문이다 — F0 아래 대역은 유성 총에너지의 1 % 미만이라 멜 포락 손실에 안 보인다
# (§26.4). 제대로 된 자로 다시 재니 결정적이다.
#
# 실측 (yang_00000040 **전체**, 나머지 기본값 전부 켠 채 잔물결만 갈라):
#
#   | | 포락 | 정밀 | 스펙트럼 |
#   |---|---|---|---|
#   | 0 | 90.71 % | 82.48 % | 71.15 % |
#   | **0.01** | **94.06 %** | **88.06 %** | **73.95 %** |
#
# +3.4 / +5.6 점이다. 제어열 곡률도 함께 내려간다 (§26.35 의 자로 rms 148 → 88).
# 이동평균과 달리 곡률의 L1 은 "움직이고 멈추는" 조각별 선형 궤적을 선호하므로
# 치찰음의 앞공동 공명이 이동하는 것은 그대로 두고 프레임률 잔떨림만 지운다 (§32.2).
RIPPLE_W = 0.01

# **조음기의 속도 한계** [neper/ms] — 협착 면적의 **로그**가 1 ms 에 이만큼 넘게
# 못 움직인다.
#
# 왜 로그인가: 조음기가 일정 속도로 움직이면 면적은 **지수적으로** 변한다(간극이
# 좁아질수록 같은 변위가 면적을 더 크게 바꾼다). 면적 그 자체에 한계를 걸면 실제
# 제스처(3.0 → 0.08 cm²)도 걸려 버린다 — 한 번 그렇게 해 보고 알았다.
#
# 왜 필요한가 (docs/MEASUREMENTS.md §24). 마찰음이 **유성 구간 한가운데서 1 ms 만에
# 켜졌다 꺼진다.** 그 순간의 `a_c`:
#
#     s040 프레임 295:  a_c  3.864 → 4.910 → **0.732**  (2 ms 안에)
#     s101 프레임 299:  a_c  1.775 → 1.089            (그때 fric_gain 208 배)
#
# 에너지로는 −36~−44 dB 라 레벨을 오염시키지는 않지만, 1 ms 짜리 광대역 스위칭이라
# 스펙트로그램에 세로 줄로 남고 고역 대역 에너지를 흔든다(마찰을 끄면 유성 구간의
# 세로 얼룩이 1.649 → 1.365).
#
# 한계값의 근거는 **v1 의 제스처 모형이 실제로 내는 값**이다 (최소 저크, 로그 면적):
#
#   | 제스처 | 중앙 | 95 분위 | 최대 |
#   |---|---|---|---|
#   | 130 ms 지속음 (0.35→0.08) | 0.0122 | 0.0213 | 0.0215 |
#   | 40 ms CV (0.35→0.08) | 0.0385 | 0.0706 | 0.0709 |
#   | **28 ms 가장 빠른 CV (0.60→0.05)** | 0.0922 | 0.1705 | **0.1724** |
#   | 적합 트랙 s101 | 0.0004 | 0.2250 | **0.8731** |
#   | 적합 트랙 s040 | 0.0019 | 0.4284 | **1.9032** |
#
# 0.20 은 가장 빠른 제스처(0.172) 바로 위이고 이탈(0.87~1.90)보다 4~10 배 아래다.
ARTIC_VEL_KNEE: dict[str, float] = {"a_c": 0.20}
# 세기. 이 항은 **기본으로 켠다** — 위 이탈이 물리적으로 불가능한 양이다.
ARTIC_VEL_W = 1.0

PRIOR_W: dict[str, float] = {
    "f0_target": 40.0, "f1": 40.0, "f2": 40.0, "f3": 20.0, "f4": 10.0,
    # 곁가지는 분석이 못 재는 양이다. 세게 묶으면 적합기가 열지를 못한다 —
    # 남 /나/ 머머가 "이득 −24 dB 내린 모음" 으로 굳던 원인.
    "velum": 3.0, "oral_open": 3.0, "lat_mix": 1.0, "lat_bw": 2.0,
    "nasal_f": 20.0, "nasal_f2": 20.0,
    "nasal_f3": 20.0, "nasal_z": 20.0, "nasal_gain": 10.0, "nasal_damp": 10.0,
    "a_c": 10.0, "obstacle": 10.0, "front_len": 10.0, "back_leak": 5.0,
    "bw1": 2.0, "bw2": 2.0, "bw3": 2.0, "bw4": 2.0,
    # p_sub 는 세기의 물리량이다. 이득과 겹치는 방향으로 끌려가지 않게 세게 묶는다.
    "p_sub": 3.0, "adduction": 0.3, "tilt": 0.1, "aspiration": 0.1,
    "rd_offset": 0.3, "tract_gain": 0.3, "fric_gain": 0.3,
    # 지터는 하모닉 차수에 비례해 위상변조 지수가 커진다(β ∝ k). 그래서 고차 하모닉을
    # 통째로 뭉개 **고역 잡음 바닥을 혼자 결정한다** (실측: 지터 0.004 + 시머 0.03 이
    # 광대역 바닥을 33 dB 들어올렸고, 그게 7 kHz 위 +9~+20 dB 오차의 정체였다).
    # 적합 대상에서 빼 두면 다른 파라미터가 그 초과분을 메우려고 비틀린다.
    "jitter": 0.1, "shimmer": 0.1,
}
# **크기 스펙트럼의 완화 상수 η** (목표 크기의 평균 대비 비율).
#
# `|z| = √(x²+y²)` 는 z = 0 에서 미분이 정의되지 않는다 — `∂|z|/∂x = x/|z|` 가 0/0 이다.
# torch 는 그 자리에서 **NaN 기울기**를 낸다. STFT 빈은 무음 구간에서 실제로 0 에
# 임의로 가까워지므로(부동소수 언더플로 포함) 이 자리가 비유한 기울기의 원인이 된다.
#
# 실측 (out/sw/r000, yang_00000040 전체, 예전 가드): 모든 단계가 "[30] 수렴 +
# 비유한 29 회 건너뜀" 으로 끝났다 — 단계당 실제 걸음이 1 번뿐이었고 포락이
# 82.7 % 에 머물렀다 (같은 파일의 정상 적합은 93.1 %).
#
# 받개는 **크기 자체를 완화하는 것**이다: `|z|_η = √(x²+y²+η²)`. 이러면
#   * 어디서나 C^∞ 이고 기울기 상한이 1 이다 (`∂|z|_η/∂x = x/|z|_η ≤ 1`),
#   * η ≪ |z| 인 곳에서는 `|z|` 와 구별되지 않으며,
#   * 로그를 씌워도 `log(|z|_η) ≥ log η` 로 아래가 막혀 있다.
# 즉 로그 처리와 완화가 같은 상수 하나로 맞물린다.
#
# 크기는 **목표 스펙트럼의 평균 크기에 비례**시킨다 — 절대값으로 두면 녹음 레벨에
# 따라 의미가 달라진다. 1e-6 이면 목표 평균보다 120 dB 아래이고, 손실이 보는 하한
# (`DB_RANGE` = 정점 −70 dB)보다 한참 밑이라 편향이 없다.
MAG_ETA_REL = 1e-6

# **세로 얼룩(스펙트럼 플럭스) 일치 항.**
#
# 사용자 조건 1 은 "세로 얼룩 없음" 인데, **지금 손실은 그것을 원리적으로 못 본다.**
# `_expect` 가 난류 우세 빈을 시간 평활한 기대 스펙트럼으로 갈아 끼워 비교하기
# 때문이다 (그 자체는 옳다 — 난류의 실현을 맞추라고 하면 기울기가 잡음이다).
# 그래서 적합기에게 프레임 간 급변을 줄일 이유가 없었다.
#
# 실현이 아니라 **통계**를 맞춘다 (§9 와 같은 철학):
#
#     F(k) = mean_t |dB_mel(t+L, k) − dB_mel(t, k)|      (L = 4 프레임 ≈ 5.3 ms)
#
# 그리고 **최소화가 아니라 일치**다. 난류는 원래 흔들리므로 목표보다 매끈하면 그것도
# 틀린 것이다 — 짧은 창의 스펙트럼이 안 흔들리는 잡음은 잡음이 아니라 음(音)이다.
# 사용자가 말한 "이전 창을 많이 반영하면 주기적 소리가 난다" 가 바로 그 이야기다.
# 그래서 `|F_합성 − F_목표|` 를 벌한다.
#
# 시간 간격 L: 합격 판정(`scripts/acceptance.py`)이 5 ms 격자에서 재므로 같은 눈금을
# 쓴다. MEL_FFT=256 의 홉이 64 표본(1.33 ms)이라 L=4 가 5.3 ms 다.
FLUX_LAG = 4
# **2.0 로 켠다** (0.3.15). 세기를 쓸어 보니 2 가 정확한 지점이다 — 같은 구간
# (yang_00000040 0.18~0.55 s), 시드 5 벌 중앙과 범위:
#
#   | 세기 | 포락 | 얼룩 p95 비 (범위) | 목표 p99 초과 % | 기울기 r |
#   |---|---|---|---|---|
#   | 0 | 91.96 % | 1.125 (1.050~1.204) | 3.08 | 0.836 |
#   | **2** | **94.04 %** | **1.058 (1.037~1.073)** | **0.000 (5 벌 전부)** | 0.834 |
#   | 5 | 93.84 % | 1.112 (1.075~1.129) | 1.54 | **0.598** (무너진다) |
#
# 2 에서 **목표 p99 를 넘는 프레임이 한 개도 없고** p95 비가 1.06 으로 목표에 붙는다.
# 시드 간 편차도 0.154 -> 0.036 으로 줄어든다 (통계를 맞추므로 실현 의존이 준다).
# 포락은 오히려 2.1 점 오른다 — 얼룩을 만들던 잔떨림이 스펙트럼에도 손해였다는 뜻이다.
# 5 는 과하다: 고역 기울기 궤적(`tilt_r`)이 0.836 -> 0.598 로 무너진다.
FLUX_W = 2.0               # 0 이면 항이 빠진다. `copyfit --flux 0` 으로 끈다.

# **포먼트 대역폭의 손실 법칙** — 공진의 "경계조건" 을 강제하는 항.
#
# 대역폭은 기하가 정하는 양이 아니라 **손실 기전**이 정한다: 벽 진동(저역 지배),
# 점성·열(∝√f), 방사(∝f²), 그리고 성문 손실(내전에 따라). 그래서 포먼트 주파수와
# 달리 **자유 파라미터로 두면 안 된다** — 자유롭게 두면 적합기가 극을 뭉개서
# 스펙트럼 기울기를 흉내 내는 데 쓴다.
#
# 실측 (out/fin/s040, 유성 프레임 중앙; 법칙은 `tract.default_bw` = 40 + 0.05·F):
#
#   | F [Hz] | 법칙 | 적합값 | 배수 |
#   |---|---|---|---|
#   | 647 | 72 | 160 | 2.21 |
#   | 1890 | 134 | 209 | 1.55 |
#   | 3125 | 196 | 351 | 1.79 |
#   | 4206 | 250 | **679** | **2.71** |
#
# 다른 적합에서는 `bw4` 가 2526 Hz 까지 간다 — 그건 극이 아니라 완만한 경사다.
#
# 그 결과가 사용자가 지적한 "울림이 부족하고 살짝 플랫" 이다. 포먼트 사이 골의
# 깊이를 재면 (300~5000 Hz, 유성): 목표 5.49 / 5.07 / 4.54 dB 에 대해 합성이
# 3.37 / 3.49 / 3.29 dB — **1.25~2.12 dB 얕다** (s040 / s101 / s034).
#
# **구역별로 보면 저역이 아니라 상위 포먼트다** (s040):
#
#   | 구역 [Hz] | 300~1200 | 1200~2600 | **2600~4200** | 4200~6500 | 6500~9000 |
#   |---|---|---|---|---|---|
#   | 목표 | 6.10 | 4.93 | 4.88 | 4.48 | 3.30 |
#   | 합성 | 6.34 | 5.87 | **2.69** | 3.77 | 2.82 |
#
# F1·F2 구역은 이미 목표보다 또렷하다. 그래서 **일률 배율은 틀린 처방이다** —
# 전 대역 ×0.35 로 좁히면 1200~2600 Hz 가 10.09 dB 로 목표(4.93)의 두 배가 된다.
# 주파수 의존 법칙이라야 한다.
#
# 그리고 이것은 "노이즈가 들린다" 와 같은 결함이다. 유성 구간의 조화/잡음비는
# 이미 목표와 ±1 dB 로 맞는다(전 대역). 2.6~5 kHz 는 목표조차 HNR 이 1~3 dB 인
# 반쯤 잡음인 대역이므로, 거기서 골이 안 닫히면 **울림 부족으로도 잡음으로도**
# 들린다. 하나의 원인이 두 증상을 만든다 (MEASUREMENTS §35).
#
# 세기 0 이면 항이 빠진다. `copyfit --bw-law` 로 조절한다.
#: **파형 상관의 붕괴를 문다** (사용자 요청: "연속 조건을 더 강화").
#:
#: 20 ms 창의 정규화 파형 상관 `r_t = <a·b>/√(<a²><b²>)`. 유성 구간에서 1 에 가까워야
#: 하고, **0 근처로 떨어지거나 음수가 되는 곳이 귀에 끊겨 들리는 곳**이다 (펄스열이
#: 반 주기 어긋나면 음수까지 간다).
#:
#: **이 항이 필요한 이유는 멜 오차가 그 사건을 못 보기 때문이다.** 실측
#: (`out/lad/s040`): 0.41 s 에서 상관이 −0.80 인데 그 자리의 **멜 dB 오차는 오히려
#: `out/fix` 보다 낮다.** 크기 스펙트럼은 멀쩡하고 위상만 뒤집혔다. 그래서 연속
#: 벌점을 멜 오차 위에 걸면 아무것도 안 문다 — 상관 위에 걸어야 한다.
#:
#: `softplus(knee − r)^POW` 로, **큰 붕괴가 지배하도록** 제곱한다. 평균 손실에서
#: 한 창은 1/1000 무게인데, 그 한 창이 들리는 결함의 전부다.
#: **세기 2.0 은 측정으로 정했다** (§45). 끊김이 있는 구간(0.38~0.60 s)에서:
#:
#:                        포락    정밀 | 조화÷비조화 [dB]  | 끊김<0.5  <0  최소상관
#:     목표                     —      — | 14.5   1.2  −3.4 |     0    0    1.00
#:     기준 (전부 0)        86.63  72.57 | 11.6  −0.9  −3.2 |     2    0    0.21
#:     --hnr 2 만          83.15  70.48 | 13.8   1.7  −1.4 |  **13    6  −0.25**
#:     **--corr 2 --hnr 2** 84.90 **80.85**| **14.2 1.2 −3.3**| **0    0    0.74**
#:
#: `--hnr` 단독은 비조화를 목표 쪽으로 끌어당기지만 **그 대가로 위상 잠금을 잃는다**
#: (끊김 2 → 13 창). 둘은 상보적이라 같이 켜야 한다.
CORR_W = 2.0
CORR_WIN_MS = 20.0
CORR_HOP_MS = 5.0
CORR_KNEE = 0.90           # 이 아래로 떨어진 만큼 문다
CORR_POW = 2.0             # 1 이면 선형, 2 면 큰 붕괴가 지배한다

#: **주기성(조화 대 비조화)을 목표에 일치시킨다** (사용자 요청).
#:
#: F0 지연에서의 정규화 자기상관 `r(t) = <x(t)x(t+T₀)>/√(<x²><x(t+T₀)²>)` — Praat 의
#: HNR 이 쓰는 것과 같은 양이고 텐서 연산만으로 미분 가능하다.
#:
#: **왜 이 항이 없으면 안 되는가.** 손실의 크기 항은 난류부를 *기대* 스펙트럼으로
#: 비교하므로 **하모닉과 잡음이 서로 교환 가능하다.** 실제로 고차 극을 늘렸더니
#: 적합기가 `p_sub` 를 9.82 → 4.55 로 반 토막 내고 `fric_gain` 을 6.29 → **70.53**
#: (+1022 %) 으로 올려 같은 스펙트럼을 만들었다. 총 레벨이 같으니 포락 점수는 안
#: 떨어지고 소리만 망가진다 (§44). 실측 주기성 (yang_00000040):
#:
#:     대역        목표     fix    pole     lad
#:     전대역    0.8616  0.8436  0.7698  0.8179
#:     1~4 kHz   0.3760  0.3789  0.2916  0.2707
#:
#: **최대화가 아니라 일치**다. 잡음을 다 죽이면 그것대로 틀린다 — 실제 음성의 난류는
#: 목표에도 있다. 다만 **시끄러운 쪽을 더 세게** 문다 (`HNR_ASYM`).
HNR_W = 2.0
HNR_ASYM = 3.0             # r 이 목표보다 낮을 때(더 시끄러울 때)의 가중
HNR_HP_HZ = 1000.0         # 두 번째 대역: 이 위만 보는 고역통과

#: **창별 손실이 악화되면 문다** (사용자 요청). 평균 손실은 국소 붕괴를 못 본다 —
#: 실측(§38.2c): 50 ms 구간별 파형 상관이 대부분 1.00 인데 한 구간만 0.88 → **−0.06**
#: 으로 뒤집힌다(펄스가 1/3 주기 미끄러짐). 그 한 구간은 평균에서 1/28 의 무게다.
#: `softplus(e_t − e_{t−L})` 로 **악화분만** 문다 — 절대 수준 항(`env_db`)이 그대로
#: 있으므로 "전부 고르게 나쁘게" 로는 회피할 수 없다.
#: 제어열을 **값**이 아니라 **움직임**으로 매개화한다.
#:
#: 왜: 지금 `w` 는 격자 위의 값이고 `_delta` 가 그것을 선형 보간한다. 마지막 단계는
#: 격자를 1 ms 로 내리므로 격자점 수 = 프레임 수가 되어 **파라미터마다 1420 개의
#: 독립 자유값**이 남는다. 연속성을 강제하는 구조가 하나도 없다. 그 결과가 실측에
#: 그대로 있다 — `nasal_damp` 는 에너지의 40 %가 150~400 Hz 에 있고(조음은 20 Hz
#: 아래다), `fric_gain` 은 마찰음 한복판에서 **1 ms 마다 제 값의 23 %씩** 튄다.
#: 시변 필터의 계수가 그 속도로 흔들리면 통과하는 잡음이 그 속도로 변조된다 —
#: 고역 포락의 150~400 Hz 변조(목표의 5~10 배)와 지글거림이 여기서 나온다.
#:
#: 무엇으로: 자유 파라미터를 **가속도**로 두고 두 번 적분해 궤적을 만든다. 그러면
#: 사용자가 요구한 세 가지 거동이 그대로 표현되고 (정지 a=v=0, 직선 a=0·v≠0,
#: 가속 a≠0), **위치와 속도의 연속성이 벌점이 아니라 구조로** 보장된다. 다음
#: 프레임이 두 양을 계승받는다는 뜻이다. 남는 불연속은 가속도뿐이고 그것은
#: `lam_smooth`(격자 위 1 차 차분 L2)가 그대로 **저크 벌점**이 된다.
#:
#: 덤: 두 번 적분은 1/f² 저역통과라, 같은 크기의 가속도가 만드는 위치 흔들림이
#: 400 Hz 에서 20 Hz 의 1/400 이다. 난동이 표현 자체로 비싸진다.
MOTION = False
#: 움직임 모드의 가장 고운 격자. 3 차 B-스플라인은 격자 간격 T 위에서 대략 1/(2T)
#: 위를 죽이므로 5 ms 는 100 Hz 가 상한이다 — 성문·포먼트의 미세구조를 담기에는
#: 거칠다 (사용자: "성대 진동, 포먼트에 대한 피팅이 너무 열악해 … 해상도를 높이거나").
#: 2 ms 로 내리면 250 Hz 까지 담기는 대신 150~400 Hz 난동이 다시 표현 가능해지므로,
#: **`MOTION_L2` 로 고운 층의 진폭을 눌러** 필요할 때만 쓰게 한다.
MOTION_MIN_GRID_MS = 2.0
#: 층별 L2 (움직임 모드). 각 층의 계수 에너지를 **해상도에 비례한 무게**로 문다 —
#: 고운 층일수록 비싸다. 그래서 거친 층이 담을 수 있는 것은 거친 층이 담고, 고운
#: 층은 그것으로 안 되는 곳에만 쓰인다 (웨이블릿 계수에 L2 를 거는 것과 같은 취지).
#:
#: 왜 필요한가: 적합 궤적이 `analyze` 의 물리 추정에서 너무 멀리 간다 — 실측
#: (`out/L6/s040`) `fric_gain` +685 %, `nasal_gain` +442 %, `bw4` +413 %, `tilt` +202 %.
#: 프레임 간 떨림(§48 이 잡았다)과는 **다른 종류의 과도한 움직임**이고, 그쪽은
#: `lam_prior`·`PRIOR_W` 가, 이쪽(층의 진폭)은 이 항이 맡는다.
MOTION_L2 = 0.0

#: **경계의 매끄러운 벽** (유사퍼텐셜). 경계가 있는 파라미터는 시그모이드로
#: 재매개화되는데, 값이 경계에 붙으면 `σ′` 가 죽어 **기울기가 사라진 채 고착**된다 —
#: 실측(`out/L12/s040`)에서 `jitter` 가 상한 0.01 에 중앙 0.00983(범위의 98 %)으로
#: 붙었고 `shimmer` 는 하한에 붙었다. §10.2 가 `obstacle` 에서 경고한 것과 같은
#: 함정이고, 이 프로젝트의 "발산" 중 지금 남아 있는 유일한 형태다.
#:
#: 처방은 내부점법의 로그 장벽과 같은 취지다 — 경계 **안쪽**에 매끄러운 반발 벽을
#: 두어 거기 닿기 전에 되밀고, 벽 바깥(=중앙부)에서는 아무 일도 하지 않는다.
#: 유사퍼텐셜이 딱딱한 중심부를 매끄러운 유효 퍼텐셜로 갈아 끼우되 바깥 척도의
#: 물리는 그대로 두는 것과 같은 구성이다. `u` 는 로짓이므로
#: |u| > BOUND_U 에서만 이차로 문다 (σ(4) = 0.982).
BOUND_W = 0.0

#: **반복마다 잡음 씨앗을 새로 뽑는다** — 손실의 기댓값(잡음 실현에 대한)을 최소화한다.
#:
#: 씨앗을 고정하면 난수열이 적합 내내 같다. 그러면 적합기는 제어열(내전·폐압·협착)과
#: 지터·시머 세기를 흔들어 **그 한 벌의 난수**를 목표의 무작위 요동에 끼워 맞출 수 있다.
#: 실측(out/L25, 제어열은 그대로 두고 씨앗만 1~3 으로 바꿔 재렌더):
#:
#:     s040  포락 0.480 → 0.513~0.562   corr 0.207 → 0.33~0.38   phase 0.571 → 0.80~0.92
#:     s101  포락 0.715 → 0.770~0.844   corr 0.084 → 0.16~0.30   phase 0.621 → 0.79~0.99
#:
#: 즉 파형 항 손실의 40~250 % 가 **실현 끼워 맞춤**이다. 그 끼워 맞춤이 물리적으로 없는
#: 흔들림이다 — 치찰음 한복판에서 내전이 20 ms 사이 0.42 → 0.07 로 떨어졌다 되오르며 마찰
#: 구동을 20 ms 간격으로 끊는다(사용자가 "치찰음이 세로로 토막" 이라 한 것, MEASUREMENTS §50).
#:
#: 씨앗을 매번 바꾸면 특정 실현에만 좋은 흔들림은 기댓값에서 상쇄되어 기울기를 못 받는다.
#: 손실이 잡음 때문에 회차마다 흔들리므로 최선 스냅샷·정체 판정은 손실의 지수이동평균
#: (`RESEED_EMA`)으로 한다. 균형 측정·이득 보정·최종 렌더는 씨앗 0 그대로다.
RESEED = False
RESEED_EMA = 0.1

#: **무성 마찰에서 성대가 떨면 문다** — 목표가 무성 마찰인 프레임에서 합성의 성문 떨림 진폭²의 평균.
#:
#: 실측(MEASUREMENTS §50.9): `L34/s040` 의 ㅊ(0.71~0.80 s) 에서 떨림 진폭이 0.28(모음 1.05),
#: 유성 마스크 100 %. 그 떨림이 배음과 성문 주기 AM 으로 마찰 위에 F0 를 실어 **치찰음을 F0 로
#: 썬다** — 성대만 멈추면 치찰음 고역 포락의 F0 대역 변조 과잉이 +9.6 → −0.5 dB 로 사라진다.
#: 손실은 이것을 못 본다: 무성 구간 목표의 저역은 밴드 바닥 아래라 떨림이 새도 오차가 없고,
#: F0 토막은 포락 오차 0.13 dB 로만 보인다(§50.2).
#:
#: 마스크는 분석 트랙의 `fricative & ~voiced` 에서 유성 프레임 ±10 ms 를 뺀 것 — 경계의 유성
#: 전이는 자유다. 기울기는 문턱 압력을 거쳐 **내전을 낮추는 쪽**으로 흐른다(벌린 성문은 문턱이
#: 높다: 내전 0.8 → 1.8, 0.3 → 4.7 cmH₂O). 폐압을 낮추는 길도 있지만 그건 마찰 유량을 줄여
#: 포락 항이 막는다. 0 이면 끈다. 균형 몫은 `BAL_W["unvoiced"]`.
UNVOICED_W = 0.0
#: 무성 벌점이 비켜 가는 유성 경계 여유 [프레임 = ms]. 10 이면 s101 에서 성대를 멈추게 한 것이 내전의
#: 시간상수(15 ms)를 타고 이웃 모음 가장자리로 번져 비마찰 끊김이 12 → 38 로 늘었다(§50.16).
UNVOICED_EDGE = 10

#: **조용한 구간의 넘침 벌점** (MEASUREMENTS §51.11). 목표가 정점 −`QUIET_DB` 아래인 10 ms 창에서, 합성 수준이
#: 목표보다 `QUIET_MARGIN_DB` 넘게 크면 그 초과 dB 의 제곱(부드러운 무릎)을 문다. 0 이면 끈다.
#:
#: 왜: 포락 SC 는 선형 크기라 정점 −50 dB 아래 구간을 사실상 보지 못하고, dB 항도 대역 바닥에 걸려 무음을 못 본다.
#: 그래서 발화 끝·시작 앞의 숨소리가 목표보다 7~9 dB 크게 남았다(`L52/s101` 끝 1351~1420 ms: 0~0.5 kHz +7.7,
#: 6~12 kHz +6.2; `L49/s040` 개시 앞 156~215 ms +7.2) — 사용자가 짚은 "발화 끝·무음의 엉뚱한 잡음띠" 다.
#: 합성이 목표보다 **작은** 쪽은 묻지 않는다(바닥 잡음은 `--floor` 가 채운다).
QUIET_W = 0.0

#: **위상 항의 상한 주파수** [Hz] (MEASUREMENTS §51.15). 0 이면 전 대역. 배음 위상 분산(`glottis.HARM_PHASE_JIT`)을
#: 켜면 그 시작 주파수 위의 합성 배음 위상은 **설계상 난수**다(씨앗을 매 회차 새로 뽑으면 매번 다르다). 거기서 목표
#: 위상과의 거리를 재면 기울기가 난수가 되고, 배음 차수 k 만큼 f0 로 증폭되어 흘러 펄스 정렬을 무너뜨린다 — 3 단계에서
#: 펄스 척도가 0.3 → 0.96 으로 가고 포락이 8~10 점 떨어졌다(`L52`·`L54`·`L58`, lr 0.05 고정). 배음 위상 분산이 없던
#: `L25` 에서는 3 단계가 포락을 90.43 → 91.64 로 올렸다.
PHASE_FMAX = 0.0

#: 기식 MVF 를 적합하지 않는다(`copyfit --mvf` 가 값을 박는다). v2.1 의 모든 판에서 적합 MVF 가 하한 2 kHz 로
#: 갔다 — 목표 s040 의 MVF 는 ≈ 6 kHz 이고(§50.17) 감사의 모음 주기성은 s101 에서 1.5 kHz 위가 목표보다 잡음이
#: 많다(0.23/0.08/0.03 대 0.41/0.21/0.17). 적합기가 기식을 2~5 kHz 의 포락 채움재로 쓰고 있다.
#: **화자 상수를 얼린다** (MEASUREMENTS §51.35). 켜면 `self.hf` (고역 극 구조·이상와 영점·앞공동
#: 대역폭·MVF·음원 EQ·성문 개방 감쇠)가 옵티마이저 목록에서 빠진다. 값은 `copyfit --speaker-lock`
#: 이 미리 넣는다.
#:
#: 왜: 성도 길이와 후두 구조는 한 사람의 것이지 문장의 것이 아니다. 그런데 발화마다 따로 적합하면
#: 그 자유도가 발화별 실현 오차를 빨아들인다 — 실측(`out/L86`, 같은 화자 두 문장): 고역 극 대역폭
#: `hf_log_bw` 가 **2.96**(= 대역폭 19 배) 벌어졌고, 이상와 영점 `pir_log_f` 2.30, `pir_depth` 1.87,
#: `log_mvf` 1.59 다. 해부가 문장마다 그렇게 바뀔 리 없다.
SPEAKER_LOCK = False

#: **포먼트마다의 제 구역** [Hz] (MEASUREMENTS §51.38). 이름 -> (하한, 상한).
#:
#: 왜: 사용자: *"f1, f2, f3 이 사는 주파수 중 겹치는 게 몇이나 되지? 각자의 영역을 정확히 배정하면
#: 그럴 문제도 없을 거 아니야."* 재 보니 `ParamSpec` 의 범위가 `f1`~`f8` **전부 12 kHz 까지** 열려
#: 있었다. 그래서 적합된 `f4` 가 `s040` 에서 **7321 Hz** 까지 갔다 — 목표에서 관측한 1~99 % 구간은
#: 3876~5426 Hz 다. 관측은 순서를 한 번도 안 뒤집는데(F_k > F_{k+1} 인 프레임 0.0 %) 제어는 아무
#: 데나 갈 수 있었다. 구역을 주면 그 난동이 **구조적으로** 불가능해진다.
#:
#: 값은 `scripts/speaker_profile.py --formant-bands` 가 목표 분석의 1~99 % 구간에 여유를 두어 만든다.
FORMANT_BAND: dict[str, tuple[float, float]] = {}

#: **포먼트마다의 Q 구역** (MEASUREMENTS §51.39). 이름(`f1`…) -> (Q 하한, Q 상한).
#:
#: **지금은 쓰지 않는다.** 모음 프레임에서 낸 Q 를 전 구간에 걸었더니 출발점의 26~65 % 가 구역
#: 밖이 되어 1 단계부터 6.5 점이 무너졌다(`L97`). 유성 마스크를 두거나 벌점으로 다시 만들 것.
#: 대역폭을 `f/Q` 로 가두므로 주파수가 움직여도 날카로움의 상한·하한이 유지된다.
#:
#: 왜: 사용자가 `s040` 1.138 s 에서 *"f1과 f2가 확고히 분리가 되어서 중간에 골이 있음에도 우리는
#: 하나로 거의 퉁쳐져 있다"* 고 짚었다. 재 보니 모음 프레임의 Q 중앙값이
#:
#:     F1 관측 9.7 / 적합 **4.1**    F2 관측 7.4 / 적합 10.9    F3 관측 9.1 / 적합 6.2    F4 관측 10.5 / 적합 8.4
#:
#: 로, **F1 이 2.4 배 뭉개져** 그 치마가 위의 골을 메운다. 실제로 `bw1`·`bw2` 를 반으로 줄이면
#: F1–F2 골 깊이가 6.1 → 8.8 dB 로 목표(8.0)를 넘는다. 지금은 `ParamSpec` 이 대역폭을 20~3000 Hz 로
#: 열어 두어 Q 가 1.5 까지 내려간다 — 생리적으로 그런 포먼트는 없다.
Q_BAND: dict[str, tuple[float, float]] = {}

MVF_FROZEN = False

#: **펄스 잠금** (MEASUREMENTS §51.16). 켜면 성문 위상을 적합된 f0 의 적분이 아니라 **목표 녹음의 성문 폐쇄
#: 시각**에서 만든다: 이웃한 두 폐쇄 사이를 한 주기로 보고 위상을 0→2π 로 선형으로 잇는다(그 간격이 2~25 ms 인
#: 구간만; 나머지는 제어 f0 로 잇는다). 전역 오프셋 하나(`pulse_phi0`)만 학습해 LF 폐쇄 시점과 분석기 표시의
#: 관례 차이를 흡수한다.
#:
#: 왜: 폐쇄 시각은 잡음의 실현이 아니라 **목표에서 관측되는 결정적 정보**이고(§51.2 의 시머와 같은 부류),
#: 적합기는 이미 그것을 `pulse_loss` 로 맞추려 하고 있었다 — 다만 f0 궤적을 통해 간접으로 맞추느라 실패했다.
#: 실측: 저역(1 kHz 아래) 파형의 20 ms 창 상관이 최적 지연을 주면 0.93 인데 0 지연에서는 0.72~0.85 이고,
#: 목표 펄스의 |오프셋| 중앙값이 0.6~0.7 ms (주기의 20 %) 였다(§51.10). 5.3 ms 창으로 재는 포락 점수는 주기
#: 안의 펄스 위치에 민감하므로 이것이 남은 오차의 큰 몫이다 — 프레임별·대역별 최적 이득을 줘도 89 % 에서
#: 막혔다(§51.2 의 잠재 회복량).
#:
#: 켜면 `f0_target` 은 위상을 만들지 않으므로(배음 개수·기울기·생리 문턱에만 남는다) 적합 대상에서 뺀다 —
#: 안 그러면 관측되지 않는 자유도가 떠돈다. 펄스 항도 항등적으로 0 이라 끈다.
PULSE_LOCK = False

#: **한 파라미터만 격자 하한 아래로 푼다** (MEASUREMENTS §51.19). `copyfit --fast 이름` 이 이 집합을 채우고,
#: `MOTION_MIN_GRID_MS` 를 1 ms 로 낮춘 뒤 나머지 파라미터는 전부 2 ms 층에서 잠근다(`MOTION_SLOW`).
#:
#: 왜: 목표 모음의 **저역 수준이 60~150 Hz 로 흔들린다** — 150~500 Hz 대역 수준의 변조 RMS 가 목표 7.04 dB
#: 인데 적합은 2.23 dB 이고, 같은 모델을 씨앗만 바꾼 천장 쌍도 2.56 dB 다(분석 자체의 몫). 그 흔들림을 만드는
#: 손잡이는 **시머**(주기별 진폭)뿐이다: 시머를 0.18 로 올리면 4.87 dB 가 나온다. 그런데 시머는 확률 변수라
#: 실현이 목표와 달라 점수는 오히려 떨어진다(85.95 → 83.40). 목표의 **펄스별 진폭은 관측 가능한 결정적
#: 정보**이므로(§51.2) `voice_gain` 으로 따라가야 하는데, 그것이 2 ms 격자(`MOTION_MIN_GRID_MS`)에 막혀
#: 60~150 Hz 를 낼 수 없었다. F0 ≈ 290 Hz 면 한 주기가 3.4 ms 라 **주기별 진폭은 1 ms 격자가 있어야** 표현된다.
#: `voice_gain` 은 성문 배음에만 곱하므로(잡음 제외) 이것을 풀어도 잡음을 썰지 않는다.
MOTION_FAST: set[str] = set()

#: **움직임 예산** (MEASUREMENTS §51.20). 사용자: *"무브먼트가 너무 과해. 억제시킬 방안을 찾아봐."*
#:
#: 제어열이 얼마나 흔들려도 되는지에는 두 개의 독립된 기준이 있다.
#:
#: * **관측** — 포먼트·대역폭은 분석기가 목표에서 실제로 잰 궤적이 있다. 적합이 그것보다 빨리 흔들리면
#:   그건 조음이 아니라 적합기의 난동이다. `MOVE_RATE_REL` 배까지만 허용한다.
#: * **생리** — 음원 손잡이(`voice_gain`·`tilt`·`rd_offset`·`p_sub`·`adduction`)에는 관측 궤적이 없다.
#:   대신 생리적 크기가 있다: 모달 발성의 시머는 9 % ≈ **0.75 dB**, 음원 기울기는 발성 노력이라 한 음절
#:   안에서 몇 dB/oct 씩 튀지 않고, 폐압은 호흡이라 더 느리다. `MOVE_CAP` 이 그 상한이다 [파라미터 단위].
#:
#: 느린 음절 윤곽은 정당하므로 **빠른 성분만** 문다 — σ `MOVE_SMOOTH_MS` 가우시안으로 추세를 뺀 나머지의
#: 유성 구간 rms 가 상한을 넘는 만큼(부드러운 무릎)의 제곱. 실측(§51.20): `L53/s040` `voice_gain` 빠른 σ
#: 0.81 dB, `L53/s101` 1.77, `L62/s040` 3.12, `L25/s040` 는 `p_sub` 0.85 (상한 0.30).
MOVE_W = 0.0
MOVE_SMOOTH_MS = 8.0
MOVE_CAP = {"voice_gain": 1.0, "tilt": 0.6, "rd_offset": 0.10, "p_sub": 0.30, "adduction": 0.05}
MOVE_RATE_REL = 1.3
MOVE_RATE_PARAMS = ("f1", "f2", "f3", "f4", "bw1", "bw2", "bw3", "bw4")

#: **요동만 허용하는 제어** (MEASUREMENTS §51.22). 여기 적힌 파라미터는 값에서 자기 추세(σ `FAST_ONLY_MS`
#: 가우시안)를 빼고 쓴다 — 느린 수준 윤곽을 만들 수 없고 **빠른 요동만** 낸다. 유성 마스크도 곱한다.
#:
#: 왜: `voice_gain` 은 목표의 주기별 진폭(시머 실현)을 따라가라고 만든 손잡이인데(§51.2), 적합기는 그것을
#: **전체 소리 크기의 주된 수단**으로 썼다 — 0 으로 두면 포락이 86 → 39 %로 무너진다. 그러면서 구간별
#: ms 당 변화가 모음 0.90 dB, 치찰음 경계 **1.24 dB**(한 주기에 4 dB)까지 갔고, 분석에는 대응하는 관측이
#: 아예 없다(0.00). 사용자가 "치찰음 근방에서 흔들림" 이라고 들은 것이 이것이다. 크기의 느린 윤곽은
#: 폐압·성도 이득처럼 **관측에 묶인** 제어가 져야 한다.
FAST_ONLY: set[str] = set()
FAST_ONLY_MS = 8.0

#: **마찰 안에서는 공명을 붙잡는다** (MEASUREMENTS §51.30). 켜면 `HOLD_PARAMS` 의 제어가 마찰 구간
#: **하나하나의 안쪽에서 상수**가 된다 — 그 구간에서의 제 평균(로그)으로 끌어당기고, 가장자리
#: `HOLD_RAMP_MS` [ms] 에 걸쳐 풀어 준다. 전이(협착을 만들고 푸는 동작)는 그대로 자유롭다.
#:
#: 왜: 사용자가 되풀이해 지적한 "치찰음 근방의 흔들림" 이 마찰 **안**에 있다. `out/L7` 은 사용자가
#: 치찰음이 좋다고 한 유일한 판인데, 마찰 프레임 안에서 `f3`·`f4` 의 ms 당 |Δ| 중앙값이 0.003 / 0.005 %
#: 로 사실상 얼어 있었고 변화의 상위 10 % 가 전체의 95 / 91 % 였다 — 가장자리에서만 움직였다. 같은 자로
#: 잰 `L53` 은 0.055 / 0.041 %, `L85` 는 0.296 / 0.088 % 에 상위 10 % 몫이 20~25 % 로, 마찰 내내
#: 움직인다. 그것도 1 ms 잔떨림이 아니라 **구간을 가로지르는 완만한 쓸림**이라(가우시안 평활로는
#: 중앙값이 안 줄고 σ8 에서 오히려 0.296 → 0.319 % 로 늘었다) 평활이 아니라 **고원**이 답이다.
#:
#: 물리도 같은 말을 한다. 마찰은 협착을 **유지**하는 동작이고(Browman & Goldstein 의 제스처 고원),
#: 난류는 무작위라 목표의 그 실현을 성도 공명으로 따라갈 수 없다. 실제로 마찰 구간에 백색 떨림을
#: 심으면 어느 손실 항도 내려가지 않는다(4 % 떨림에 env_db +0.19 %) — 움직임은 상을 받아서가 아니라
#: **말리지 않아서** 남아 있다. 그래서 벌점이 아니라 **구조**로 막는다.
#:
#: 비용은 심어서 쟀다 (`out/_tmp/sibfreeze.py`: 적합된 제어열을 구간 고원으로 갈아 끼우고 재렌더).
#: 포먼트·대역폭만 붙잡으면 `L53/s040` −0.00, `L85/s040` −0.06, `L85/s101` −0.01 % 다. 제약을 건
#: 최적해의 손실은 이 사영점의 손실보다 클 수 없으므로(사영점이 실행 가능하다) 이것이 상한이다.
#: 반대로 `tract_gain`·`a_c` 까지 붙잡으면 −0.07 ~ **−0.58 %** 로 뛴다 — 마찰의 **세기 윤곽**은
#: 진짜 정보다. 그래서 `HOLD_PARAMS` 는 공명(포먼트·대역폭)만이다. 참고로 전 구간을 σ8 로 평활하면
#: −0.46 ~ −0.76 % 이므로, 증거가 없는 구간에만 거는 것이 전역 평활보다 10 배 싸다.
#: **어택 항** (MEASUREMENTS §51.31). 목표의 고역이 급히 서는 자리에서 **서는 크기**를 맞춘다.
#:
#: 왜: 파열은 통째로 빠져 있다. 목표의 2~8 kHz 는 3 ms 안에 13~31 dB 서는데 합성은 −7.5 ~ +9.2 dB
#: 다(`out/_tmp/burst2.py`, 두 파일 일곱 자리). 파열 **뒤** 10 ms 의 수준은 ±3 dB 로 맞으니 문제는
#: 레벨이 아니라 **모서리**다. 그런데 지금 손실은 그 모서리를 볼 수 없다 — 멜 홉이 5.3 ms 이고
#: `_expect` 가 난류 빈을 25 ms 로 평활한다. 파열은 폐쇄 위치의 주 단서라(Stevens; Blumstein &
#: Stevens 1979) 명료도에 직접 걸린다.
#:
#: 잡음에 속지 않는 이유: 대역 에너지를 `ATTACK_SMOOTH_MS` 로 고르고 **오르는 쪽만** 센다. 난류의
#: 무작위 요동은 기울기의 평균이 0 이라 상쇄되지만 파열은 한쪽으로 13~31 dB 다. 그리고 가중이
#: 목표가 실제로 서는 자리(`ATTACK_MIN_DB` 위)에 몰려 있어 정상 구간은 거의 안 본다.
ATTACK_W = 0.0
#: 어택을 **대역마다** 따로 맞춘다 (MEASUREMENTS §51.43). 한 덩어리로 재면 대역이 따로 서도
#: 손실이 모르는데, 실측으로 마찰 시작이 대역 사이에서 14.5~18.5 ms 어긋난다(감사의
#: `치찰 대역 시작 어긋남`). 셋을 각각 맞추면 같이 서야 한다.
ATTACK_BANDS = ((2000.0, 4000.0), (4000.0, 8000.0), (8000.0, 16000.0))
ATTACK_BAND = (2000.0, 8000.0)
ATTACK_RISE_MS = 3.0
ATTACK_SMOOTH_MS = 1.5
ATTACK_MIN_DB = 6.0
ATTACK_FLOOR = 0.05

#: **파열 자리에서만 제어 해상도를 푼다** (MEASUREMENTS §51.31). `MOTION_SLOW` 로 묶인 파라미터라도
#: `ControlTrack.bursts` 의 ±`BURST_WIN_MS` 안에서는 고운 층의 계수를 살린다.
#:
#: 왜: 손실이 모서리를 보게 해도(=`ATTACK_W`) 제어가 3 ms 계단을 만들 수 없으면 소용이 없다.
#: 지금은 `--slow a_c=10` 등으로 모든 조음 손잡이가 10~20 ms 격자에 묶여 있다. 반대로 전역으로
#: 풀면 사용자가 지적한 난동이 돌아온다 — 그래서 **증거가 있는 자리에서만** 푼다. 마찰 안에서
#: 붙잡는 `HOLD_FRIC` 과 같은 원리의 반대쪽이다.
BURST_FINE = False
#: **관측이 빠르게 움직이는 곳도 연다** (MEASUREMENTS §51.37). 분석 궤적의 포먼트가 ms 당
#: `MOVE_FINE_PCT` % 넘게 움직이는 프레임 둘레 ±`BURST_WIN_MS` 에서도 고운 층을 살린다.
#:
#: 왜: 사용자가 `L91/s040` 의 0.395~0.447 s 를 "포먼트 무브먼트가 괴상하다" 고 짚었다. 재 보면
#: **목표 F2 가 2300 → 1333 → 2417 Hz 를 60 ms 안에** 오가는데(자음 폐쇄의 전이) 우리 제어는
#: `--slow f2=20` 에 묶여 그 골을 못 만든다 — 454~462 ms 에서 **3.21 Bark** 어긋난다. 파열만
#: 여는 것으로는 부족하다. 관측 궤적이 "여기서 빨리 움직인다" 고 말하는 자리를 함께 연다.
#: 0 이면 끈다.
MOVE_FINE_PCT = 0.0
MOVE_FINE_PARAMS = ("f1", "f2", "f3")
#: 그 자리의 창 반폭 [ms]. 파열(`BURST_WIN_MS` = 20)보다 좁다 — 파열은 앞의 폐쇄를 만들 자리가
#: 필요하지만 전이는 제자리에서 빨라지면 된다. 넓게 잡으면 파일의 65 % 가 열려 (실측) 사용자가
#: 지적한 난동이 돌아온다.
MOVE_FINE_WIN_MS = 6.0
#: 그 자리에서 **출발점 평활도 끈다** — 반폭 [ms]. 0 이면 끈다. `_fast_obs_mask` 참조.
BASE_SMOOTH_KEEP_FAST = 8.0
#: 20 ms 로 잡았다 — 손으로 밀어 넣어 보니 파열 앞 20 ms 를 닫으면 엔진이 7.5~19.9 dB 를 낸다
#: (`out/_tmp/burstable.py`). 창이 그보다 좁으면 닫을 자리가 없다.
BURST_WIN_MS = 20.0
BURST_PARAMS = ("a_c", "tract_gain", "aspiration", "obstacle", "front_len", "adduction")

HOLD_FRIC = False
#: 마찰이 **끝나는** 쪽에서 붙잡기가 풀리는 거리 [ms].
HOLD_RAMP_MS = 15.0
#: 마찰이 **시작하는** 쪽의 거리 [ms]. 두 가장자리는 대칭이 아니다 — 협착은 빨리 서고 천천히 풀린다.
#:
#: 사용자: *"치찰음 포먼트가 실제 음원보다 늦게 적용되고"*. 실측(목표 녹음의 마찰 구간 3 개, 2.5~16 kHz
#: 무게중심이 고원값 ±5 % 안에 드는 시각): **시작 뒤 2·5·14 ms**, **끝 앞 0·0·35 ms**. 그런데 램프를
#: 양쪽 다 15 ms 로 두면 시작 쪽에서 공명이 15 ms 동안 고원에 못 닿는다 — 감사의 "치찰 대역 시작
#: 어긋남" 이 M1 에서 17.5~18.5 ms (기준 12) 였던 것이 이것이다. 들어가는 쪽만 5 ms 로 줄인다.
HOLD_RAMP_IN_MS = 5.0
HOLD_PARAMS = ("f1", "f2", "f3", "f4", "bw1", "bw2", "bw3", "bw4")

#: **미래를 보는 레이놀즈 전이** (MEASUREMENTS §51.24). 사용자: *"어차피 지금 하는 건 예측이 아니고
#: 피팅이니까 그냥 미래 정보도 가져다 써. 미래 데이터에 난류 조짐이 보이면 레이놀즈 현상이 일어나는
#: 방향으로, 그 조짐이 없어지면 꺼지는 방향으로 패널티를 부과해."*
#:
#: 엔진의 마찰 구동은 레이놀즈 수의 문턱(`noise.RE_CRIT` = 1800)을 넘어야 켜진다 — 물리적으로 옳지만
#: **문턱은 기울기가 거의 없는 벽**이라, 적합기가 그 벽의 어느 쪽에 있는지를 스펙트럼 오차만으로
#: 찾아가기 어렵다. 목표에는 난류가 언제 켜지고 꺼지는지가 **이미 적혀 있다**(고역/저역 비). 피팅이므로
#: 그 정보를 미래까지 당겨 써도 된다: 앞으로 `RE_LEAD_MS` 안에 난류가 오면 Re 를 문턱 위로 밀고,
#: 앞뒤로 그만큼 안에 난류가 없으면 문턱 아래로 민다. 협착이 **미리 좁아지기 시작**해야 개시가 자연스럽고
#: (조음은 목표에 앞서 움직인다), 끝난 뒤에는 확실히 꺼져야 꼬리 잡음이 남지 않는다.
RE_LEAD_W = 0.0
RE_LEAD_MS = 25.0
RE_MARGIN = 0.15          # 문턱의 ±15 % 까지 밀어붙인다
QUIET_DB = 45.0
QUIET_MARGIN_DB = 2.0

#: **녹음 바닥 잡음을 합성에 고정으로 더한다.** 목표의 가장 조용한 10 % 프레임의 파워 스펙트럼
#: (잡음 제거기 `noise_profile` 과 같은 방법)을 모양·수준 그대로 쓴다. 적합하지 않는다 — 목소리가
#: 아니라 녹음의 성질이다.
#:
#: 없으면 적합기가 무음의 녹음 바닥(−75 dB)을 **목소리로 흉내 낸다.** 실측(out/L35/s040, 발화 전
#: 0.02~0.18 s): 폐압 6.3 cmH₂O, 내전 0.13 으로 성문을 열어 두고 모음의 66 배 기식을 고정된 성도
#: 공명(920/1500/3100/4280 Hz)에 통과시켰다. 수준은 목표와 −75.1 dB 로 같지만 질감은 **공명 줄**
#: 이다 — 녹음 바닥은 평평한 잡음이라 목표에는 줄이 없다. 사용자가 본 "평평한 줄" 의 한 출처다
#: (MEASUREMENTS §50.15). 바닥을 따로 주면 무음에서 폐압을 0 으로 내릴 수 있다.
#: 난수는 엔진 씨앗(`cfg.seed`)을 따른다 — `RESEED` 면 매번 새 실현이다.
FLOOR_NOISE = False

#: **학습률 스케줄.** "cos" = 단계마다 코사인 감쇠 한 번(lr → 0.05·lr, 예전 거동).
#: "sgdr" = 감쇠형 웜 리스타트 코사인 (Loshchilov & Hutter 2017, SGDR).
#:
#: 사용자: *"아무래도 로컬 미니멈이 너무 많은 게 문제인 듯하다."* 코사인 한 번이면 lr 이
#: 단조로 줄어 한 번 들어간 골짜기에서 나올 힘이 없다. SGDR 은 한 단계 안에서 lr 을
#: `SGDR_CYCLES` 번 다시 올려 골짜기 벽을 넘을 기회를 준다. 정점은 주기마다
#: `SGDR_DECAY` 배로 낮춘다 — 뒤 주기일수록 탐색보다 다듬기다. 최선 스냅샷(씨앗을 바꾸면
#: 지수이동평균)은 주기를 가로질러 지키므로, 재시동이 해를 망쳐도 잃지 않는다.
#: `RESEED` 의 확률적 손실과 함께 쓰면 담금질(annealed Langevin)에 가깝다.
SCHED = "cos"
SGDR_CYCLES = 3
SGDR_DECAY = 0.7
BOUND_U = 4.0

#: 위상 가중을 올리는 데 쓰는 구간 (위상 단계 예산의 비율). 0 이면 예전처럼 계단.
PHASE_RAMP = 0.0

#: **손실 항의 기울기 균형** (GradNorm 계열, Chen et al. 2018 의 단순형).
#:
#: 왜: 항마다 눈금이 제각각이라 가중이 뜻을 잃었다. 모든 항을 가중 1 로 두고 제어열에
#: 대한 기울기 크기를 재면(`yang_00000040` 0.2~0.9 s, 짧은 적합 뒤):
#:
#:     corr 59.9 %   phase 19.1 %   pulse 4.6 %   sharp 3.4 %   hnr 3.1 %
#:     subf0 2.9 %   **env_db(주 목적) 2.9 %**   sc 1.8 %   flux 1.3 %   env_sc 0.8 %
#:     **prior 0.0 % (1.4e-4)   smooth 0.0 % (8.3e-5)**
#:
#: 기본값이 `--corr 2` 이므로 실제로는 **파형 상관이 스펙트럼 포락보다 약 40 배 세게**
#: 적합을 끈다. 포먼트·스펙트럼 피팅이 열악했던 이유이고, 물리 사전은 자릿수 다섯 개
#: 아래라 사실상 없어서 `fric_gain` 이 초기 추정의 +685 %로 달아났다.
#:
#: 무엇을: 각 적합 단계 시작에서 항마다 기울기 규범 g_i 를 재고, 유효 가중을
#: `BAL_W[i] * g_포락 / g_i` 로 둔다. 그러면 BAL_W 는 **"포락 대비 영향력"** 이라는 한
#: 가지 뜻만 갖는다. 항을 켜고 끄는 것은 여전히 각 플래그(`--corr` 등)가 한다 —
#: 균형은 켜진 항의 눈금만 맞춘다.
BALANCE = False
#: `corr` 는 포락과 같은 몫이다 — 0.5 로 두었더니 끊김이 21 → 47 로 돌아왔다 (`out/L16`).
BAL_W: dict[str, float] = {
    "pulse": 0.3, "phase": 1.0, "corr": 1.0, "hnr": 0.5,
    "sharp": 0.3, "subf0": 0.3, "flux": 0.3, "cont": 0.3,
    "prior": 0.3, "smooth": 0.2, "unvoiced": 0.5, "quiet": 0.3, "move": 1.0, "relead": 1.0,
    "gesture": 0.5, "delta": 1.0, "formant": 1.0, "ripple": 1.0,
}
#: **균형에서 빼는 항** (MEASUREMENTS §51.26). GradNorm-lite 는 `가중 = 몫 × ‖∇env‖ / ‖∇항‖` 로 정하는데,
#: **제약 항**(상한 안에 있으면 값도 기울기도 0)은 분모가 0 에 가까워 가중이 폭주한다 — 실측 `L76/s101` 에서
#: 움직임 예산의 가중이 **1.23e+04** 가 됐고 포락이 85.7 → 72.8 로 무너졌다. 이 항들은 `copyfit` 이 준 세기를
#: 그대로 쓴다. (`--init` 과 `prior` 도 같은 병이다, §51.16 정정.)
BAL_FIXED = frozenset(("move", "quiet", "relead", "gesture", "event"))
#: 다중 해상도. 켜면 `set_grid` 가 격자를 **갈아 끼우지 않고 층을 쌓는다** — 20 / 10 /
#: 5 ms 의 B-스플라인을 동시에 두고 더한다. 거친 층이 제스처를, 고운 층이 미세구조를
#: 싣고 **모든 층이 C²** 다. DMP (Ijspeert; 음성 적용은 Parrell 2019) 가 임계감쇠
#: 2 차 점끌개 위에 강제항을 국소 기저의 가중합으로 펼치는 것과 같은 구성이고,
#: 계층적 B-스플라인(Forsey & Bartels 1988)의 표준 형태이기도 하다.
#:
#: 왜 층을 쌓아야 하는가: 갈아 끼우면 고운 격자가 거친 궤적을 **다시 표현**해야 하고,
#: 그 과정에서 거친 층이 갖고 있던 권한이 흩어진다. 쌓으면 거친 층은 그대로 서 있고
#: 고운 층은 잔차만 맡는다 — 생 이중적분(`out/L7`)이 2.1 단계에서 멈춰 버린 것과
#: 같은 조건수 문제를 피한다.
MOTION_MULTI = True

#: **액션의 기저** (MEASUREMENTS §51.25). 사용자: *"아직도 이상한 움직임들이 많은데... 액션 부분을
#: 수정해야 하나? 속도/가속도로는 도저히 안 되는 거 같기도 하고."*
#:
#: `"bspline"` (예전) 은 격자점의 **값**을 자유도로 둔다. 3 차 B-스플라인이라 위치·속도·가속도가 연속이지만,
#: **매끄러움은 흔들림을 막지 못한다** — 격자점들이 번갈아 오르내리면 매끄럽게 흔들리는 궤적이 그대로
#: 표현된다. 정칙화(`MOTION_L2`, `lam_smooth`)는 그 크기를 줄일 뿐 종류를 막지는 못한다.
#:
#: `"target"` 은 조음 문헌의 표준인 **목표 근사**다 (Birkholz 2011 `papers/2011_Birkholz_model_based_
#: articulatory_trajectories`, Saltzman & Munhall 의 과제 동역학, Xu 의 qTA). 격자값은 이제 **목표**이고,
#: 궤적은 그 목표를 향한 **임계감쇠 3 차 응답**이다:
#:
#:     x(t) = (h_τ * ZOH(목표))(t),   h_τ(t) = t² e^{−t/τ} / (2τ³)  (인과적, 단위 이득, 과충격 없음)
#:
#: 그러면 목표가 바뀌지 않는 동안 궤적은 **단조로운 접근**뿐이다 — 흔들림이 표현 자체가 안 된다. 흔들리려면
#: 목표가 번갈아 바뀌어야 하고, 목표의 개수는 격자가 정한다. τ 는 그 조음기의 시간상수이고 이미 생리로
#: 잰 표가 있다(`PARAM_TAU_PHYS`: 협착 2 ms, 포먼트 5~6, 후두 15, 호흡 30). 커널이 인과적이라 궤적이
#: 목표보다 **늦게** 따라가는 것도 물리적으로 맞다.
MOTION_MODE = "bspline"
#: 목표 근사에서 τ 를 못 찾은 파라미터의 기본 시간상수 [ms].
TARGET_TAU_DEFAULT = 8.0

#: **제스처 벌점** — 이웃 목표의 차에 거는 **L1** (MEASUREMENTS §51.25). 0 이면 끈다.
#:
#: 왜 L2 가 아니라 L1 인가: 실측으로 조음의 속도는 **드문드문**하다 — 목표 녹음의 F1 은 속도 첨도 32.7,
#: 변화량의 41 % 가 상위 10 % 프레임에 몰려 있다(멈춤–이동–멈춤). 우리 적합은 첨도 1.6, 30 % 로 **가우시안과
#: 같다** — 쉬지 않고 고르게 흔든다. 사용자가 들은 "이상한 움직임" 이 이것이다. L2(`lam_smooth`)는 큰 변화를
#: 벌하므로 움직임을 **고르게 펴고**, L1 은 변화의 **개수**를 벌하므로 몇 번의 큰 전이와 긴 멈춤으로 몬다
#: (전변동 정칙화의 조각상수 해). 조음 음운론의 제스처가 바로 그 모양이다.
GESTURE_W = 0.0

#: **스펙트럼 전이 항** (MEASUREMENTS §51.29). 목표와 합성의 **로그 멜 시간 미분**(Furui 의 회귀형 델타)을
#: 맞춘다. 0 이면 끈다.
#:
#: 왜: 손실 전수 조사에서 지금 손실은 **포먼트 전이가 20 ms 뭉개져도, 자음이 20 ms 밀려도 반응하지 않는다**
#: (env 변화 < 2 %). 반면 F0 3 %·음원 기울기 3 dB/oct 에는 55~60 % 반응한다. 즉 **정상 구간의 스펙트럼**만
#: 보고 **변화**를 안 본다. 그런데 Furui(1986)는 최대 스펙트럼 전이 구간이 음절 식별에 필요한 정보를 담는다고
#: 했고, 포먼트 전이는 조음 위치의 주 단서다. 최근 음성 향상 연구들도 자음 전이·파열·마찰 대역이 정상 모음보다
#: 언어 정보를 불균형하게 많이 담으며, 시간-주파수 가중 손실이 자음 복원을 개선한다고 보고한다.
#:
#: 델타는 ±`DELTA_K` 프레임의 회귀 계수다 (Furui 1986): Δ(t) = Σ_{k} k·x(t+k) / Σ k². 멜 프레임 간격이
#: 1.33 ms 이므로 K=8 이면 ±10.6 ms — 전이의 시간 규모다.
DELTA_W = 0.0
DELTA_K = 8
#: 전이 가중의 바닥 — 전이가 없는 칸도 이만큼은 본다 (목표 |Δ| 평균 대비).
DELTA_FLOOR = 0.2

#: **구간 균형 가중** (MEASUREMENTS §51.29). 켜면 포락 dB 항의 프레임 가중을 구간 종류(모음·마찰·비음·
#: 조용함·전이)마다 **같은 총량**이 되게 맞춘다. 0 이면 끈다(지금까지의 거동 = 길이에 비례).
#:
#: 왜: dB 항은 발화 전체의 평균이라 60 ms 짜리 마찰음(프레임의 4 %)은 묻힌다 — 실측에서 마찰 이득을 2 배
#: (+6 dB) 로 틀려도 손실이 2 % 미만으로 움직였다. 자음은 길이에 비해 정보가 많다.
SEG_BALANCE = 0.0

#: **대역 균형 가중** (MEASUREMENTS §51.29). 포락 dB 항의 멜 빈을 로그 간격 `BAND_GROUPS` 개 묶음으로 나누고
#: 묶음마다 평균한 뒤 묶음끼리 평균한다. 0 이면 예전처럼 빈 수에 비례한다.
#:
#: 왜: **소리 변화 1 dB 당** 손실 반응을 재면 음원 기울기 40.1, F0 30.5, F1 22.2 인데 **F2 8.3, F3 6.3,
#: 포먼트 전이 4.7, 자음 타이밍·레벨 0** 이다. 같은 크기의 소리 변화인데 반응이 0~40 배로 벌어진다 — 멜 빈이
#: 저역에 몰려 있고(멜 척도) 고역 오차가 그 수에 묻히기 때문이다. 음운 정체는 F2·F3 와 전이가 지고 있다.
BAND_BALANCE = 0.0
BAND_GROUPS = 8

#: **포먼트 자리 항** (MEASUREMENTS §51.29). 대역마다 목표와 합성의 **스펙트럼 무게중심**(Bark)을 맞춘다.
#: 0 이면 끈다.
#:
#: 왜: 손실에는 "포먼트" 라는 개념이 없다. 전대역 스펙트럼 거리는 **바꾼 빈의 수**에 지배되므로, 전 대역을
#: 조금씩 바꾸는 F0·음원 기울기가 봉우리 하나를 옮기는 F2 보다 항상 크게 잡힌다 — 소리 변화 1 dB 당 반응이
#: 기울기 40.1 / F0 30.5 / F1 22.2 대 **F2 8.3 / F3 6.3 / 전이 4.7** 이다. 대역 가중을 바꿔도 이 순서는 안
#: 뒤집힌다(실측: 대역 균형을 켜면 F2 9.0 → 7.2 로 오히려 준다). 모음의 정체는 포먼트 **자리**가 지므로
#: 자리를 직접 재는 항을 따로 둔다.
#:
#: 무게중심은 Chistovich 의 중심 무게 효과(3~3.5 Bark 안의 포먼트 쌍은 하나로 합쳐 들린다)와 같은 좌표이고,
#: 미분 가능하며 봉우리 찾기처럼 불연속이 아니다. 대역은 F1·F2·F3 의 탐색 범위다.
FORMANT_W = 0.0
FORMANT_BANDS = ((200.0, 1100.0), (800.0, 3000.0), (2000.0, 4500.0))

#: **공진 돌출 항** (MEASUREMENTS §51.33). 대역마다 **봉우리 − 골** 깊이를 목표와 맞춘다. 0 이면 끈다.
#:
#: 왜: 사용자는 *"원본은 공진이 매우 잘 느껴져서 자극적으로 딱 청취가 되는데, 우리가 만든 거는 퍼진
#: 소리 같고 산란이 많이 일어난 듯한 목소리, 가까이에서 듣는 느낌이 아니야"* 라고 했다. 켑스트럼
#: 포락(리프터 2 ms)에서 포먼트 봉우리와 이웃 골의 차를 재면 그대로 나온다 (`out/_tmp/prominence.py`,
#: 목표 / 합성 중앙 [dB]):
#:
#:     `L86/s040`  F1 17.7 / 19.9 (**+2.3**)   F2 9.5 / 7.5 (**−2.0**)   F3 8.7 / 5.7 (**−3.0**)
#:     `L86/s101`  F1 11.8 / 15.2 (+3.4)       F2 8.0 / 6.5 (−1.5)       F3 8.3 / 5.9 (−2.4)
#:
#: **F1 은 과하게 솟고 F2·F3 은 눌린다.** 3 dB 넘게 얕은 창이 F2 30~34 %, F3 38~43 % 고 최악은
#: −14.6 / −18.2 dB 다. 세 가지 청취 지적이 모두 여기로 모인다 — 공진이 안 서면 모음 정체가 흐려지고
#: (어눌함), 그 대역의 잡음 바닥이 상대적으로 도드라져 기식이 **따로** 들리고, 전체가 퍼져 들린다.
#:
#: 손실이 이것을 못 보는 이유는 §51.29 와 같다 — 멜 빈이 저역에 몰려 있어 소리 변화 1 dB 당 반응이
#: F1 22.2 / F2 8.3 / F3 6.3 이다. 적합기는 빈이 많은 F1 쪽에 정확도를 쓴다. 그래서 **깊이를 직접
#: 재는 항**을 따로 둔다. 자리를 재는 `FORMANT_W` 와 짝이다 (자리 + 깊이 = 공진).
#:
#: 부드러운 최대·최소는 log-sum-exp 다 — `PROM_BETA` [1/dB] 가 클수록 날카롭다. 유성 프레임만 본다.
#: **고역 물결 손실** `HFRIP_W` (MEASUREMENTS §51.53). 포락 손실은 **멜 띠** 위에서 재는데, 12 kHz 의 멜 띠는
#: 폭이 516 Hz, 17 kHz 는 750 Hz 다. 그런데 목표 고역 구조의 반높이 폭은 **281 Hz** 다 —
#: 즉 **포락 손실은 고역의 마루와 골을 한 띠 안에서 평균 내 버려 아예 못 본다.**
#:
#: 그래서 영점·마루 손잡이를 켜 줘도 적합기가 쓸 이유가 없었다. `M14` 에서 깊이의 중앙값이
#: **0.02** (거의 꺼짐) 이고 상한에 닿는 프레임이 0 % 였다. 사용자: *"영점 극도 더 강화해야 해."*
#: 손잡이의 범위가 아니라 **손실이 문제**다 — mix=1 이면 −18 dB 까지 파진다 (§51.51).
#:
#: 재는 법: 2048 점 스펙트럼의 dB 를 주파수 축에서 **띠통과**한다 — 좁은 평활(`HFRIP_NARROW_HZ`)
#: 이 F0 빗살(200 Hz 간격)을 지우고, 넓은 평활(`HFRIP_WIDE_HZ`)을 빼면 1~2 kHz 주기의 물결만
#: 남는다. 프레임마다 그 rms 를 재서 목표보다 **모자란 만큼**을 문다. 넘치는 쪽도 1/4 로 문다 —
#: 안 그러면 잡음의 무작위 요동까지 좇아 과적합한다.
HFRIP_W = 0.0
HFRIP_BAND = (4000.0, 16000.0)
HFRIP_NARROW_HZ = 400.0
HFRIP_WIDE_HZ = 1600.0
HFRIP_FFT = 2048
HFRIP_OVER = 0.25

#: **위상 단계의 조기 종료** (MEASUREMENTS §51.43). 최선값이 이만큼 정체하면 멈춘다. 0 이면 끈다.
#:
#: 왜: 위상 단계는 400 반복(한 판의 29 %)인데, 적합기가 `best` 스냅샷을 복원하므로 이 단계가 망친
#: 것은 어차피 버려진다. 실측(`M1/s040`): 2.4 단계 끝 89.47 / 82.74 -> 위상 단계 끝 85.79 / 66.90,
#: 그런데 최종 보고는 89.73 / 82.95 다. 즉 400 반복이 통째로 버려졌다.
#:
#: **그런데 60 은 틀린 값이었다** (§51.52). 위상 단계는 펄스 정렬을 갈아엎느라 **먼저 크게 파고
#: 나중에 갚는다**. `M1/s101` 의 궤적:
#:
#:     [0] 86.93 -> [100] **79.98** -> [200] 85.94 -> [250] 88.33 -> [399] **88.72**
#:
#: 손실도 1.334 -> 1.705 -> 1.268 -> 1.197 로 같은 모양이다. 회복에 **200 반복**이 걸리는데 60 에서
#: 끊으면 갚는 것을 영영 못 받는다 — `M13`·`M14`·`M15` 가 61 회에서 멈춰 `M1` 보다 1.7~3.1 점을
#: 잃었다. 사용자: *"속도 개선은 나중에 할 테니, 네가 할 수 있는 최대로 정확한 모델을 만들어."*
#: 그래서 **끈다**. 되살릴 거라면 250 아래로는 두지 않는다.
PHASE_PATIENCE = 0

#: **고역만 따로 적합하는 단계** (MEASUREMENTS §51.44). 반복 수. 0 이면 끈다.
#:
#: 사용자: *"고역만 따로 피팅을 해야 할 수준이다. 고역 피팅 매커니즘을 싹다 엎고 다시해라."*
#: 움직임 단계가 끝난 뒤 제어 궤적을 얼리고 **고역 전역 손잡이만**(`self.hf`: 고역 EQ·고역 극 위치·
#: 대역폭·이상와 영점·MVF·음원 EQ) `HF_STAGE_FMIN` 위의 멜 띠만 보는 손실로 적합한다. 궤적을 안
#: 건드리므로 저역을 망칠 수 없고, 열이 하나도 안 는다.
#: **펄스 이벤트 층** (MEASUREMENTS §51.45). 관측이 급히 움직이는 자리마다 **계단 하나**를 얹는다.
#: 계단은 `EVENT_FADE_MS` 안에 다 오르므로 B-스플라인 격자·τ·출발점 평활의 제약을 받지 않는다.
#:
#: 사용자: *"원래는 잠잠히 있다가 순간적으로 움직여야 하는 무브먼트가 많았는데, 우리가 괜히
#: 정규화를 걸어서 그게 스무스해지면서 인지 가능한 왜곡으로 변질된 게 아닌가 … 이벤트 파라미터가
#: 필요한가 생각을 하게 되네."* 실측이 그대로다 — 우리 기계의 최소 10~90 % 상승은 31 ms 인데
#: (격자 16 + τ 11 + 출발점 평활 24, 제곱합) 목표의 F2 는 가장 빠른 1 % 에서 **4~5 ms** 만에 20 %
#: 를 움직인다. 4 ms 도약은 자음 경계로 지나가지만 31 ms 글라이드는 미끄러짐으로 들린다.
#:
#: 열은 **이벤트 수 × `EVENT_PARAMS`** 로, 20 ms 격자(71 점 × 33 열)에 비하면 작다.
EVENT_W = 0.0
#: **`front_len` 과 `obstacle` 이 빠져 있었다** (§51.54). 치찰음의 공명은 포먼트가 아니라 **앞공동**이
#: 만든다 (`_front_cavity`: f_p = c/4L_front). 그런데 `front_len` 은 `--slow front_len=20` 으로 σ 20 ms
#: 가우시안에 묶여 있고 계단 층도 없었다. 실측(`M1/s040` 0.69 s 의 /s/): 목표의 2.5~16 kHz 무게중심이
#: 690 -> 700 ms 에 6424 -> **11358 Hz** 로 한 프레임에 뛰는데, 우리는 5508 -> 6281 -> 7231 -> 9869 로
#: **40 ms** 에 걸쳐 미끄러진다. 사용자가 되풀이해 말한 *"치찰음 포먼트가 실제 음원보다 늦게 적용되고"*
#: 가 이것이다 — 붙잡기 램프(§51.48)를 고쳐도 남았던 이유다.
EVENT_PARAMS = ("f1", "f2", "f3", "f4", "a_c", "tract_gain", "front_len", "obstacle")
#: 계단의 **10~90 % 상승 시간** [ms]. 사용자: *"이벤트 파라미터의 페이드를 1-2 ms 정도로 바꿔야 할 듯"*,
#: 그리고 앞서 *"펄스라고 해서 불연속적으로 움직이게 하라는 게 아니야. 다만, 페이드인/아웃이 매우 짧은
#: 그런 형태겠지."* 로지스틱의 척도는 여기서 나온다 — s = 상승 / (2·ln 9).
#: 그 뒤 *"이벤트 파라미터를 더 강화해야 할 거 같고(더 세고 빠른 펄스)"* 라 해서 1.0 으로 줄인다 —
#: 1 ms 프레임에서 계단이 두 프레임에 다 오르는 값이고, 그보다 빠르면 격자에서 그냥 불연속이다.
EVENT_FADE_MS = 1.0
#: 이벤트 자리는 `MOVE_FINE_PCT` 가 찾은 빠른 프레임의 **연속 덩어리마다 하나**다.
EVENT_MIN_GAP_MS = 12.0
#: 덩어리의 **총 도약**(로그 비)이 이보다 작으면 이벤트를 두지 않는다 — 진짜 분기만 잡는다.
#: 0.12 -> 0.08 ("더 세고 빠른 펄스"): 12 % 도약은 한 반음 반이라 자음 경계의 작은 것들을 놓쳤다.
EVENT_MIN_JUMP = 0.08
#: **이벤트를 액션 신호로 쓰면 강하게 벌한다** (MEASUREMENTS §51.45). 사용자: *"만약 펄스 신호를
#: 액션 신호로 구현하려는 조짐이 보이면 강한 벌점을 부과해."*
#:
#: 둘로 막는다. (1) 이벤트 하나의 크기를 그 자리에서 **관측된 도약**의 `EVENT_CAP_REL` 배 안으로
#: 가둔다 — 관측이 안 뛴 자리에서는 계단도 못 선다. (2) L1 을 걸어 쓰지 않는 이벤트는 0 으로 간다.
#: 벌점이라 `BAL_FIXED` 에 넣어 균형에서 뺀다(§51.26 의 1.2e+04 폭주를 피한다).
#: **벌점은 대칭이어야 한다** (사용자: *"액션을 이벤트에 피팅하거나 이벤트를 액션으로 피팅하거나
#: 둘 다 벌점이 부여되는 대칭성이 있는지도 봐야 해"*). 한쪽만 걸면 최적화가 반대쪽으로 새기만 한다 —
#: 실제로 이벤트 크기만 벌했더니 적합기가 이벤트를 안 쓰고 **부드러운 층으로 완만하게** 올렸다.
#:
#:   (가) 이벤트 -> 액션: 이벤트 크기가 그 자리의 관측 도약 × `EVENT_CAP_REL` 를 넘으면 벌한다.
#:   (나) 액션 -> 이벤트: **이벤트 자리에서 부드러운 층이 움직이면** 벌한다. 도약은 계단이 져야 한다.
EVENT_PEN_W = 80.0
#: 1.2 -> 2.0. 상한은 **관측된 도약의 몇 배까지 계단이 설 수 있는가**다. 관측 궤적 자체가 분석기의
#: 평활을 거쳐 이미 완만해져 있으므로, 목표의 진짜 도약은 관측보다 크다 — 위 무게중심 실측이 그
#: 증거다(관측 궤적은 40 ms 에 걸쳐 오르는데 소리는 10 ms 에 뛴다). 2 배까지 허용한다.
EVENT_CAP_REL = 2.0
EVENT_L1 = 0.05
#: (나) 의 창 반폭 [ms] 과 공짜 폭 — 이 안에서 부드러운 층이 이만큼 넘게 움직이면 벌한다.
EVENT_RAMP_MS = 10.0
EVENT_RAMP_FREE = 0.20
#: (나) 쪽 가중 — 같은 비율의 위반이면 (가) 와 비슷한 값이 되도록 맞춘다.
EVENT_RAMP_W = 3.0

HF_STAGE_ITERS = 0
HF_STAGE_FMIN = 4000.0

PROM_W = 0.0
#: 고역 둘을 더 둔다 (MEASUREMENTS §51.43). 손실이 4.8 kHz 위의 **구조**를 한 번도 요구하지 않아서
#: 적합기가 고역 극의 대역폭을 범위의 넓은 끝(×2.43)까지 밀어 평평하게 만들고 있었다 — 켑스트럼
#: 포락의 봉우리−골이 5~10 kHz 에서 목표 11.8 dB 대 합성 6.6 dB 다. 극을 새로 풀 것이 아니라
#: **이미 있는 전역 손잡이(`hf_log_bw`)에게 구조를 요구**하면 된다. 열이 하나도 안 는다.
PROM_BANDS = ((250.0, 1300.0), (900.0, 3000.0), (2000.0, 4800.0),
              (4800.0, 10000.0), (10000.0, 16000.0))
PROM_BETA = 3.0

#: **파라미터별 가장 고운 층** [ms] (움직임 모드). 여기 적힌 파라미터는 그보다 고운 층에서
#: 움직이지 못한다 — 그 층의 해당 열을 0 으로 둔다.
#:
#: 왜: 사용자가 들은 치찰음의 "원인 모를 노이즈" 를 `out/L7` 만 피했다. `L7` 은 매개화가
#: 너무 세서 점수는 무너졌지만, 마찰 구간에서 소음 계열 파라미터가 1 ms 마다 제 값의
#: 몇 %씩 움직이는지 재면 (s040 / s101):
#:
#:     fric_gain   L6 19.28/9.79   **L7 0.18/0.01**   L18 5.32/5.23
#:     tract_gain  L6 19.84/11.75  **L7 0.08/0.07**   L18 5.81/3.37
#:     aspiration  L6  5.57/7.36   **L7 0.14/0.08**   L18 0.87/0.96
#:     a_c (필터)   L6  8.30/9.89   **L7 1.74/2.67**   L18 3.20/4.53
#:
#: **`L7` 은 잡음 이득이 고요하면서 필터(`a_c`)는 여전히 움직였다.** 사용자가 앞서 말한
#: "치찰음은 노이즈는 그대로 방출하고 필터만 움직여야 한다" 가 거기서 우연히 실현돼
#: 있었다. 그 성질만 가져온다 — 잡음 이득은 거친 층(20 ms, B-스플라인이면 ~25 Hz
#: 위를 죽인다)에서만 움직이고, 필터는 고운 층까지 그대로 쓴다. 전부 얼린 `L7` 은
#: s101 에서 오히려 나빴으므로(마찰 150~400 Hz 변조 5.76 배) 전부를 가져오지는 않는다.
MOTION_SLOW: dict[str, float] = {}

#: **파라미터별 시상수** [ms] — 적합기가 더하는 **보정분**을 파라미터마다 그 조음기의
#: 관성으로 저역통과한다 (가우시안, σ = 시상수). 비면 끈다.
#:
#: 왜: 사용자 — *"사실상 모든 파라미터에 각각 존재해야 하는 거 알지? 거의 모든 파라미터가
#: 궤적이 각각 다를 거 아니야."* 궤적 자체는 이미 파라미터마다 따로다 (`w` 의 열마다
#: B-스플라인). 그런데 **빠르기의 상한이 공유**돼 느려야 할 것들이 빨라졌다. 궤적 변조
#: 에너지의 90 %가 드는 주파수 (`yang_00000040`, 분석 초기값 → 적합 `out/L25`):
#:
#:     p_sub      4.9 → **31.7 Hz**   (6.5 배 — 폐압이 30 Hz 로 흔들린다)
#:     adduction  6.3 → **16.9 Hz**
#:     f3/f4     20   → 35~38 Hz
#:
#: 분석이 잰 출발점(`u0`)은 그대로 두고 **보정분만** 거른다 — 측정된 세부(예: f0 의 실제
#: 흔들림)는 보존하고, 적합기가 다른 것을 흉내 내려고 느린 변수를 떠는 것만 막는다.
#: 가우시안 σ 의 −3 dB 모서리는 약 0.13/σ 이므로 30 ms ≈ 4.4 Hz, 15 ms ≈ 8.8 Hz,
#: 5 ms ≈ 27 Hz, 2 ms ≈ 66 Hz. 값은 생리에서 잡았다 (호흡 < 후두 < 조음 < 폐쇄·개방).
PARAM_TAU_MS: dict[str, float] = {}

#: **출발점(`u0`)도 조음 대역으로 거른다** — `MOTION_SLOW` 에 적힌 파라미터의 분석 궤적을 가우시안
#: σ = `BASE_SMOOTH` × (그 파라미터의 가장 고운 층 ms) 로 저역통과한다. 0 이면 끈다.
#:
#: 왜: `MOTION_SLOW`·`PARAM_TAU_MS` 는 적합기가 더하는 **보정분**만 묶는다. 제어는 `u0 + 보정분` 이라
#: 분석 궤적의 프레임별 흔들림은 그대로 지나간다. 실측(`L48/s101`, 유성 조각의 궤적 스펙트럼): 분석
#: F2·F3·F4 의 움직임 에너지 가운데 **22~30 % 가 20~60 Hz** 에 있고(포먼트 추적기의 잡음), 20 ms 층만
#: 허용한 적합 궤적도 29~40 % 로 그대로다. 그 흔들림이 종속 가지의 고역 수준(∝ ΠF_k²)을 흔들어 모음
#: 고역 5~12 kHz 의 20~60 Hz 변조를 목표보다 +12.4 dB(s101)·+8.4 dB(s040) 크게 만들었다 — 음원 쪽
#: (배음 위상 분산·기식·지터·시머)을 0 으로 해 다시 렌더해도 ±0.5 dB 안이었고, F1~F4·대역폭만 σ 9 ms
#: 로 펴면 +3.9 / +3.3 dB 로 내려갔다(`out/_tmp/ablate.py`). 20 ms 층의 가우시안 대응은 σ ≈ 0.45 × 20 ms
#: (20 Hz 에서 −5 dB, 40 Hz 에서 −22 dB). 폐쇄·개방 시각을 쥔 `BASE_SMOOTH_KEEP` 은 거르지 않는다.
BASE_SMOOTH = 0.0

#: 마찰 소스 고역 절벽(`FricationNoise.log_lp_ratio`, 정점의 1.6~6.4 배)을 파일 전역으로 적합한다.
#: 그동안 "적합기가 건드리지 않는 내부값" 이었다 — 그러면 치찰음 12~19 kHz 수준을 폐압·협착(제트 속도)
#: 으로만 움직일 수 있어, 폐압을 느리게 묶은 판에서 치찰음이 7~22 dB 어두워졌다(§51.8).
FRIC_LP_FIT = False
BASE_SMOOTH_KEEP = frozenset(("a_c", "oral_open", "f0_target"))
PARAM_TAU_PHYS: dict[str, float] = {
    "a_c": 2.0,                                              # 폐쇄·개방은 빠르다
    "f0_target": 3.0,                                        # 억양 (지터는 따로 있다)
    "f1": 6.0, "f2": 5.0, "f3": 5.0, "f4": 5.0,              # 포먼트 전이
    "front_len": 8.0, "obstacle": 8.0,
    "bw1": 10.0, "bw2": 10.0, "bw3": 10.0, "bw4": 10.0,
    "nasal_f": 10.0, "nasal_f2": 10.0, "nasal_f3": 10.0, "nasal_z": 10.0,
    "velum": 15.0, "oral_open": 15.0, "back_leak": 15.0,
    "adduction": 15.0,                                       # 후두
    "rd_offset": 20.0, "tilt": 20.0, "tract_gain": 20.0,
    "p_sub": 30.0,                                           # 호흡
    "jitter": 30.0, "shimmer": 30.0,
}
#: `--slow-noise` 가 쓰는 묶음.
SLOW_NOISE = {"fric_gain": 20.0, "tract_gain": 20.0, "aspiration": 20.0}
#: `--noise-global` 이 쓰는 묶음. **잡음 이득을 시간 제어가 아니라 화자 눈금으로 만든다.**
#:
#: 왜: 사용자가 들은 "10 kHz 위 전부가 배음 없이 평탄하다" 는 **마찰(앞공동) 잡음이 모음
#: 구간으로 샌 것**이었다. 유성 프레임 10 kHz 위에서 경로별로 재면(`out/L21`) 앞공동이
#: 성문 경로보다 9~17 dB 위였고, `fric_gain` 을 끄면 s101 의 10~12 kHz 가 −12.6 dB 로
#: 떨어진다. 엔진은 이미 물리로 막고 있다 — 마찰 구동이 레이놀즈 수와 `0.1 / a_c` 로
#: 게이트되어 열린 모음은 조용하다. 그런데 그 위에 **시간에 따라 움직이는** `fric_gain` 을
#: 곱하니, 손실이 고역을 보게 되자(밴드별 바닥) 적합기가 물리가 줄인 마찰을 이득으로
#: 되살려 가장 싼 광대역 채움재로 썼다.
#:
#: `fric_gain`·`aspiration` 은 분석 초기값이 전 구간 상수(1.0)이고 `GLOBAL_PARAMS` 라
#: 전역 오프셋이 있다. 움직임 층에서 그 열을 **모든 층에서** 막으면 정확히 전역 스칼라 하나가
#: 된다 — 마찰의 시간 구조는 협착(`a_c`)·레이놀즈 수·방전이 정하고, 적합기는 `a_c` 를
#: 움직여 마찰을 켜고 끈다. 사용자가 처음부터 말한 "노이즈는 물리대로, 필터만 움직인다" 다.
#: `tract_gain` 은 음절 따라 레벨이 변해야 하므로 느리게만 둔다.
#: 비강 가지의 이득(`nasal_damp`·`nasal_gain`)도 느리게 둔다 — 마찰 이득을 막았더니
#: 적합기가 그리로 옮겨 가 `nasal_damp` 가 60 Hz 위에 80 %를 두었다 (`out/L22/s040`).
#: 이득 계열은 전부 느리거나 전역이어야 하고, 빨리 움직일 자격은 조음기에만 있다.
NOISE_GLOBAL = {"fric_gain": float("inf"), "aspiration": float("inf"), "tract_gain": 20.0,
                "nasal_damp": 20.0, "nasal_gain": 20.0}

CONT_W = 0.0
CONT_LAG = 4               # 프레임. 256 창의 hop 이 64 샘플이라 4 = 창 하나 (5.3 ms)
CONT_KNEE_DB = 0.5         # 이만큼의 악화는 공짜 (계측 잡음)

#: **유성 구간의 스펙트럼 첨예도** (사용자 요청). 포락 일치율은 이것을 볼 수 없다 —
#: 멜 48 밴드는 6 kHz 에서 폭이 700~900 Hz 인데 포먼트 간격은 c/(2L) ≈ 1199 Hz 라
#: 봉우리와 골이 한 밴드 안에서 평균되어 사라진다 (§37).
#:
#: 실측 비(적합/목표): 포먼트 대비 0.867~0.932, 하모닉 첨예 0.850~0.927. 두 자 다
#: 적합이 **덜 첨예**하다고 말한다.
#:
#: **한쪽 방향으로만 문다.** 목표보다 뭉툭할 때만 벌하고 날카로울 때는 공짜다 —
#: 양쪽으로 물면 목표보다 날카로운 해를 만들어 낼 이유가 생기고, 그건 그것대로
#: 인공적이다.
SHARP_W = 0.0
SHARP_WIN_HZ = 2000.0      # 포먼트 대비: 극 간격보다 넓어야 봉우리와 골이 모두 남는다
SHARP_LO_HZ, SHARP_HI_HZ = 300.0, 12000.0
HARM_WIN_HZ = 900.0        # 하모닉 첨예: 긴 창(2048)에서 F0 빗살이 보이는 폭
HARM_LO_HZ, HARM_HI_HZ = 100.0, 4000.0

#: **F0 아래의 진동** (사용자 요청). 실측(`out/fix/s040`): 목표는 F0 아래가 기준대역
#: 대비 −20.99 dB 인데 적합은 −9.31 dB — **+11.7 dB 초과**다 (s101 +2.5, out/pole 은
#: +15.0 까지 나빠졌다).
#:
#: **"F0 아래를 금지" 가 아니라 "목표보다 초과한 만큼" 을 문다.** 사용자가 지적한
#: 대로 금지하면 성대 프라이나 서브하모닉까지 억누른다 — 목표에 진짜 프라이가 있으면
#: 목표의 F0 아래 레벨도 높으므로 벌점이 저절로 0 이 된다. 기준 F0 는 **목표의 것**을
#: 쓴다 (적합된 F0 를 쓰면 벌점이 제 꼬리를 문다).
SUBF0_W = 0.0
SUBF0_HI = 0.75            # F0 의 이 배율 아래를 "F0 아래" 로 본다
SUBF0_REF = (0.9, 3.0)     # 기준 대역 (F0 배율)
SUBF0_KNEE_DB = 1.0

BW_LAW_W = 0.0
#: 허용 배수의 로그. 0.3 이면 법칙의 ×/÷ 1.35 배까지는 사실상 공짜다 — 화자·모음마다
#: 실제로 그만큼 흔들리기 때문이다(기식도, 성문 손실). 그 밖은 값을 치른다.
BW_LAW_TOL = 0.3

F_MARGIN_HZ = 120.0        # 인접 포먼트 최소 간격

#: 비유한 기울기가 났을 때 어느 파라미터·격자점인지 찍는다 (느리지 않다 — 났을 때만).
DEBUG_NONFINITE = bool(os.environ.get("FORMANT_ML_DEBUG_NONFINITE"))

# 프레임별 적합의 **한 걸음 크기**(raw 좌표). Adam 은 기울기 크기와 무관하게 lr 만큼
# 걷기 때문에, 모든 파라미터에 같은 lr 을 주면 F0 가 한 걸음에 11 % 씩 뛴다(실측:
# 그래서 2 단계가 52 % -> 8 % 로 무너졌다). 성문 위상은 누적합이라 F0 를 한 프레임
# 흔들면 그 뒤 전부가 흔들린다. 물리적으로 말이 되는 분해능으로 걸음을 잘라 준다.
STEP: dict[str, float] = {
    # **음향 곡률로 잰 값** (MEASUREMENTS §51.27). Adam 은 좌표마다 기울기를 정규화하므로 한 걸음의 크기가
    # |Δw| ≈ η 로 일정하고, raw 좌표에서는 |Δu| = η·STEP 이다. 최적점 근처에서 L ≈ L* + ½ΣH_ii Δu_i² 이므로
    # **한 걸음이 손실을 바꾸는 양은 ½H_ii η² STEP_i²** — 즉 Adam 은 기울기 규모만 고르게 할 뿐 곡률
    # 불균형은 고치지 못한다. 균형 조건은 STEP_i ∝ 1/√H_ii 다.
    #
    # 같은 raw 걸음 δ=0.025 에서의 |Δ포락| D_i 를 두 파일에서 재어(기하평균) H_ii ∝ D_i 를 얻었다. D 가 δ²
    # 에 비례함을 확인했다(δ 를 2 배 하면 3.2~4.6 배, 이론 4). 잰 값: f0 5.18, adduction 1.83, f1 0.80,
    # voice_gain 0.69, p_sub 0.69, tract_gain 0.22, f2 0.18, bw1 0.036, f3 0.014, rd_offset 0.008.
    #
    # **줄이기만 한다.** 둔감한 쪽의 걸음을 키우면 손실은 안 나빠지지만 그 파라미터가 표류한다 — 식이
    # 예측한 f3 의 표류가 100 걸음에 62 % 였다. 그래서 STEP_i = min(예전, C/√D_i) 만 적용한다.
    # 효과(한 걸음의 |Δ포락|): adduction 1.586 → 0.083, p_sub 2.622 → 0.149, voice_gain 1.503 → 0.191.
    # 표류 예측도 함께 줄어든다: p_sub 이 100 걸음에 1.84 → 0.50 cmH₂O (관측된 떠돎이 ±2~3 이었다).
    "f0_target": 0.10, "adduction": 0.17, "p_sub": 0.27, "voice_gain": 0.27, "tract_gain": 0.48,
    "f1": 0.25, "f2": 0.25, "f3": 0.3, "f4": 0.3,
    "bw1": 0.5, "bw2": 0.5, "bw3": 0.5, "bw4": 0.5,
    "velum": 0.5, "oral_open": 0.5, "a_c": 0.5, "front_len": 0.3,
    "lat_mix": 0.5, "lat_bw": 0.3,
    "jitter": 0.5, "shimmer": 0.5,
    "nasal_f": 0.3, "nasal_f2": 0.3, "nasal_f3": 0.3, "nasal_z": 0.3,
}
STEP_DEFAULT = 1.0

# 제어 격자 성김 -> 촘촘함 (ms). 1 ms 부터 풀면 200×28 개의 자유도가 잡음을 좇는다.
GRID_MS = (20.0, 10.0, 5.0, 1.0)

DEFAULT_PARAMS = (
    "p_sub", "adduction", "f0_target", "rd_offset", "tilt", "aspiration",
    "jitter", "shimmer",
    # **F1~F4 만 자유다.** F5~F8 을 자유 파라미터로 풀어 봤다가 되돌렸다 (§40):
    # 열이 32 → 40 개가 되자 같은 예산으로 2.4 단계가 회복을 못 했고 (87.45 → 85.31,
    # `out/pole` 은 같은 자리에서 88.81 → 90.63), 위상 단계가 66.79 % / 43.94 % 로
    # 무너졌다. 위쪽 포먼트는 **자유도가 아니라 물리**다 — 극 간격 c/(2L) 는 조음과
    # 무관한 절대 제약이고, 실제로 5 번 모드부터 무작위 면적 함수 8 종에서 그 20 Hz
    # 안에 든다 (§35.1). `_formant_tracks` 가 "빠진 포먼트 = 직전 + c/(2L)" 로 만든다.
    "f1", "f2", "f3", "f4", "bw1", "bw2", "bw3", "bw4",
    # F5~F8 은 `--hf-free` 일 때만 더한다 (copyfit 이 붙인다). 기본은 위 주석대로 물리에 맡긴다.
    "tract_gain", "a_c", "fric_gain", "obstacle", "back_leak", "front_len",
    "velum", "oral_open", "nasal_f", "nasal_f2", "nasal_f3", "nasal_z",
    "nasal_damp", "nasal_gain",
    # 켰다 끄는 보조 공진의 깊이 (tract.AUX_POLES, §51.40). 기본 0 = 투명이라 꺼져 있으면 무해하다.
    "aux1_mix", "aux2_mix", "aux3_mix", "aux4_mix", "aux5_mix", "aux6_mix", "aux7_mix",
    "azr1_mix", "azr2_mix", "azr3_mix", "azr1_f", "azr2_f", "azr3_f",
    # 고역 영점 (tract.HF_ZEROS, §51.50). 마찬가지로 깊이 0 = 투명이다.
    "hzr1_mix", "hzr2_mix", "hzr3_mix", "hzr1_f", "hzr2_f", "hzr3_f",
    "hzp1_mix", "hzp2_mix", "hzp3_mix", "hzp1_f", "hzp2_f", "hzp3_f",
    # 곁가지(side branch)를 적합 대상에 넣는다. 이게 빠져 있으면 설측·비음처럼
    # 곁가지가 정의적 특징인 조음을 **원리적으로 못 맞춘다** (실측 §8.4).
    "lat_mix", "lat_bw",
    # 성문 배음만의 빠른 이득 — 주기별 진폭(시머)을 잡음을 썰지 않고 따라간다 (0.4 기반).
    "voice_gain",
    # 앞공동 Q (tract.FRONT_Q_CTRL). 켜지 않으면 copyfit 이 얼린다.
    "front_q",
)


def _stft(x: torch.Tensor, n: int, win: torch.Tensor) -> torch.Tensor:
    return torch.stft(x, n_fft=n, hop_length=n // 4, win_length=n, window=win,
                      center=True, return_complex=True, pad_mode="reflect")


def mel_bank(n_fft: int, fs: float, n_mels: int = 80,
             fmin: float = 60.0, fmax: float | None = None) -> torch.Tensor:
    """삼각 멜 필터뱅크 (n_mels, n_fft//2+1). 포락 일치율용."""
    fmax = fmax or fs / 2
    def hz2m(f): return 2595.0 * np.log10(1.0 + f / 700.0)
    def m2hz(m): return 700.0 * (10.0 ** (m / 2595.0) - 1.0)
    m = np.linspace(hz2m(fmin), hz2m(fmax), n_mels + 2)
    f = m2hz(m)
    bins = np.fft.rfftfreq(n_fft, 1.0 / fs)
    w = np.zeros((n_mels, len(bins)))
    for i in range(n_mels):
        lo, ct, hi = f[i], f[i + 1], f[i + 2]
        left = (bins - lo) / max(ct - lo, 1e-9)
        right = (hi - bins) / max(hi - ct, 1e-9)
        w[i] = np.clip(np.minimum(left, right), 0.0, None)
        s = w[i].sum()
        if s > 0:
            w[i] /= s
    return torch.as_tensor(w, dtype=torch.float32)


@dataclass
class FitReport:
    env: float                         # 포락 일치율 % (멜 크기의 스펙트럼 수렴도)
    fine: float                        # 정밀 일치율 % (선형 다해상도)
    db: float                          # 멜 대역 평균 |오차| dB — 가장 읽기 쉬운 값
    per_size: dict[int, float]
    loss: float
    iters: int
    history: list[tuple[float, float]] = field(default_factory=list)

    def __str__(self) -> str:
        per = " ".join(f"{n}:{v:.1f}" for n, v in sorted(self.per_size.items()))
        return (f"포락 {self.env:5.2f}%  정밀 {self.fine:5.2f}%   평균오차 {self.db:.2f} dB  "
                f"(손실 {self.loss:.4f}, {self.iters} 회)  [{per}]")


class CopySynthFitter:
    def __init__(self, engine, target: np.ndarray, sr: int, init: ControlTrack,
                 params: tuple[str, ...] = DEFAULT_PARAMS,
                 lam_smooth: float = 3e-3, lam_prior: float = 1e-4,
                 lam_l1: float = 0.0,
                 phase_weight: float = 0.0, pulse_weight: float = 1.0,
                 n_mels: int = 48, device: str = "cpu",
                 room_ir: np.ndarray | None = None):
        self.eng = engine
        self.hop = engine.cfg.hop
        self.fs = engine.cfg.sample_rate
        self.device = device
        self.lam_smooth, self.lam_prior = lam_smooth, lam_prior
        self.lam_l1 = float(lam_l1)
        self.phase_weight = phase_weight
        self.pulse_weight = pulse_weight
        self._bal: dict = {}          # 균형 모드의 유효 가중 (비면 원래 가중)
        self._collect = False         # True 면 loss() 가 항을 self._terms 에 모은다
        self._terms: dict = {}
        self.room_ir = (None if room_ir is None else
                        torch.as_tensor(np.asarray(room_ir, np.float32), device=device))
        self._last_pulse = float("nan")
        self._last_flux = float("nan")
        self._flux_db = None
        self.flux_live = None
        self._mel_err = self._mel_raw_db = self._harm_db = None
        self.sizes = list(FFT_SIZES)

        # 녹음의 원래 나이퀴스트. **손실에서 그 위를 보면 안 된다.**
        # 44.1 kHz 녹음을 48 kHz 로 올리면 22.05~24 kHz 가 정확히 비어 있고, 게다가
        # 그 아래 16~22 kHz 도 녹음 장비의 안티에일리어싱이 만든 값이다. 그걸 목표로
        # 두면 적합기가 "저 위를 비워라" 를 물리 파라미터로 달성하려 들고, 그 왜곡이
        # 8~12 kHz 를 10 dB 어둡게 만들었다(실측: 남성 /사/).
        # 손실 압축을 거친 음원은 sr 이 48 kHz 여도 대역이 잘려 있다 (orphan 코퍼스
        # 실측: 전부 20 kHz 에서 급락). 그 빈 대역을 손실이 보는 것은 원리적으로 나쁘다.
        #
        # **그런데 자동 검출은 채택하지 않는다.** `turbulence.effective_bandwidth` 를
        # 만들어 재 봤더니 녹음에서는 정확했지만(20.1 kHz) 합성 신호의 자연스러운 고역
        # 롤오프를 컷으로 오인했다(9.96 → 조건을 두 번 조인 뒤에도 11.1 kHz). 잘못 자르면
        # **진짜 신호를 버린다** — 20~21.6 kHz 를 안 보는 이득보다 손해가 크다.
        # 그 함수는 진단용으로 남기고, 여기서는 표본화율만 쓴다.
        self.f_max = min(0.5 * float(sr), 0.5 * self.fs) * 0.90
        if sr != self.fs:
            from scipy.signal import resample_poly
            g = math.gcd(int(sr), int(self.fs))
            target = resample_poly(target, self.fs // g, int(sr) // g)
        # **분석이 붙여 준 부가 정보(성문 펄스·유성 마스크)를 같이 옮긴다.** 새로
        # ControlTrack 을 만들면서 빠뜨리면 조용히 기본값(빈 배열)이 되고, 마찰 이득
        # 보정이 아무 일도 안 하게 된다 (실측: 남 /사/ 무성부가 −40.8 dB 인 채로 시작).
        self.track = ControlTrack(init.values.copy(), init.frame_ms, list(init.events),
                                  pulses=np.asarray(getattr(init, "pulses", np.zeros(0))).copy(),
                                  voiced=np.asarray(getattr(init, "voiced",
                                                            np.zeros(0, dtype=bool))).copy(),
                                  fricative=np.asarray(getattr(init, "fricative",
                                                               np.zeros(0, dtype=bool))).copy(),
                                  bursts=np.asarray(getattr(init, "bursts",
                                                            np.zeros(0, dtype=int))).copy())
        self.track["residual_mix"] = 0.0
        n = self.track.n_frames * self.hop
        t = np.zeros(n)
        t[:min(n, len(target))] = target[:min(n, len(target))]
        self.target = torch.as_tensor(t, dtype=torch.float32, device=device).unsqueeze(0)

        base = torch.as_tensor(self.track.values, dtype=torch.float64, device=device)
        self.base = base
        self.names = [p for p in params
                      if not (PARAMS[p].log and float(base[:, INDEX[p]].abs().min()) == 0.0)]
        self.cols = torch.tensor([INDEX[p] for p in self.names], device=device)
        self.specs = [PARAMS[p] for p in self.names]
        if FORMANT_BAND:
            # **포먼트마다 제 구역을 준다** (MEASUREMENTS §51.38).
            import dataclasses as _dc
            self.specs = [(_dc.replace(sp, lo=max(sp.lo, FORMANT_BAND[nm][0]),
                                       hi=min(sp.hi, FORMANT_BAND[nm][1]))
                           if nm in FORMANT_BAND else sp)
                          for nm, sp in zip(self.names, self.specs)]
        u0 = torch.stack([self._to_raw(base[:, INDEX[p]], PARAMS[p]) for p in self.names], 1)
        if MOTION and BASE_SMOOTH > 0.0 and MOTION_SLOW:
            u0 = self._smooth_base(u0)
        self.u0 = u0.detach()
        self.scale = torch.tensor([STEP.get(n, STEP_DEFAULT) for n in self.names],
                                  dtype=torch.float64, device=device)
        self.n_frames = u0.shape[0]
        self.stride = 1
        self.w = torch.zeros_like(u0).requires_grad_(True)   # (Tc, P_fit) 격자 위 증분
        self.w_coarse: list = []        # 다중 해상도의 굳은 거친 층들 (여전히 학습된다)
        self.d = torch.zeros(len(self.names), dtype=torch.float64, device=device,
                             requires_grad=True)          # 파라미터별 전역 오프셋
        self.w_ev = None
        if EVENT_W > 0.0:
            self._build_events(device)
        self.d_mask = torch.tensor([1.0 if n in GLOBAL_PARAMS else 0.0
                                    for n in self.names], dtype=torch.float64,
                                   device=device)
        self.prior_w = torch.tensor([PRIOR_W.get(n, 1.0) for n in self.names],
                                    dtype=torch.float64, device=device)
        # 자유 포먼트만. F5~F8 은 `_formant_tracks` 가 c/(2L) 사다리로 만들므로
        # 순서가 구조적으로 보장된다 — 벌점을 걸 대상이 아니다.
        self.f_idx = [i for i, n in enumerate(self.names)
                      if len(n) == 2 and n[0] == "f" and n[1].isdigit()]
        self.log_gain = torch.zeros(1, dtype=torch.float64, device=device,
                                    requires_grad=True)

        # 목표의 성문 폐쇄 시각 -> 샘플 색인 (있으면 위상 고정에 쓴다)
        pl = np.asarray(getattr(init, "pulses", np.zeros(0)), dtype=np.float64)
        pl = pl[(pl >= 0.0) & (pl * self.fs < n - 1)]
        self.pulse_idx = torch.as_tensor((pl * self.fs).astype(np.int64), device=device)
        self.pulse_phi0 = torch.zeros(1, dtype=torch.float64, device=device,
                                      requires_grad=True)
        self._pulse_phase = self._build_pulse_phase() if PULSE_LOCK else None
        self.wins = {k: torch.hann_window(k, device=device) for k in FFT_SIZES}
        # 포락은 **짧은 창**에서 잰다. 1024 (21 ms) 는 F0 240 Hz 의 하모닉을 분해하므로
        # 그 위의 멜은 포락이 아니라 하모닉 정렬을 재게 된다.
        # 256 (5.3 ms, 분해능 187 Hz) 이면 하모닉이 뭉개져 순수한 포락이 남는다.
        self.mel = mel_bank(MEL_FFT, self.fs, n_mels, fmax=self.f_max).to(device)
        # 선형 SC 도 같은 상한을 쓴다.
        self.bin_max = {k: int(math.ceil(self.f_max / (self.fs / k))) + 1 for k in FFT_SIZES}
        # 목표의 조화/잔차 분해. 위상 항(조화부만 본다)과 크기 항(난류부는 기대
        # 스펙트럼으로 본다)이 같은 것을 쓰므로 한 번만 만든다.
        # **η 를 먼저 정한다.** 아래의 모든 크기·로그가 이 값을 쓴다. 목표의 평균
        # 크기에 비례시키므로 녹음 레벨과 무관하게 같은 뜻을 갖는다. 여기 한 번만
        # 쓰는 `.abs()` 는 `no_grad` 안이라 기울기가 없다.
        with torch.no_grad():
            _m = _stft(self.target, MEL_FFT, self.wins[MEL_FFT]).abs().mean()
            self.mag_eta = float(MAG_ETA_REL * _m.clamp_min(1e-12))
        self._har_res = self._decompose_target()
        self._floor_amp = None
        if FLOOR_NOISE:
            self._floor_amp = self._floor_shape()
        with torch.no_grad():
            self._harm_w = {k: self._harmonic_weight(k) for k in FFT_SIZES}
            self._noise_w = {k: (1.0 - self._harm_w[k]) for k in FFT_SIZES}
            self._sm_frames = {k: max(1, int(round(NOISE_EXPECT_MS
                                                   / (1000.0 * (k // 4) / self.fs))))
                               for k in FFT_SIZES}
            raw = {k: self._cabs(_stft(self.target, k, self.wins[k]))[:, :self.bin_max[k]]
                   for k in FFT_SIZES}
            self.tgt_S = {k: self._expect(v, k) for k, v in raw.items()}
            self.tgt_M = self.mel[:, :self.tgt_S[MEL_FFT].shape[1]] @ self.tgt_S[MEL_FFT]
            self.tgt_Mdb = self._db(self.tgt_M)
            self.db_floor = float(self.tgt_Mdb.max()) - DB_RANGE
            self._ripple_setup(raw.get(HFRIP_FFT))
            self.db_floor_band = self._band_floor(self.tgt_Mdb)
            # **플럭스 통계는 `_expect` 를 지나기 전의 날것에서 잰다.** 평활된
            # 스펙트럼에서 재면 재려는 흔들림이 이미 지워져 있다.
            raw_mel = self.mel[:, :raw[MEL_FFT].shape[1]] @ raw[MEL_FFT]
            self.tgt_flux, self.flux_live = self._flux_stat(self._db(raw_mel),
                                                            make_mask=True)
            self._prepare_voice_stats(raw)
        self._prepare_corr()
        # **화자의 고역 손실은 적합 대상이다.** `log_extra_bw`(고차 극 보정의 대역폭
        # 배율)와 `log_front_bw`(앞공동 대역폭 배율)는 `nn.Parameter` 인데 어느
        # 옵티마이저 목록에도 없어서 영원히 0(= 배율 1)에 머물렀다 (§37.3 이 지적).
        # 고차 극이 고정 위치라 화자의 고역을 맞출 수단이 사실상 이 둘뿐인데 그마저
        # 잠겨 있었다 — 실측으로 8.9 kHz 가 −5.5 dB, 15.9 kHz 가 −4.7 dB 모자란다.
        self.hf = [q for q in (getattr(self.eng.tract, "log_extra_bw", None),
                               getattr(self.eng.tract, "log_front_bw", None),
                               None if MVF_FROZEN else getattr(getattr(self.eng, "aspiration", None), "log_mvf", None),
                               getattr(self.eng.tract, "hf_log_df", None),
                               getattr(self.eng.tract, "hf_log_bw", None),
                               getattr(self.eng.tract, "hf_eq_db", None)
                               if _tract_mod.HF_EQ else None,
                               getattr(self.eng.tract, "pir_log_f", None),
                               getattr(self.eng.tract, "pir_depth", None),
                               getattr(getattr(self.eng, "frication", None), "log_lp_ratio", None)
                               if FRIC_LP_FIT else None,
                               getattr(self.eng.tract, "open_damp", None) if _tract_mod.OPEN_DAMP else None,
                               getattr(self.eng.glottis, "hjit_log", None) if _glottis_mod.HJIT_FIT else None,
                               getattr(self.eng.glottis, "src_eq_db", None) if _glottis_mod.SRC_EQ else None,
                               getattr(self.eng.tract, "open_f1", None) if _tract_mod.OPEN_DAMP else None)
                   if isinstance(q, torch.nn.Parameter)]
        if SPEAKER_LOCK:
            # **화자 상수는 발화마다 다시 적합하지 않는다** (MEASUREMENTS §51.35).
            self.hf = []
        self.calibrate_gain()

    # ------------------------------------------------------- 재매개화
    @staticmethod
    def _to_raw(v: torch.Tensor, spec) -> torch.Tensor:
        lo, hi = spec.lo, spec.hi
        if spec.log:
            x = (torch.log(v.clamp_min(1e-9)) - math.log(lo)) / (math.log(hi) - math.log(lo))
        else:
            x = (v - lo) / (hi - lo)
        return torch.log(x.clamp(1e-4, 1 - 1e-4) / (1 - x.clamp(1e-4, 1 - 1e-4)))

    @staticmethod
    def _to_val(u: torch.Tensor, spec) -> torch.Tensor:
        x = torch.sigmoid(u)
        lo, hi = spec.lo, spec.hi
        if spec.log:
            return torch.exp(math.log(lo) + x * (math.log(hi) - math.log(lo)))
        return lo + x * (hi - lo)

    def _delta(self) -> torch.Tensor:
        """격자 위 증분 -> 프레임별 raw 증분.

        `MOTION` 이 꺼져 있으면 예전대로 격자 값의 선형 보간이다. 켜면 `w` 를
        **가속도**로 읽고 두 번 적분한다 (윗쪽 `MOTION` 주석이 근거다).
        """
        w = self._expand(self._mask_slow(self.w))
        for c in self.w_coarse:
            w = w + self._expand(self._mask_slow(c))
        if PARAM_TAU_MS:
            w = self._smooth_cols(w)
        w = w * self.scale
        self._w_smooth = w
        if EVENT_W > 0.0 and getattr(self, "w_ev", None) is not None:
            # **계단은 평활 뒤에 더한다** — τ·열 평활을 안 받아야 4 ms 안에 오른다.
            w = w.index_add(1, self._ev_cols, (self._ev_basis @ self.w_ev) * EVENT_W)
        return w

    def _smooth_base(self, u0: torch.Tensor) -> torch.Tensor:
        """분석 출발점을 `MOTION_SLOW` 의 층에 맞춰 가우시안 저역통과 (σ = `BASE_SMOOTH` × 층 ms, 끝은 복제)."""
        fm = float(self.track.frame_ms)
        out = u0.clone()
        for i, n in enumerate(self.names):
            ms = MOTION_SLOW.get(n, 0.0)
            # 무한대 = 파일 전역 상수(`NOISE_GLOBAL`) — 층이 하나도 없다. 출발점도 상수라 거를 것이 없다.
            if not (0.0 < ms < float("inf")) or n in BASE_SMOOTH_KEEP:
                continue
            sig = BASE_SMOOTH * ms / fm
            half = max(1, int(math.ceil(3.0 * sig)))
            t = torch.arange(-half, half + 1, dtype=u0.dtype, device=u0.device)
            ker = torch.exp(-0.5 * (t / sig) ** 2)
            ker = (ker / ker.sum()).view(1, 1, -1)
            x = torch.nn.functional.pad(u0[:, i].view(1, 1, -1), (half, half), mode="replicate")
            sm = torch.nn.functional.conv1d(x, ker)[0, 0]
            keep = self._fast_obs_mask(u0.shape[0], u0.dtype, u0.device)
            out[:, i] = sm * (1.0 - keep) + u0[:, i] * keep
        return out

    def _build_events(self, device) -> None:
        """관측이 급히 움직이는 자리마다 계단 하나. 기저 (T, n_ev) 와 계수 (n_ev, P_ev)."""
        T = int(self.base.shape[0])
        cols = [i for i, n in enumerate(self.names) if n in EVENT_PARAMS]
        fo = self._fast_obs_frames()
        if not cols or fo.size == 0:
            return
        # **덩어리마다 하나, 그 덩어리의 봉우리에.** 첫 프레임에 놓으면 전이의 시작이라 계단이
        # 목표보다 이르게 선다. 덩어리의 **총 도약**을 같이 기록해 벌점의 기준으로 쓴다.
        gap = max(1, int(round(EVENT_MIN_GAP_MS / float(self.track.frame_ms))))
        runs, cur = [], [int(fo[0])]
        for i in fo[1:]:
            if int(i) - cur[-1] <= gap:
                cur.append(int(i))
            else:
                runs.append(cur); cur = [int(i)]
        runs.append(cur)
        spd = self._fast_obs_speed()
        ev, jump = [], []
        for r in runs:
            # **덩어리의 속도 중앙값** 자리에 세운다. 봉우리에 세우면 목표보다 늦다 — 전이는 앞이
            # 가파르고 뒤가 길어서 봉우리가 움직임의 한가운데보다 뒤에 있다 (사용자: *"치찰음
            # 포먼트가 실제 음원보다 늦게 적용되고"*). 누적 속도가 절반을 넘는 자리가 계단의
            # 50 % 지점과 맞는 자리다. 첫 프레임은 전이의 시작이라 반대로 이르다.
            cs = np.cumsum(spd[r])
            j = int(r[int(np.searchsorted(cs, 0.5 * cs[-1]))]) if cs[-1] > 0 else int(r[0])
            tot = float(np.sum(spd[r[0]:r[-1] + 1])) * float(self.track.frame_ms) / 100.0
            if tot < EVENT_MIN_JUMP:
                continue
            ev.append(j); jump.append(tot)
        if not ev:
            return
        # 부드러운 계단 — 10 % 에서 90 % 까지 `EVENT_FADE_MS` 에 오른다 (로지스틱).
        # 척도는 0.25 프레임 밑으로 안 내린다: 그 밑은 프레임 격자에서 그냥 불연속이라 클릭이 된다.
        sc = max(EVENT_FADE_MS / (2.0 * math.log(9.0) * float(self.track.frame_ms)), 0.25)
        t = np.arange(T)[:, None]
        # 지수를 ±60 으로 자른다 — 그 바깥은 배정도에서 정확히 0 / 1 이고, 안 자르면 exp 가 넘친다.
        z = np.clip((t - np.asarray(ev)[None, :]) / sc, -60.0, 60.0)
        B = 1.0 / (1.0 + np.exp(-z))
        self._ev_basis = torch.as_tensor(B, dtype=torch.float64, device=device)
        self._ev_cols = torch.as_tensor(cols, dtype=torch.long, device=device)
        self.w_ev = torch.zeros(len(ev), len(cols), dtype=torch.float64,
                                device=device, requires_grad=True)
        self._ev_n = len(ev)
        self._ev_jump = torch.as_tensor(jump, dtype=torch.float64, device=device)
        self._ev_idx = torch.as_tensor(ev, dtype=torch.long, device=device)

    def _fast_obs_mask(self, T: int, dtype, device) -> torch.Tensor:
        """**관측이 빠르게 움직이는 프레임은 출발점을 안 뭉갠다** (MEASUREMENTS §51.44).

        `BASE_SMOOTH` 는 출발점의 흔들림을 막으려고 넣었는데, 목표가 실제로 가진 **빠른 도약까지**
        지웠다. 실측: `--base-smooth 0.45` (σ 9 ms) 만으로 10~90 % 상승이 24 ms 아래로 못 내려가고,
        20 ms 격자(16 ms)·τ 5 ms(11 ms)까지 겹치면 **31 ms** 다. 그런데 목표의 F2 는 가장 빠른 1 %
        에서 **4~5 ms** 만에 20 % 를 움직인다. 4 ms 도약은 자음 경계로 들리고 지나가지만 31 ms
        글라이드는 음높이가 미끄러지는 것으로 또렷이 들린다 — 사용자가 지적한 "괜히 정규화를 걸어서
        인지 가능한 왜곡으로 변질된" 것이 이것이다.

        `MOVE_FINE_PCT` 가 0 이면 예전처럼 전부 뭉갠다.
        """
        if MOVE_FINE_PCT <= 0.0 or BASE_SMOOTH_KEEP_FAST <= 0.0:
            return torch.zeros((T,), dtype=dtype, device=device)
        if getattr(self, "_fobm", None) is not None and self._fobm.shape[0] == T:
            return self._fobm.to(dtype)
        fo = self._fast_obs_frames()
        m = np.zeros(T)
        if fo.size:
            half = max(1, int(round(BASE_SMOOTH_KEEP_FAST / float(self.track.frame_ms))))
            for i in fo:
                m[max(int(i) - half, 0):int(i) + half + 1] = 1.0
        self._fobm = torch.as_tensor(m, dtype=dtype, device=device)
        return self._fobm

    def _smooth_cols(self, w: torch.Tensor) -> torch.Tensor:
        """열(파라미터)마다 `PARAM_TAU_MS` 의 시상수로 가우시안 저역통과. 끝은 복제로 채운다."""
        fm = float(self.track.frame_ms)
        out = w
        groups: dict[float, list[int]] = {}
        for i, n in enumerate(self.names):
            tau = PARAM_TAU_MS.get(n, 0.0)
            if tau > 0.0:
                groups.setdefault(float(tau), []).append(i)
        for tau, cols in groups.items():
            sig = tau / fm                                   # 프레임 단위 σ
            half = max(1, int(math.ceil(3.0 * sig)))
            t = torch.arange(-half, half + 1, dtype=w.dtype, device=w.device)
            ker = torch.exp(-0.5 * (t / sig) ** 2)
            ker = (ker / ker.sum()).view(1, 1, -1)
            idx = torch.tensor(cols, device=w.device)
            x = w[:, idx].t().unsqueeze(1)                   # (C, 1, T)
            x = torch.nn.functional.pad(x, (half, half), mode="replicate")
            y = torch.nn.functional.conv1d(x, ker)[:, 0].t()  # (T, C)
            out = out.index_copy(1, idx, y)
        return out

    def _mask_slow(self, q: torch.Tensor) -> torch.Tensor:
        """`MOTION_SLOW` 에 적힌 파라미터를 그보다 고운 층에서 0 으로 둔다."""
        if not (MOTION and MOTION_SLOW):
            return q
        grid_ms = self.track.frame_ms * self.n_frames / max(q.shape[0] - 1, 1)
        keep = torch.tensor([0.0 if MOTION_SLOW.get(n, 0.0) > grid_ms + 0.5 else 1.0
                             for n in self.names], dtype=q.dtype, device=q.device)
        if not BURST_FINE:
            return q * keep
        near = self._burst_coeffs(q.shape[0], q.dtype, q.device)
        if near is None:
            return q * keep
        col = torch.tensor([1.0 if n in BURST_PARAMS else 0.0 for n in self.names],
                           dtype=q.dtype, device=q.device)
        # 파열 창 안에서는 느린 층 마스크를 무시한다 (`BURST_FINE` 참조).
        return q * torch.clamp(keep.unsqueeze(0) + near.unsqueeze(1) * col.unsqueeze(0), max=1.0)

    def _fast_obs_speed(self) -> np.ndarray:
        """프레임별 관측 속도 — `MOVE_FINE_PARAMS` 가운데 최대 |Δ log| [%/ms]."""
        if getattr(self, "_fos", None) is not None:
            return self._fos
        T = int(self.base.shape[0])
        out = np.zeros(T)
        for nm in MOVE_FINE_PARAMS:
            if nm not in INDEX:
                continue
            x = np.asarray(self.track[nm], dtype=np.float64)[:T]
            if x.size < 4 or not np.all(x > 0):
                continue
            d = np.abs(np.diff(np.log(x))) * 100.0 / float(self.track.frame_ms)
            out[:-1] = np.maximum(out[:-1], d)
        self._fos = out
        return out

    def _fast_obs_frames(self) -> np.ndarray:
        """분석 궤적이 ms 당 `MOVE_FINE_PCT` % 넘게 움직이는 프레임. `MOVE_FINE_PCT` 참조."""
        if getattr(self, "_fof", None) is not None:
            return self._fof
        T = int(self.base.shape[0])
        out = np.zeros(T, bool)
        for nm in MOVE_FINE_PARAMS:
            if nm not in INDEX:
                continue
            x = np.asarray(self.track[nm], dtype=np.float64)[:T]
            if x.size < 4 or not np.all(x > 0):
                continue
            d = np.abs(np.diff(np.log(x))) * 100.0 / float(self.track.frame_ms)
            out[:-1] |= d > MOVE_FINE_PCT
        self._fof = np.flatnonzero(out)
        return self._fof

    def _burst_coeffs(self, kc: int, dtype, device) -> torch.Tensor | None:
        """이 층의 계수 가운데 파열 창(±`BURST_WIN_MS`) 안에 있는 것 = 1."""
        b = np.asarray(getattr(self.track, "bursts", np.zeros(0, dtype=int)), int)
        fo = self._fast_obs_frames() if MOVE_FINE_PCT > 0.0 else np.zeros(0, dtype=int)
        if b.size == 0 and fo.size == 0 or kc < 2:
            return None
        key = (kc, str(device), dtype)
        if getattr(self, "_bc_key", None) == key:
            return self._bc
        pos = np.arange(kc) * (self.n_frames - 1) / max(kc - 1, 1)
        fm_ = float(self.track.frame_ms)
        m = np.zeros(kc)
        if b.size:
            m = np.maximum(m, (np.abs(pos[:, None] - b[None, :]).min(1)
                               <= BURST_WIN_MS / fm_).astype(np.float64))
        if fo.size:
            m = np.maximum(m, (np.abs(pos[:, None] - fo[None, :]).min(1)
                               <= MOVE_FINE_WIN_MS / fm_).astype(np.float64))
        self._bc_key, self._bc = key, torch.as_tensor(m, dtype=dtype, device=device)
        return self._bc

    def _expand(self, w: torch.Tensor) -> torch.Tensor:
        if w.shape[0] == self.n_frames and not MOTION:
            return w
        if MOTION and MOTION_MODE == "target":
            return self._target_approach(w)
        if MOTION:
            return self._bspline(w)
        return torch.nn.functional.interpolate(
            w.t().unsqueeze(0), size=self.n_frames, mode="linear",
            align_corners=True)[0].t()

    def _target_kernels(self, dtype, device):
        """파라미터를 τ 로 묶고 임계감쇠 3 차 커널을 만든다 -> [(열 색인, 커널)]. `MOTION_MODE` 참조."""
        key = (str(device), dtype)
        if getattr(self, "_tk_key", None) == key:
            return self._tk
        fm = float(self.track.frame_ms)
        groups: dict[float, list[int]] = {}
        for i, n in enumerate(self.names):
            tau = float(PARAM_TAU_PHYS.get(n, TARGET_TAU_DEFAULT))
            groups.setdefault(max(tau, 0.5), []).append(i)
        out = []
        for tau, cols in groups.items():
            s = tau / fm
            L = max(3, int(math.ceil(7.0 * s)))
            t = torch.arange(L, dtype=dtype, device=device)
            h = (t * t) * torch.exp(-t / s)
            h = h / h.sum().clamp_min(1e-12)
            out.append((torch.tensor(cols, device=device), h.flip(0).view(1, 1, -1)))
        self._tk_key, self._tk = key, out
        return out

    def _target_approach(self, w: torch.Tensor) -> torch.Tensor:
        """격자 **목표값** -> 프레임 궤적. 영차유지 뒤 임계감쇠 3 차 응답 (`MOTION_MODE == "target"`)."""
        tc, n = w.shape[0], self.n_frames
        pos = torch.arange(n, device=w.device, dtype=w.dtype) * (tc - 1) / max(n - 1, 1)
        idx = pos.floor().long().clamp(0, tc - 1)
        zoh = w[idx]                                     # (T, P) 목표는 구간마다 상수
        out = zoh
        for cols, h in self._target_kernels(w.dtype, w.device):
            x = zoh.index_select(1, cols).t().unsqueeze(0)          # (1,C,T)
            pad = h.shape[-1] - 1
            x = torch.nn.functional.pad(x, (pad, 0), mode="replicate")
            c = x.shape[1]
            y = torch.nn.functional.conv1d(x, h.expand(c, 1, -1), groups=c)[0].t()
            out = out.index_copy(1, cols, y)
        return out

    def _bspline(self, w: torch.Tensor) -> torch.Tensor:
        """격자 값 -> 프레임 값, **균일 3 차 B-스플라인** (C²).

        선형 보간은 격자점마다 **가속도가 불연속**이라 난류 진폭의 계단을 만든다
        (HANDOFF §11.1 이 `phones.min_jerk` 를 만들며 실측한 것이고, 그때 적합기에는
        안 붙였다). 3 차 B-스플라인은 위치·속도·가속도가 전부 연속이므로 다음
        구간이 앞 구간의 속도와 가속도를 **계승**한다 — 사용자가 요구한 "한 창에서
        정적 물리 변수가 아니라 움직임을 기술한다" 가 이 형태다.

        **생 이중적분(자유 파라미터를 가속도로) 은 먼저 시험했고 실패했다.** 국소
        수정에 가속도 쌍극자가 필요해 조건수가 나쁘고, 격자를 촘촘히 해도 한
        격자점의 권한이 사라진다 — 실측(`out/L7`)에서 2.1 단계 68.30 에서 2.4 단계
        69.79 로 **격자 정교화가 아무 일도 못 했다** (같은 설정의 `out/L6` 은
        83.92 → 91.34). B-스플라인은 기저가 국소라 그 병이 없고, `w` 가 여전히
        값이므로 `STEP`·`PRIOR_W`·`scale` 눈금을 그대로 쓸 수 있다.
        """
        tc, n = w.shape[0], self.n_frames
        u = torch.linspace(0.0, float(tc - 1), n, device=w.device, dtype=w.dtype)
        i0 = torch.floor(u).clamp(0, tc - 1)
        f = (u - i0).unsqueeze(1)
        i0 = i0.long()

        def at(k):
            return w[(i0 + k).clamp(0, tc - 1)]

        f2, f3 = f * f, f * f * f
        b0 = (1.0 - 3.0 * f + 3.0 * f2 - f3) / 6.0
        b1 = (4.0 - 6.0 * f2 + 3.0 * f3) / 6.0
        b2 = (1.0 + 3.0 * f + 3.0 * f2 - 3.0 * f3) / 6.0
        b3 = f3 / 6.0
        return b0 * at(-1) + b1 * at(0) + b2 * at(1) + b3 * at(2)

    def _u(self) -> torch.Tensor:
        return self.u0 + self._delta() + self.d * self.d_mask

    def set_grid(self, grid_ms: float) -> int:
        """제어 격자 간격을 바꾼다. 현재 해를 보간해서 옮기므로 이어서 최적화된다."""
        if MOTION:
            grid_ms = max(float(grid_ms), MOTION_MIN_GRID_MS)
        stride = max(1, int(round(grid_ms / self.track.frame_ms)))
        tc = max(2, int(math.ceil(self.n_frames / stride)))
        if MOTION and MOTION_MULTI:
            # **층을 쌓는다.** 같은 해상도를 다시 요구받으면(격자 하한에 걸린 경우)
            # 층을 늘리지 않는다 — 같은 기저를 두 번 두면 중복 매개화가 된다.
            if self.w.shape[0] == tc:
                self.stride = stride
                return tc
            # **프레임 해상도의 층은 쌓지 않고 버린다.** 초기 `w` 는 1 ms 격자라,
            # 그것을 층으로 남기면 프레임별 자유가 그대로 살아 있어 움직임 매개화가
            # 무의미해진다 (실측: 그 층을 남겼을 때 `nasal_f3` 의 48 %가 60 Hz 위였다).
            if self.w.shape[0] < self.n_frames:
                self.w_coarse.append(self.w)
            self.w = torch.zeros(tc, self.w.shape[1], dtype=self.w.dtype,
                                 device=self.w.device).requires_grad_(True)
            self.stride = stride
            return tc
        with torch.no_grad():
            old = self.w.detach()
            new = torch.nn.functional.interpolate(
                old.t().unsqueeze(0), size=tc, mode="linear", align_corners=True)[0].t()
        self.w = new.clone().requires_grad_(True)
        self.stride = stride
        return tc

    def control(self) -> torch.Tensor:
        u = self._u()
        cols = torch.stack([self._to_val(u[:, i], s) for i, s in enumerate(self.specs)], 1)
        if FAST_ONLY:
            cols = self._fast_only(cols)
        if HOLD_FRIC:
            cols = self._hold(cols)
        if Q_BAND:
            cols = self._q_band(cols)
        c = self.base.clone().index_copy(1, self.cols, cols).unsqueeze(0)
        self._last_ctrl = c                      # 움직임 예산이 본다 (float64 그대로)
        return c.to(torch.float32)

    def _fast_only(self, cols: torch.Tensor) -> torch.Tensor:
        """`FAST_ONLY` 열에서 추세를 빼고 유성 마스크를 곱한다 — 빠른 요동만 남는다."""
        idx = [i for i, n in enumerate(self.names) if n in FAST_ONLY]
        if not idx:
            return cols
        if getattr(self, "_fo_ker", None) is None:
            sig = FAST_ONLY_MS / float(self.track.frame_ms)
            half = max(1, int(math.ceil(3.0 * sig)))
            t = torch.arange(-half, half + 1, dtype=cols.dtype, device=cols.device)
            k = torch.exp(-0.5 * (t / sig) ** 2)
            self._fo_ker = (k / k.sum()).view(1, 1, -1)
            v = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)), float)
            T = int(cols.shape[0])
            if v.size < T:
                v = np.pad(v, (0, T - v.size))
            self._fo_voi = torch.as_tensor(v[:T], dtype=cols.dtype, device=cols.device)
        half = (self._fo_ker.shape[-1] - 1) // 2
        out = cols
        for i in idx:
            x = cols[:, i]
            xx = torch.nn.functional.pad(x.view(1, 1, -1), (half, half), mode="replicate")
            trend = torch.nn.functional.conv1d(xx, self._fo_ker.to(x.dtype))[0, 0]
            out = out.index_copy(1, torch.tensor([i], device=cols.device),
                                 ((x - trend) * self._fo_voi).unsqueeze(1))
        return out

    def _q_band(self, cols: torch.Tensor) -> torch.Tensor:
        """`Q_BAND` 에 적힌 포먼트의 대역폭을 `f/Qmax` ~ `f/Qmin` 안으로 부드럽게 가둔다."""
        if getattr(self, "_qb", None) is None:
            pairs = []
            for nm, (qlo, qhi) in Q_BAND.items():
                bw = f"bw{nm[1:]}"
                if nm in self.names and bw in self.names:
                    pairs.append((self.names.index(nm), self.names.index(bw),
                                  float(qlo), float(qhi)))
            self._qb = pairs
        if not self._qb:
            return cols
        out = cols
        for i_f, i_b, qlo, qhi in self._qb:
            fk = out[:, i_f]
            lo, hi = fk / qhi, fk / qlo          # Q 가 크면 대역폭이 작다
            b = out[:, i_b]
            # 부드러운 상자 — 로그 공간에서 tanh 로 접는다 (미분 가능, 경계에서 포화)
            mid, half = torch.sqrt(lo * hi), 0.5 * torch.log(hi / lo)
            y = mid * torch.exp(half * torch.tanh(torch.log(b.clamp_min(1e-6) / mid) / half))
            out = out.index_copy(1, torch.tensor([i_b], device=cols.device), y.unsqueeze(1))
        return out

    def _hold_setup(self, dtype, device) -> None:
        """마찰 구간마다의 고원 마스크와 구간 평균 연산자. `HOLD_FRIC` 참조."""
        T = int(self.base.shape[0])
        fm = np.asarray(getattr(self.track, "fricative", np.zeros(0, dtype=bool)), bool)
        if fm.size < T:
            fm = np.pad(fm, (0, T - fm.size))
        fm = fm[:T]
        # 두 가장자리까지의 거리 [프레임] — 비마찰 프레임은 0. 시작 쪽과 끝 쪽을 **따로** 센다.
        d_in, d_out = np.zeros(T), np.zeros(T)
        run = 0.0
        for i in range(T):
            run = run + 1.0 if fm[i] else 0.0
            d_in[i] = run
        run = 0.0
        for i in range(T - 1, -1, -1):
            run = run + 1.0 if fm[i] else 0.0
            d_out[i] = run
        fm_ms = float(self.track.frame_ms)
        r_in = max(1.0, HOLD_RAMP_IN_MS / fm_ms)
        r_out = max(1.0, HOLD_RAMP_MS / fm_ms)
        m = np.minimum(np.clip(d_in / r_in, 0.0, 1.0), np.clip(d_out / r_out, 0.0, 1.0))
        d = np.minimum(d_in, d_out)
        # 구간마다 열 하나. 고원 값은 **m 으로 가중한** 구간 평균이라 가장자리가 끌어내리지 않는다.
        cols_r = []
        i = 0
        while i < T:
            if fm[i]:
                j = i
                while j + 1 < T and fm[j + 1]:
                    j += 1
                w = np.zeros(T)
                w[i:j + 1] = m[i:j + 1]
                if w.sum() > 0:
                    cols_r.append(w / w.sum())
                else:                                   # 램프보다 짧은 구간은 붙잡지 않는다
                    m[i:j + 1] = 0.0
                i = j + 1
            else:
                i += 1
        self._hold_m = torch.as_tensor(m, dtype=dtype, device=device)
        if cols_r:
            P = np.stack(cols_r, 1)                      # (T, R) 구간 평균을 뽑는 가중
            S = (P > 0).astype(np.float64)               # (T, R) 구간 소속
            self._hold_P = torch.as_tensor(P, dtype=dtype, device=device)
            self._hold_S = torch.as_tensor(S, dtype=dtype, device=device)
        else:
            self._hold_P = self._hold_S = None
        self._hold_idx = [i for i, n in enumerate(self.names) if n in HOLD_PARAMS]

    def _hold(self, cols: torch.Tensor) -> torch.Tensor:
        """마찰 구간 안에서 `HOLD_PARAMS` 를 그 구간의 고원 값으로 끌어당긴다."""
        if getattr(self, "_hold_m", None) is None:
            self._hold_setup(cols.dtype, cols.device)
        if not self._hold_idx or self._hold_P is None:
            return cols
        m = self._hold_m.to(cols.dtype)
        P, S = self._hold_P.to(cols.dtype), self._hold_S.to(cols.dtype)
        out = cols
        for i in self._hold_idx:
            x = cols[:, i]
            lx = torch.log(x.clamp_min(1e-9))
            plateau = S @ (P.transpose(0, 1) @ lx)       # 구간마다 제 평균으로 채운다
            y = torch.exp(lx * (1.0 - m) + plateau * m)
            out = out.index_copy(1, torch.tensor([i], device=cols.device), y.unsqueeze(1))
        return out

    def _rms(self, x: torch.Tensor, mask: np.ndarray | None) -> float:
        if mask is None or not mask.any():
            return float(x.pow(2).mean().sqrt()) + 1e-12
        i = np.flatnonzero(np.repeat(mask, self.hop)[:x.shape[-1]])
        return float(x[..., i].pow(2).mean().sqrt()) + 1e-12

    def calibrate_gain(self) -> float:
        """초기 이득을 닫힌 형태로 준다. **유성/무성을 따로 맞춘다.**

        하나의 이득만 맞추면 유성과 무성 중 한쪽이 반드시 크게 어긋난다. 마찰 세기는
        `fric_gain` 이 정하는데 그 초기값이 1.0 이고, 실측과의 차이가 40 dB 를 넘으면
        경사 하강으로는 못 간다 (한 걸음이 상한 64 = 36 dB 안에서 움직인다). 실제로
        남 /사/ 는 마찰부가 −42.8 dB 인 채로 시작해 끝까지 못 따라잡았다.
        그래서 전체 이득은 **유성 프레임**에서, `fric_gain` 은 **무성 프레임**에서
        각각 닫힌 형태로 준다.
        """
        voi = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)))
        if voi.shape[0] != self.n_frames:
            voi = np.zeros(0, dtype=bool)
        # `fric_gain` 은 **마찰 프레임**에서만 잰다. 폐쇄(비음 머머 등)를 섞으면
        # 조용한 구간까지 마찰로 메우려 든다.
        fri = np.asarray(getattr(self.track, "fricative", np.zeros(0, dtype=bool)))
        if fri.shape[0] != self.n_frames:
            fri = ~voi if voi.size else np.zeros(0, dtype=bool)
        unv = fri
        with torch.no_grad():
            self.eng.reset()
            y = self.eng(self.control(), self.track.events, 0.0)["audio"]
            m = voi if voi.any() else None
            r = self._rms(self.target, m) / self._rms(y, m)
            self.log_gain.copy_(torch.tensor([math.log(r)], dtype=torch.float64,
                                             device=self.device))
            if unv.any() and "fric_gain" in self.names:
                self.eng.reset()
                y = self.eng(self.control(), self.track.events, 0.0)["audio"] * r
                need = self._rms(self.target, unv) / self._rms(y, unv)
                j = self.names.index("fric_gain")
                sp = self.specs[j]
                g0 = float(self._to_val(self.u0[:, j], sp).median())
                tgt = float(np.clip(g0 * need, sp.lo + 1e-3, sp.hi - 1e-3))
                # raw 좌표의 전역 오프셋으로 넣는다 (프레임별 모양은 보존)
                d = float(self._to_raw(torch.tensor([tgt], dtype=torch.float64), sp)[0]
                          - self._to_raw(torch.tensor([g0], dtype=torch.float64), sp)[0])
                self.d[j] = d / max(float(self.d_mask[j]), 1.0) if self.d_mask[j] else 0.0
                if not self.d_mask[j]:      # fric_gain 은 전역 대상이어야 한다
                    self.d[j] = d
        return self.gain_db()

    # ------------------------------------------------------------ 손실
    def synth(self, want_phase: bool = False):
        """엔진을 돌리고, **녹음 경로**가 주어졌으면 그것까지 통과시킨다.

        `room_ir` 은 방·마이크·코덱을 녹음에서 추정한 필터다 (engine/room.py). 이걸
        순방향에 넣으면 적합기가 그 응답을 성도·소스 파라미터로 흡수하지 않는다 —
        추정되는 물리량이 **마른 목소리**의 것이 된다. 위상(`out["phase"]`)은 성문
        위상이라 방을 안 통과한다: 펄스 항은 소스의 시각을 거는 것이므로 그대로 둔다.
        """
        self.eng.reset()
        pp = None if self._pulse_phase is None else self._pulse_phase + self.pulse_phi0
        out = self.eng(self.control(), self.track.events, 0.0, pulse_phase=pp)
        y = out["audio"] * torch.exp(self.log_gain).to(torch.float32)
        self._amp = out.get("amp")               # 무성 벌점이 쓴다 (샘플률 떨림 진폭)
        self._re = out.get("reynolds")           # 미래를 보는 레이놀즈 전이 항이 쓴다
        if self.room_ir is not None:
            y = room.apply_ir(y, self.room_ir)
        if self._floor_amp is not None:          # 녹음 바닥 — 방을 지난 뒤, 마이크 앞
            fl = self._floor(y.shape[-1])
            if fl is not None:
                y = y + fl.to(y.device)
        return (y, out["phase"]) if want_phase else y

    def _floor_shape(self) -> torch.Tensor:
        """녹음 바닥 잡음의 rfft 진폭 모양 (n//2+1,). 백색 잡음에 곱해 목표와 같은 바닥을 낸다."""
        from .denoise import noise_profile
        t = self.target[0].detach().cpu().double().numpy()
        nf = 2048
        prof = noise_profile(t, int(self.fs), n_fft=nf, hop=512, quantile=0.10)
        n = t.shape[0]
        fr = np.fft.rfftfreq(n, 1.0 / self.fs)
        fp = np.fft.rfftfreq(nf, 1.0 / self.fs)
        shape = np.sqrt(np.interp(fr, fp, prof) + 1e-30)
        # 수준 맞춤: 같은 모양의 잡음을 한 번 만들어 같은 자로 재고 비를 곱한다.
        rng = np.random.default_rng(0)
        z = np.fft.irfft(np.fft.rfft(rng.standard_normal(n)) * shape, n)
        got = noise_profile(z, int(self.fs), n_fft=nf, hop=512, quantile=0.5)
        k = (fp > 200) & (fp < 0.45 * self.fs)
        shape *= np.sqrt(np.median(prof[k]) / max(np.median(got[k]), 1e-30))
        return torch.as_tensor(shape, dtype=torch.float64, device=self.target.device)

    def _floor(self, n: int) -> torch.Tensor | None:
        if self._floor_amp is None:
            return None
        w = self.eng.noise.white("floor", 0, n, 1, torch.float64, self.target.device)[0]
        amp = self._floor_amp
        if amp.shape[0] != n // 2 + 1:
            return None
        return torch.fft.irfft(torch.fft.rfft(w) * amp, n).to(torch.float32).unsqueeze(0)

    def synth_dry(self) -> torch.Tensor:
        """녹음 경로를 빼고 렌더한다 — 이게 **목소리 자체**다."""
        self.eng.reset()
        pp = None if self._pulse_phase is None else self._pulse_phase + self.pulse_phi0
        out = self.eng(self.control(), self.track.events, 0.0, pulse_phase=pp)
        return out["audio"] * torch.exp(self.log_gain).to(torch.float32)

    def _build_pulse_phase(self) -> torch.Tensor | None:
        """목표 폐쇄 시각에서 만든 샘플률 위상 (1, N) [rad, 누적]. `PULSE_LOCK` 참조."""
        n = int(self.target.shape[-1])
        idx = self.pulse_idx.detach().cpu().numpy().astype(np.int64)
        idx = idx[(idx >= 0) & (idx < n)]
        if idx.size < 3:
            return None
        f0 = np.asarray(self.track["f0_target"], dtype=np.float64)
        f0s = np.repeat(np.clip(f0, 50.0, 800.0), self.hop)[:n]
        if f0s.size < n:
            f0s = np.pad(f0s, (0, n - f0s.size), mode="edge")
        inc = 2.0 * np.pi * f0s / self.fs
        lo, hi = int(0.002 * self.fs), int(0.025 * self.fs)
        for a, b in zip(idx[:-1], idx[1:]):
            L = int(b - a)
            if lo <= L <= hi:                       # 그럴듯한 한 주기만 — 무성 틈은 f0 로 잇는다
                inc[a:b] = 2.0 * np.pi / L
        ph = np.cumsum(inc)
        ph -= ph[idx[0]]                            # 첫 폐쇄에서 위상 0
        return torch.as_tensor(ph[None, :], dtype=torch.float64, device=self.target.device)

    def pulse_loss(self, phase: torch.Tensor) -> torch.Tensor:
        """성문 펄스 위치를 목표의 폐쇄 시각에 건다.

        크기 스펙트럼만 맞추면 위상은 자유롭게 흐른다. 이 엔진은 위상이 모형에서 나오므로
        (시변 IIR 이 샘플마다 이어진다) 소스 펄스만 제자리에 놓으면 나머지 위상은 물리가
        정한다. 그래서 **파형 위상이 아니라 펄스 시각**을 건다 — 손실면이 매끈하다.

        `1 − cos(φ(t_k) − φ0)` 를 쓴다. φ0 는 학습되는 상수 하나다 (LF 파형의 폐쇄
        시점과 Praat 의 폐쇄 시각 정의가 몇 도 다르므로, 그 차이는 상수로 흡수시킨다).
        """
        idx = self.pulse_idx
        if idx.numel() == 0:
            return torch.zeros((), device=phase.device)
        ph = phase[0].index_select(0, idx).double()
        return (1.0 - torch.cos(ph - self.pulse_phi0)).mean()

    def _cabs(self, z: torch.Tensor) -> torch.Tensor:
        """완화된 복소 크기 `√(x²+y²+η²)` — `z.abs()` 대신 쓴다.

        `z.abs()` 는 z=0 에서 기울기가 0/0 이라 NaN 이다 (`MAG_ETA_REL` 주석 참조).
        η 를 더하면 어디서나 미분 가능하고 기울기 상한이 1 이다.
        """
        return torch.sqrt(z.real ** 2 + z.imag ** 2 + self.mag_eta ** 2)

    def _db(self, x: torch.Tensor) -> torch.Tensor:
        """dB. **로그의 인자를 η 로 막는다** — `log 0 = −∞` 도, 그 기울기 발산도 없다.

        `_cabs` 를 지난 크기는 이미 η 이상이므로 여기 더하는 η 는 이중 안전장치이자
        멜 합성 뒤(선형 결합이라 0 이 될 수 있다)의 바닥이다.
        """
        return 20.0 * torch.log10(x + self.mag_eta)

    def _sc(self, at: torch.Tensor, ap: torch.Tensor) -> torch.Tensor:
        """스펙트럼 수렴도. **놈도 완화한다** — `‖v‖` 는 v=0 에서 같은 0/0 이다.

        적합이 완벽해질수록 `at − ap → 0` 이므로 그 자리로 **다가가는 것이 목적**이다.
        완화하지 않으면 잘 맞을수록 기울기가 불안정해진다.
        """
        m = min(at.shape[-1], ap.shape[-1])
        at, ap = at[..., :m], ap[..., :m]
        e2 = self.mag_eta ** 2
        d = at - ap
        num = torch.sqrt((d * d).sum() + e2)
        den = torch.sqrt((at * at).sum() + e2)
        return num / (den + 1e-9)

    def _ripple_smooth(self, db: torch.Tensor, k: int) -> torch.Tensor:
        """주파수 축의 이동평균 (빈 k 개). 끝은 복제로 채운다."""
        k = max(1, k | 1)
        if k == 1:
            return db
        x = db.reshape(-1, 1, db.shape[-2], db.shape[-1]).transpose(-1, -2)
        x = x.reshape(-1, 1, db.shape[-2])
        x = torch.nn.functional.pad(x, (k // 2, k // 2), mode="replicate")
        ker = torch.ones(1, 1, k, dtype=db.dtype, device=db.device) / k
        y = torch.nn.functional.conv1d(x, ker)
        return y.reshape(-1, db.shape[-1], db.shape[-2]).transpose(-1, -2).reshape(db.shape)

    def _ripple_rms(self, mag: torch.Tensor) -> torch.Tensor:
        """프레임마다 4~16 kHz 물결의 rms [dB]. `HFRIP_W` 참조."""
        db = 20.0 * torch.log10(mag.clamp_min(1e-10))
        lo, hi = self._rip_lo, self._rip_hi
        db = db[..., lo:hi, :]
        r = self._ripple_smooth(db, self._rip_kn) - self._ripple_smooth(db, self._rip_kw)
        return torch.sqrt((r * r).mean(dim=-2) + 1e-12)

    def _ripple_setup(self, tgt_raw) -> None:
        df = self.fs / HFRIP_FFT
        self._rip_lo = int(round(HFRIP_BAND[0] / df))
        self._rip_hi = int(round(HFRIP_BAND[1] / df))
        self._rip_kn = max(3, int(round(HFRIP_NARROW_HZ / df)) | 1)
        self._rip_kw = max(5, int(round(HFRIP_WIDE_HZ / df)) | 1)
        if tgt_raw is None:
            raw = self._cabs(_stft(self.target, HFRIP_FFT, self.wins[HFRIP_FFT]))
            tgt_raw = raw[:, :self.bin_max[HFRIP_FFT]]
        self._rip_hi = min(self._rip_hi, int(tgt_raw.shape[-2]))
        self._rip_tgt = self._ripple_rms(tgt_raw).detach()

    def hfrip_loss(self, y: torch.Tensor) -> torch.Tensor:
        """고역 물결이 목표보다 **얕은 만큼**을 문다. 넘치는 쪽은 `HFRIP_OVER` 배로.

        `HFRIP_FFT` 가 그 단계의 창 목록에 있으면 `spectral_loss` 가 이미 뜬 스펙트럼을 쓴다.
        없으면 여기서 따로 뜨는데 — 그게 2.1·2.2 단계를 크게 늦춘다.
        """
        raw = getattr(self, "_hfrip_raw", None)
        if raw is None:
            raw = self._cabs(_stft(y, HFRIP_FFT, self.wins[HFRIP_FFT]))[:, :self.bin_max[HFRIP_FFT]]
        rp = self._ripple_rms(raw)
        n = min(rp.shape[-1], self._rip_tgt.shape[-1])
        d = self._rip_tgt[..., :n] - rp[..., :n]
        short = self._soft_floor(d, 0.0, 0.2)                 # 모자란 쪽 (부드러운 relu)
        over = self._soft_floor(-d, 0.0, 0.2)
        return (short + HFRIP_OVER * over).mean() / 5.0      # 5 dB = 목표의 전형적 물결

    def spectral_loss(self, y: torch.Tensor):
        """(정밀 SC, 포락 dB 오차, 포락 SC, 창별 SC%).

        **선형 빈의 로그 크기 평균을 쓰면 안 된다.** 24 kHz 까지 선형이면 빈의 90 % 가
        2.4 kHz 위에 있고, 그 대역은 목표가 거의 무음이다. 그러면 손실이 무음 구간
        일치로 지배되어 전체 이득을 낮추는 쪽이 이긴다 (실측: 그렇게 해서 0~500 Hz 가
        27 dB 낮은 해의 손실이 더 낮게 나왔다). 그래서 포락 오차는 **멜 대역의 dB**
        로 재고 (로그 주파수 = 대역마다 같은 무게), 정점 −70 dB 아래는 잘라 낸다.
        """
        sc_sum, per = 0.0, {}
        Smel = Smel_raw = harm_raw = None
        self._hfrip_raw = None
        for k in self.sizes:
            raw = self._cabs(_stft(y, k, self.wins[k]))[:, :self.bin_max[k]]
            Sp = self._expect(raw, k)
            if k == MEL_FFT:
                Smel, Smel_raw = Sp, raw
            if k == HARM_FFT:
                harm_raw = self._db(raw)
            if k == HFRIP_FFT:
                self._hfrip_raw = raw          # 물결 항이 재활용한다 (STFT 를 또 뜨지 않게)
            sc = self._sc(self.tgt_S[k], Sp)
            sc_sum = sc_sum + sc
            per[k] = float(100.0 * (1.0 - sc.detach()))
        if Smel is None:
            Smel_raw = self._cabs(
                _stft(y, MEL_FFT, self.wins[MEL_FFT]))[:, :self.bin_max[MEL_FFT]]
            Smel = self._expect(Smel_raw, MEL_FFT)
        Mp = self.mel[:, :Smel.shape[1]] @ Smel
        m = min(Mp.shape[-1], self.tgt_M.shape[-1])
        Mp, Mt = Mp[..., :m], self.tgt_M[..., :m]
        # **clamp 도 abs 도 꺾임이다.** 멜 빈 수천 개마다 꺾이면 손실이 C² 가 아니게
        # 되고 헤시안 기반 진단이 통째로 막힌다 (docs/MEASUREMENTS.md §8.7). 폭은
        # dB 단위 0.5 — 계측 잡음보다 작아 거동은 사실상 그대로다.
        fl = getattr(self, "db_floor_band", None)
        fl = self.db_floor if fl is None else fl
        a = self._soft_floor(self._db(Mt), fl, 0.5)
        b = self._soft_floor(self._db(Mp), fl, 0.5)
        e_ab = self._soft_abs(a - b, 0.5)
        env_db = self._weighted_db(e_ab) / 20.0
        self._env_ab = (a.detach(), b)          # 전이 항이 쓴다 (목표 쪽은 상수)
        env_sc = self._sc(Mt, Mp)
        if FLUX_W > 0.0:
            # **날것 멜**로 잰다 (`_expect` 를 지나면 재려는 흔들림이 이미 없다).
            self._flux_db = self._db(self.mel[:, :Smel_raw.shape[1]] @ Smel_raw)
        # 사용자 요청 세 항이 쓸 재료. 첨예도·F0 아래는 **평활 전** 스펙트럼이라야 한다.
        self._mel_err = (self._soft_abs(a - b, 0.5).mean(-2)
                         if CONT_W > 0.0 else None)
        self._mel_raw_db = self._db(Smel_raw) if SHARP_W > 0.0 else None
        self._harm_db = (harm_raw if (SHARP_W > 0.0 or SUBF0_W > 0.0) else None)
        return sc_sum / len(self.sizes), env_db, env_sc, per

    def _flux_stat(self, mdb: torch.Tensor, make_mask: bool = False):
        """**프레임별** 대역평균 변화율 `mean_k |Δ dB(t,k)|` — 세로 얼룩 그 자체.

        **대역 평균이 아니라 프레임별로 낸다.** 대역별 시간평균으로 재면 프레임이
        많은 계급이 통계를 지배한다 — 실측(out/room2/s040): 얼룩이 몰려 있는 마찰
        프레임은 살아있는 238 개 중 **16 개(7 %)** 뿐이다. 계급별 얼룩 비는

            전체 1.150,  **마찰 1.856**,  유성 1.128        (s040)
            전체 1.198,    마찰 1.232,    유성 1.024        (s101)

        이므로 고쳐야 할 곳은 마찰인데, 대역평균 항은 거기에 7 % 의 무게만 준다.
        프레임별로 맞추면 모든 프레임이 같은 무게를 갖는다. 정의도 합격 판정
        (`scripts/acceptance.py:flux`)과 정확히 같아진다.

        `make_mask=True` 면 "소리 나는 프레임" 마스크도 같이 만든다. 무음의 −∞ 근처
        dB 는 잘게 흔들려 통계를 통째로 오염시킨다 (§30 에서 합격 판정이 같은 이유로
        틀렸다). 목표 정점 대비 −45 dB 를 문턱으로 쓴다.
        """
        L = max(1, int(FLUX_LAG))
        if mdb.shape[-1] <= L:
            z = torch.zeros(1, dtype=mdb.dtype, device=mdb.device)
            return (z, None) if make_mask else z
        f = self._soft_abs(mdb[..., L:] - mdb[..., :-L], 0.5).mean(-2)   # (…, T−L)
        if make_mask:
            lvl = mdb.mean(-2)
            live = lvl > float(lvl.max()) - 45.0
            return f, (live[..., L:] & live[..., :-L])
        return f

    def flux_loss(self, raw_mel_db: torch.Tensor) -> torch.Tensor:
        """플럭스를 **일치**시킨다 — 크면 세로 줄, 작으면 기계적이다.

        최소화가 아니라 일치인 이유: 짧은 창의 스펙트럼이 안 흔들리는 잡음은 잡음이
        아니라 음(音)이다. 목표의 마찰음도 flux p95 가 4.85 dB 로 흔들린다.
        """
        f = self._flux_stat(raw_mel_db)
        m = min(f.shape[-1], self.tgt_flux.shape[-1])
        d = self._soft_abs(f[..., :m] - self.tgt_flux[..., :m], 0.1)
        live = self.flux_live
        if live is not None and live.shape[-1] >= m:
            w = live[..., :m].to(d.dtype)
            return (d * w).sum() / w.sum().clamp_min(1.0) / 20.0
        return d.mean() / 20.0

    # ------------------------------------------- 파형 상관 · 주기성 (사용자 요청)
    def _frames(self, x: torch.Tensor, win: int, hop: int) -> torch.Tensor:
        """(1, n) -> (T, win). 마지막 자투리는 버린다."""
        return x[0].unfold(0, win, hop)

    def _prepare_corr(self) -> None:
        """상관·주기성 항이 쓸 창 격자와 유성 마스크를 만든다."""
        self._corr_win = max(64, int(CORR_WIN_MS * self.fs / 1000.0))
        self._corr_hop = max(16, int(CORR_HOP_MS * self.fs / 1000.0))
        tf = self._frames(self.target, self._corr_win, self._corr_hop)
        self._corr_tgt = tf
        self._corr_te = (tf * tf).sum(-1)
        v = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)))
        T = tf.shape[0]
        if v.size:
            c = (np.arange(T) * self._corr_hop + self._corr_win // 2) / self.hop
            vm = v[np.clip(c.astype(int), 0, v.size - 1)]
        else:
            vm = np.ones(T, dtype=bool)
        lvl = self._corr_te.detach()
        live = lvl > float(lvl.max()) * 1e-4          # 정점 대비 −40 dB
        self._corr_live = torch.as_tensor(vm, device=live.device) & live
        # --- 주기성 ---
        f0 = np.asarray(self.track["f0_target"], dtype=np.float64)
        c = (np.arange(T) * self._corr_hop + self._corr_win // 2) / self.hop
        f0f = f0[np.clip(c.astype(int), 0, max(f0.size - 1, 0))] if f0.size else np.zeros(T)
        lag = np.where(f0f > 50.0, np.round(self.fs / np.maximum(f0f, 50.0)), 0).astype(int)
        self._per_lag = lag
        self._per_ok = torch.as_tensor((lag > 0) & (lag < self._corr_win // 2),
                                       device=live.device) & self._corr_live
        # 고역통과 커널 (창 씌운 sinc). 고정 상수라 미분에 영향이 없다.
        n = 129
        t = np.arange(n) - n // 2
        fc = HNR_HP_HZ / self.fs
        lp = np.sinc(2 * fc * t) * np.hanning(n)
        lp = lp / lp.sum()
        hp = -lp
        hp[n // 2] += 1.0
        self._hp = torch.as_tensor(hp, dtype=self.target.dtype,
                                   device=self.target.device).view(1, 1, n)
        with torch.no_grad():
            self.tgt_per = self._periodicity(self.target)

    def _periodicity(self, y: torch.Tensor) -> torch.Tensor:
        """(2, T) — 전대역과 고역의 F0 지연 정규화 자기상관."""
        hp = torch.nn.functional.conv1d(y.unsqueeze(1), self._hp,
                                        padding=self._hp.shape[-1] // 2)[:, 0]
        out = []
        for sig in (y, hp):
            w, h = self._corr_win, self._corr_hop
            n = sig.shape[-1]
            T = self._per_ok.shape[0]
            idx = torch.arange(T, device=sig.device) * h
            lag = torch.as_tensor(self._per_lag, device=sig.device)
            ar = torch.arange(w, device=sig.device)
            a = sig[0][(idx[:, None] + ar[None, :]).clamp(max=n - 1)]
            b = sig[0][(idx[:, None] + lag[:, None] + ar[None, :]).clamp(max=n - 1)]
            num = (a * b).sum(-1)
            den = torch.sqrt((a * a).sum(-1) * (b * b).sum(-1) + 1e-20)
            out.append(num / den)
        return torch.stack(out)

    def corr_loss(self, y: torch.Tensor) -> torch.Tensor:
        """유성 구간에서 파형 상관이 무릎 아래로 떨어진 만큼. 큰 붕괴가 지배한다."""
        pf = self._frames(y, self._corr_win, self._corr_hop)
        m = min(pf.shape[0], self._corr_tgt.shape[0])
        a, b = self._corr_tgt[:m], pf[:m]
        num = (a * b).sum(-1)
        den = torch.sqrt(self._corr_te[:m] * (b * b).sum(-1) + 1e-20)
        r = num / den
        w = self._corr_live[:m].to(r.dtype)
        over = self._soft_over(CORR_KNEE - r, 0.05)
        if CORR_POW != 1.0:
            over = over ** CORR_POW
        return (over * w).sum() / w.sum().clamp_min(1.0)

    def _move_setup(self) -> None:
        """움직임 예산이 쓸 유성 가중과 **관측 속도 기준**(분석 궤적의 ms 당 로그 변화 rms)."""
        v = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)), bool)
        T = int(self.base.shape[0])
        if v.size < T:
            v = np.pad(v, (0, T - v.size))
        self._move_voi = torch.as_tensor(v[:T].astype(np.float64), device=self.base.device)
        sig = MOVE_SMOOTH_MS / float(self.track.frame_ms)
        half = max(1, int(math.ceil(3.0 * sig)))
        t = torch.arange(-half, half + 1, dtype=torch.float64, device=self.base.device)
        k = torch.exp(-0.5 * (t / sig) ** 2)
        self._move_ker = (k / k.sum()).view(1, 1, -1)
        self._move_rate_ref = {}
        for nm in MOVE_RATE_PARAMS:
            if nm not in INDEX:
                continue
            x = np.asarray(self.track[nm], dtype=np.float64)[:T]
            if x.size < 8 or not np.all(x > 0):
                continue
            d = np.diff(np.log(np.maximum(x, 1e-6)))
            mv = v[:d.size]
            if mv.sum() < 32:
                continue
            self._move_rate_ref[nm] = float(np.sqrt((d[mv] ** 2).mean()))

    def _move_fast(self, x: torch.Tensor) -> torch.Tensor:
        """추세(σ `MOVE_SMOOTH_MS` 가우시안)를 뺀 빠른 성분."""
        half = (self._move_ker.shape[-1] - 1) // 2
        xx = torch.nn.functional.pad(x.view(1, 1, -1), (half, half), mode="replicate")
        return x - torch.nn.functional.conv1d(xx, self._move_ker)[0, 0]

    def move_loss(self, ctrl: torch.Tensor) -> torch.Tensor:
        """움직임 예산 — 관측(속도)·생리(빠른 진폭) 상한을 넘은 만큼. `MOVE_W` 참조."""
        if getattr(self, "_move_voi", None) is None:
            self._move_setup()
        w = self._move_voi
        wsum = w.sum().clamp_min(1.0)
        out = torch.zeros((), dtype=torch.float64, device=ctrl.device)
        c = ctrl[0].double()
        for nm, cap in MOVE_CAP.items():
            if nm not in self.names or nm not in INDEX:
                continue
            f = self._move_fast(c[:, INDEX[nm]])
            s = torch.sqrt((f * f * w).sum() / wsum + 1e-12)
            out = out + (self._soft_over(s - cap, 0.15 * cap) / cap) ** 2
        for nm, ref in self._move_rate_ref.items():
            if nm not in self.names:
                continue
            d = torch.diff(torch.log(c[:, INDEX[nm]].clamp_min(1e-6)))
            wd = w[:d.shape[0]]
            r = torch.sqrt((d * d * wd).sum() / wd.sum().clamp_min(1.0) + 1e-20)
            lim = MOVE_RATE_REL * ref
            out = out + (self._soft_over(r - lim, 0.15 * lim) / lim) ** 2
        return out

    def _re_setup(self) -> None:
        """목표의 난류 증거와 그 **미래/과거 창**을 만든다 (`RE_LEAD_W` 참조)."""
        from .segment import FRICATIVE_HL_DB
        t = self.target[0].detach().cpu().numpy()
        h = self.hop
        T = int(len(t) // h)
        lo = np.empty(T)
        hi = np.empty(T)
        w = int(0.025 * self.fs) // 2 * 2
        fr = np.fft.rfftfreq(w, 1.0 / self.fs)
        m_lo = (fr >= 100) & (fr < 1000)
        m_hi = (fr >= 3000) & (fr < 16000)
        win = np.hanning(w)
        for i in range(T):
            c = max(i * h + h // 2 - w // 2, 0)
            seg = t[c:c + w]
            if len(seg) < w:
                seg = np.pad(seg, (0, w - len(seg)))
            p = np.abs(np.fft.rfft(seg * win)) ** 2 + 1e-20
            lo[i], hi[i] = p[m_lo].sum(), p[m_hi].sum()
        r = 10.0 * np.log10(hi / lo)
        e = 1.0 / (1.0 + np.exp(-(r - FRICATIVE_HL_DB) / 3.0))     # 난류 증거 0~1
        L = max(1, int(RE_LEAD_MS / self.track.frame_ms))
        fut = np.array([e[i:i + L + 1].max() for i in range(T)])    # 앞으로 L 안의 최대
        both = np.array([e[max(i - L, 0):i + L + 1].max() for i in range(T)])
        dev = self.target.device
        self._re_on = torch.as_tensor(fut, dtype=torch.float64, device=dev)
        self._re_off = torch.as_tensor(1.0 - both, dtype=torch.float64, device=dev)

    def re_lead_loss(self, re: torch.Tensor) -> torch.Tensor:
        """난류가 올 곳에서는 Re 를 문턱 위로, 없는 곳에서는 아래로. `RE_LEAD_W` 참조."""
        from .noise import RE_CRIT
        if getattr(self, "_re_on", None) is None:
            self._re_setup()
        h = self.hop
        T = min(int(re.shape[-1] // h), int(self._re_on.shape[0]))
        if T < 4:
            return torch.zeros((), dtype=torch.float64, device=re.device)
        rf = re[0, :T * h].double().reshape(T, h).mean(1) / RE_CRIT   # 문턱 기준 비
        on, off = self._re_on[:T], self._re_off[:T]
        up = self._soft_over((1.0 + RE_MARGIN) - rf, 0.05)
        dn = self._soft_over(rf - (1.0 - RE_MARGIN), 0.05)
        return ((on * up * up).sum() + (off * dn * dn).sum()) / float(T)

    def _band_weight(self, n_mel: int, dtype, device) -> torch.Tensor:
        """멜 빈마다의 **대역 균형 가중** (1, F, 1). `BAND_BALANCE` 참조."""
        key = (n_mel, BAND_GROUPS, BAND_BALANCE, str(device))
        if getattr(self, "_bw_key", None) == key:
            return self._bw
        cf = self.mel.detach().cpu().numpy().argmax(1) * (self.fs / MEL_FFT)
        cf = cf[:n_mel]
        edges = np.geomspace(max(cf[cf > 0].min(), 50.0), max(cf.max(), 100.0), BAND_GROUPS + 1)
        w = np.ones(n_mel)
        for i in range(BAND_GROUPS):
            m = (cf >= edges[i]) & (cf < edges[i + 1] if i < BAND_GROUPS - 1 else cf <= edges[i + 1])
            if m.sum() > 0:
                w[m] = 1.0 / m.sum()
        w = w / w.mean()
        w = (1.0 - BAND_BALANCE) + BAND_BALANCE * w
        self._bw_key = key
        self._bw = torch.as_tensor(w[None, :, None], dtype=dtype, device=device)
        return self._bw

    def _weighted_db(self, e: torch.Tensor) -> torch.Tensor:
        """구간·대역 균형을 적용한 dB 오차의 평균. 둘 다 0 이면 단순 평균이다."""
        if getattr(self, "_hf_focus", False):
            # 고역 단계: `HF_STAGE_FMIN` 위의 멜 띠만 본다.
            cf = self.mel.detach().cpu().numpy().argmax(1) * (self.fs / MEL_FFT)
            k = np.flatnonzero(cf[:e.shape[-2]] >= HF_STAGE_FMIN)
            if k.size:
                return e.index_select(-2, torch.as_tensor(k, dtype=torch.long,
                                                          device=e.device)).mean()
        if BAND_BALANCE <= 0.0 and SEG_BALANCE <= 0.0:
            return e.mean()
        w = torch.ones_like(e[:1, :1, :1])
        if BAND_BALANCE > 0.0:
            w = w * self._band_weight(e.shape[-2], e.dtype, e.device)
        if SEG_BALANCE > 0.0:
            w = w * self._seg_weight(e.shape[-1])
        return (e * w).sum() / (w.expand_as(e).sum()).clamp_min(1e-9)

    def _seg_weight(self, T: int) -> torch.Tensor:
        """멜 프레임마다의 **구간 균형 가중** (1, 1, T). `SEG_BALANCE` 참조."""
        if getattr(self, "_segw", None) is not None and self._segw.shape[-1] >= T:
            return self._segw[..., :T]
        from .segment import fricative_mask
        hop = MEL_FFT // 4
        t = self.target[0].detach().cpu().numpy()
        idx = np.clip((np.arange(T) * hop / self.fs * 1000.0).astype(int), 0, None)
        voi = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)), bool)
        fri = np.asarray(getattr(self.track, "fricative", np.zeros(0, dtype=bool)), bool)
        vel = np.asarray(self.track["velum"], dtype=np.float64)
        m = max(voi.size, 1)
        j = np.clip(idx, 0, m - 1)
        lv = np.array([20 * np.log10(np.std(t[i * hop:(i + 1) * hop]) + 1e-20) for i in range(T)])
        quiet = lv < lv.max() - 35
        vv = voi[j] if voi.size else np.ones(T, bool)
        ff = fri[j] if fri.size else np.zeros(T, bool)
        nn = (vel[j] > 0.5) if vel.size else np.zeros(T, bool)
        trans = np.zeros(T, bool)
        ch = np.diff(np.concatenate([[vv[0]], vv]).astype(int)) != 0
        for k in np.flatnonzero(ch):
            trans[max(k - 15, 0):k + 15] = True
        cls = np.where(quiet, 0, np.where(ff, 1, np.where(nn, 2, np.where(trans, 3, np.where(vv, 4, 5)))))
        w = np.ones(T)
        for c in range(6):
            s = cls == c
            if s.sum() > 0:
                w[s] = 1.0 / s.sum()
        w = w / w.mean()
        # SEG_BALANCE 로 길이비례(1)와 구간균형(w) 사이를 섞는다.
        w = (1.0 - SEG_BALANCE) + SEG_BALANCE * w
        self._segw = torch.as_tensor(w[None, None, :], dtype=self.target.dtype,
                                     device=self.target.device)
        return self._segw

    @staticmethod
    def _bark(f):
        return 13.0 * torch.atan(0.00076 * f) + 3.5 * torch.atan((f / 7500.0) ** 2)

    def formant_loss(self) -> torch.Tensor:
        """대역별 스펙트럼 무게중심(Bark)을 목표와 맞춘다. `FORMANT_W` 참조."""
        ab = getattr(self, "_env_ab", None)
        if ab is None:
            return torch.zeros((), device=self.target.device)
        a, b = ab                                    # (1, F, T) dB, 목표는 상수
        if getattr(self, "_fmt_masks", None) is None:
            cf = self.mel.detach().cpu().numpy().argmax(1) * (self.fs / MEL_FFT)
            cf = cf[:a.shape[-2]]
            self._fmt_cf = torch.as_tensor(cf, dtype=a.dtype, device=a.device).view(1, -1, 1)
            self._fmt_bk = self._bark(self._fmt_cf)
            self._fmt_masks = [torch.as_tensor(((cf >= lo) & (cf < hi)).astype(np.float64),
                                               dtype=a.dtype, device=a.device).view(1, -1, 1)
                               for lo, hi in FORMANT_BANDS]
            v = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)), bool)
            hop = MEL_FFT // 4
            idx = np.clip((np.arange(a.shape[-1]) * hop / self.fs * 1000).astype(int), 0,
                          max(v.size - 1, 0))
            self._fmt_voi = torch.as_tensor((v[idx] if v.size else np.ones(a.shape[-1])).astype(np.float64),
                                            dtype=a.dtype, device=a.device).view(1, 1, -1)
        out = torch.zeros((), dtype=a.dtype, device=a.device)
        vw = self._fmt_voi
        for m in self._fmt_masks:
            # dB 를 선형으로 되돌려 무게로 쓴다 (대역 안에서만 정규화하므로 전체 수준과 무관하다).
            pa = torch.pow(10.0, a / 20.0) * m
            pb = torch.pow(10.0, b / 20.0) * m
            ca = (pa * self._fmt_bk).sum(-2) / pa.sum(-2).clamp_min(1e-12)
            cb = (pb * self._fmt_bk).sum(-2) / pb.sum(-2).clamp_min(1e-12)
            d = self._soft_abs(ca - cb, 0.05) * vw[:, 0]
            out = out + d.sum() / vw.sum().clamp_min(1.0)
        return out / len(self._fmt_masks)

    def prom_loss(self) -> torch.Tensor:
        """대역마다 **봉우리 − 골** 깊이를 목표와 맞춘다. `PROM_W` 참조."""
        ab = getattr(self, "_env_ab", None)
        if ab is None:
            return torch.zeros((), device=self.target.device)
        a, b = ab                                   # (1, n_mel, T) dB
        if getattr(self, "_prom_bins", None) is None:
            # 멜 띠의 중심 주파수 — `formant_loss`·`_band_weight` 와 같은 방식.
            fc = self.mel.detach().cpu().numpy().argmax(1) * (self.fs / MEL_FFT)
            fc = np.asarray(fc, float)[:a.shape[-2]]
            self._prom_bins = [np.flatnonzero((fc >= lo) & (fc < hi)) for lo, hi in PROM_BANDS]
            self._prom_voi = self._voiced_frames(a.shape[-1], a.dtype, a.device)
        w = self._prom_voi
        out = torch.zeros((), dtype=a.dtype, device=a.device)
        nb = 0
        for ix in self._prom_bins:
            if ix.size < 6:
                continue
            i = torch.as_tensor(ix, dtype=torch.long, device=a.device)
            beta = PROM_BETA
            def depth(x):
                v = x.index_select(-2, i)
                hi = torch.logsumexp(beta * v, dim=-2) / beta
                lo = -torch.logsumexp(-beta * v, dim=-2) / beta
                return hi - lo                       # (1, T)
            e = (depth(b) - depth(a)) / 20.0
            out = out + (self._pseudo_huber(e) * w).sum() / w.sum().clamp_min(1e-9)
            nb += 1
        return out / max(nb, 1)

    def _voiced_frames(self, T: int, dtype, device) -> torch.Tensor:
        """멜 프레임마다의 유성 가중 (1, T)."""
        hop = MEL_FFT // 4
        voi = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)), bool)
        if voi.size == 0:
            return torch.ones((1, T), dtype=dtype, device=device)
        idx = np.clip((np.arange(T) * hop / self.fs * 1000.0).astype(int), 0, voi.size - 1)
        return torch.as_tensor(voi[idx].astype(np.float64), dtype=dtype, device=device).view(1, T)

    def delta_loss(self) -> torch.Tensor:
        """로그 멜의 **시간 미분**(Furui 회귀 델타)을 맞춘다. `DELTA_W` 참조."""
        ab = getattr(self, "_env_ab", None)
        if ab is None:
            return torch.zeros((), device=self.target.device)
        a, b = ab
        K = int(DELTA_K)
        if a.shape[-1] < 2 * K + 3:
            return torch.zeros((), device=a.device)
        k = torch.arange(-K, K + 1, dtype=a.dtype, device=a.device)
        ker = (k / (k * k).sum()).view(1, 1, -1)
        def d(x):
            xx = torch.nn.functional.pad(x.reshape(-1, 1, x.shape[-1]), (K, K), mode="replicate")
            return torch.nn.functional.conv1d(xx, ker).reshape(x.shape)
        da, db = d(a), d(b)
        e = self._soft_abs(da - db, 0.05)
        # **전이가 큰 칸에 무게를 준다** (Furui 1986: 최대 스펙트럼 전이가 음절 식별의 정보를 담는다).
        # 균등 평균이면 전이 칸이 정상 구간에 묻혀 20 ms 뭉갬에 손실이 **오히려 줄었다**(실측 −4.2 %).
        wt = da.abs()
        wt = wt / wt.mean().clamp_min(1e-9) + DELTA_FLOOR
        if BAND_BALANCE > 0.0:
            wt = wt * self._band_weight(e.shape[-2], e.dtype, e.device)
        if SEG_BALANCE > 0.0:
            wt = wt * self._seg_weight(e.shape[-1])
        return (e * wt).sum() / wt.sum().clamp_min(1e-9) / 20.0

    def _attack_setup(self, dtype, device) -> None:
        """어택 항이 쓸 대역통과 핵과 목표 쪽 상승량·가중. `ATTACK_W` 참조."""
        from scipy.signal import firwin
        kers = []
        for lo, hi in ATTACK_BANDS:
            taps = firwin(257, [lo, min(hi, self.fs / 2 * 0.99)], fs=self.fs, pass_zero=False)
            kers.append(torch.as_tensor(taps, dtype=dtype, device=device).view(1, 1, -1))
        self._att_kers = kers
        self._att_ker = kers[0]
        ms = 1000.0 * self.hop / self.fs
        self._att_k = max(1, int(round(ATTACK_RISE_MS / ms)))
        sig = max(ATTACK_SMOOTH_MS / ms, 0.51)
        half = max(1, int(math.ceil(2.0 * sig)))
        t = torch.arange(-half, half + 1, dtype=dtype, device=device)
        g = torch.exp(-0.5 * (t / sig) ** 2)
        self._att_sm = (g / g.sum()).view(1, 1, -1)
        self._att_rt, self._att_w = [], []
        for k in kers:
            self._att_ker = k
            rt = self._attack_rise(self.target.to(dtype)).detach()
            self._att_rt.append(rt)
            self._att_w.append(torch.sigmoid((rt - ATTACK_MIN_DB) / 2.0) + ATTACK_FLOOR)

    def _attack_rise(self, y: torch.Tensor) -> torch.Tensor:
        """2~8 kHz 수준이 `ATTACK_RISE_MS` 뒤에 얼마나 **서는가** [dB] (오르는 쪽만)."""
        half = (self._att_ker.shape[-1] - 1) // 2
        x = torch.nn.functional.pad(y.view(1, 1, -1), (half, half), mode="constant")
        b = torch.nn.functional.conv1d(x, self._att_ker.to(y.dtype))[0, 0]
        n = b.shape[-1] // self.hop
        p = (b[:n * self.hop].reshape(n, self.hop) ** 2).mean(-1)
        hs = (self._att_sm.shape[-1] - 1) // 2
        pp = torch.nn.functional.pad(p.view(1, 1, -1), (hs, hs), mode="replicate")
        p = torch.nn.functional.conv1d(pp, self._att_sm.to(p.dtype))[0, 0]
        L = 10.0 * torch.log10(p + 1e-16)
        k = self._att_k
        return torch.nn.functional.softplus(L[k:] - L[:-k], beta=0.5)

    def attack_loss(self, y: torch.Tensor) -> torch.Tensor:
        """목표가 급히 서는 자리에서 **서는 크기**를 맞춘다. `ATTACK_W` 참조."""
        if getattr(self, "_att_kers", None) is None:
            self._attack_setup(y.dtype, y.device)
        out = torch.zeros((), dtype=y.dtype, device=y.device)
        for k, rt, w in zip(self._att_kers, self._att_rt, self._att_w):
            self._att_ker = k
            ry = self._attack_rise(y)
            m = min(ry.shape[-1], rt.shape[-1])
            e = (ry[:m] - rt[:m].to(ry.dtype)) / 20.0
            ww = w[:m].to(ry.dtype)
            out = out + (self._pseudo_huber(e) * ww).sum() / ww.sum().clamp_min(1e-9)
        return out / max(len(self._att_kers), 1)

    def event_loss(self) -> torch.Tensor:
        """이벤트가 관측된 도약보다 커지거나 여기저기 쓰이면 벌한다. `EVENT_PEN_W` 참조."""
        if getattr(self, "w_ev", None) is None:
            return torch.zeros((), device=self.target.device)
        a = self.w_ev * EVENT_W
        cap = (EVENT_CAP_REL * self._ev_jump).unsqueeze(1).to(a.dtype).clamp_min(1e-3)
        over = self._soft_over(a.abs() - cap, 0.05 * cap)
        pen = (over / cap).pow(2).mean() + EVENT_L1 * a.abs().mean()
        # (나) **이벤트 자리에서 부드러운 층이 움직이면 벌한다** — 도약은 계단이 져야 한다.
        ws = getattr(self, "_w_smooth", None)
        if ws is not None:
            h = max(1, int(round(EVENT_RAMP_MS / float(self.track.frame_ms))))
            T = ws.shape[0]
            lo = torch.clamp(self._ev_idx - h, 0, T - 1)
            hi = torch.clamp(self._ev_idx + h, 0, T - 1)
            d = (ws.index_select(0, hi) - ws.index_select(0, lo)).index_select(1, self._ev_cols)
            cap2 = (EVENT_RAMP_FREE * self._ev_jump).unsqueeze(1).to(d.dtype).clamp_min(1e-3)
            ramp = self._soft_over(d.abs() - cap2, 0.05 * cap2)
            pen = pen + EVENT_RAMP_W * (ramp / cap2).pow(2).mean()
        return pen

    def quiet_loss(self, y: torch.Tensor) -> torch.Tensor:
        """목표가 조용한 10 ms 창에서 합성이 목표보다 `QUIET_MARGIN_DB` 넘게 큰 만큼(dB)의 제곱 평균."""
        w = int(0.010 * self.fs)
        if getattr(self, "_quiet_mask", None) is None:
            tf = self._frames(self.target, w, w)
            tl = 10.0 * torch.log10((tf * tf).mean(-1) + 1e-20)
            self._quiet_tl = tl.detach()
            self._quiet_mask = (tl < tl.max() - QUIET_DB).to(tl.dtype)
        pf = self._frames(y, w, w)
        m = min(pf.shape[0], self._quiet_mask.shape[0])
        if float(self._quiet_mask[:m].sum()) == 0.0:
            return torch.zeros((), dtype=y.dtype, device=y.device)
        pl = 10.0 * torch.log10((pf[:m] * pf[:m]).mean(-1) + 1e-20)
        over = self._soft_over(pl - self._quiet_tl[:m] - QUIET_MARGIN_DB, 1.0)
        mk = self._quiet_mask[:m]
        return (over * over * mk).sum() / mk.sum() / 100.0

    def unvoiced_loss(self, amp: torch.Tensor) -> torch.Tensor:
        """목표가 무성 마찰인 샘플에서 합성 성문 떨림 진폭²의 평균 (`UNVOICED_W` 참조)."""
        m = getattr(self, "_uv_mask", None)
        if m is None:
            from scipy.ndimage import binary_dilation
            voi = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)), bool)
            fri = np.asarray(getattr(self.track, "fricative", np.zeros(0, dtype=bool)), bool)
            k = min(voi.size, fri.size)
            uv = (fri[:k] & ~binary_dilation(voi[:k], iterations=int(UNVOICED_EDGE))) if k else np.zeros(0, bool)
            m = torch.as_tensor(np.repeat(uv.astype(np.float64), self.hop), device=amp.device)
            self._uv_mask = m
        n = min(int(m.shape[-1]), int(amp.shape[-1]))
        if n == 0 or float(m[:n].sum()) == 0.0:
            return torch.zeros((), dtype=amp.dtype, device=amp.device)
        a = amp[0, :n].to(m.dtype)
        return (a * a * m[:n]).sum() / m[:n].sum()

    def hnr_loss(self, y: torch.Tensor) -> torch.Tensor:
        """주기성을 목표에 **일치**시킨다. 시끄러운 쪽을 더 세게 문다."""
        p = self._periodicity(y)
        d = self.tgt_per - p                      # + 면 합성이 더 시끄럽다
        # **목표에서 값도 기울기도 0 이어야 한다.** 예전 형태
        # `softplus(d)·A + softplus(−d)` 는 d=0 에서 0.02·ln2·(A+1) = 0.0555 이고 기울기가
        # 0.5·(1−A) ≠ 0 이라, 최소점이 d = −0.02·ln A = −0.022 — 합성이 목표보다 **더
        # 주기적인** 쪽에 있었다. 즉 이 항은 늘 배음을 더 가지런하게 미는 쪽으로 치우쳐
        # 있었다(MEASUREMENTS §50.2 탐침: 목표 그대로 넣어도 hnr 0.0555).
        # 비대칭 후버: |d| 의 매끈한 형태에 시끄러운 쪽(d>0) 가중을 σ 로 섞는다.
        pen = self._soft_abs(d, 0.02) * (1.0 + (HNR_ASYM - 1.0) * torch.sigmoid(d / 0.02))
        w = self._per_ok.to(pen.dtype)
        return (pen * w).sum() / (w.sum().clamp_min(1.0) * pen.shape[0])

    # ------------------------------------------------- 유성 구간의 세 통계
    def _contrast(self, db: torch.Tensor, k: int, win_hz: float,
                  lo: float, hi: float) -> torch.Tensor:
        """국소 이동평균 대비 잔차의 평균 |·| — 봉우리와 골이 얼마나 뚜렷한가. (…,T)

        창 `win_hz` 는 보려는 구조의 간격보다 **넓어야** 한다. 극 간격 1199 Hz 짜리
        포먼트 골을 보려면 2 kHz, F0 빗살을 보려면 900 Hz 다.
        """
        bw = self.fs / k
        w = max(3, int(round(win_hz / bw)) | 1)
        f = torch.arange(db.shape[-2], device=db.device, dtype=db.dtype) * bw
        x = db.transpose(-1, -2)                       # (…, T, F)
        sh = x.shape
        env = torch.nn.functional.conv1d(
            x.reshape(-1, 1, sh[-1]),
            torch.ones(1, 1, w, dtype=db.dtype, device=db.device) / w,
            padding=w // 2).reshape(sh).transpose(-1, -2)
        m = (f >= lo) & (f < hi)
        return self._soft_abs(db - env, 0.1)[..., m, :].mean(-2)

    def _subf0_rel(self, db: torch.Tensor) -> torch.Tensor:
        """F0 아래 대역의 레벨 − 기준 대역의 레벨 [dB]. (T,)"""
        sub, ref = self._subf0_m, self._subf0_r
        t = min(db.shape[-1], sub.shape[-1])
        d, sub, ref = db[..., :t], sub[..., :t], ref[..., :t]
        a = (d * sub).sum(-2) / sub.sum(-2).clamp_min(1.0)
        b = (d * ref).sum(-2) / ref.sum(-2).clamp_min(1.0)
        return a - b

    def _prepare_voice_stats(self, raw: dict) -> None:
        """목표의 첨예도·F0 아래 레벨과 유성 마스크를 **한 번** 만든다.

        `no_grad` 안에서 부른다. F0 는 **목표의 것**을 쓴다 — 적합된 F0 로 재면
        벌점이 제 꼬리를 문다.
        """
        self._sharp_k = HARM_FFT if HARM_FFT in raw else None
        mdb = self._db(raw[MEL_FFT])
        lvl = mdb.mean(-2)
        live = lvl > float(lvl.max()) - 45.0
        v = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)))
        hop = MEL_FFT // 4
        if v.size:
            idx = np.clip((np.arange(mdb.shape[-1]) * hop / self.hop).astype(int),
                          0, v.size - 1)
            vm = torch.as_tensor(v[idx], device=mdb.device)
        else:
            vm = torch.ones_like(live)
        self._voiced_short = (live & vm)
        self.tgt_sharp = self._contrast(mdb, MEL_FFT, SHARP_WIN_HZ,
                                        SHARP_LO_HZ, SHARP_HI_HZ)
        self.tgt_cont = None
        self._subf0_m = self._subf0_r = self.tgt_subf0 = None
        self.tgt_harm = self._voiced_long = None
        if self._sharp_k is None:
            return
        k = self._sharp_k
        hdb = self._db(raw[k])
        hop = k // 4
        f0 = np.asarray(self.track["f0_target"], dtype=np.float64)
        idx = np.clip((np.arange(hdb.shape[-1]) * hop / self.hop).astype(int),
                      0, max(f0.size - 1, 0))
        f0f = torch.as_tensor(f0[idx] if f0.size else np.zeros(len(idx)),
                              dtype=hdb.dtype, device=hdb.device)
        fr = (torch.arange(hdb.shape[-2], device=hdb.device, dtype=hdb.dtype)
              * (self.fs / k)).unsqueeze(-1)
        ok = f0f > 50.0
        f0s = f0f.clamp_min(50.0)
        self._subf0_m = ((fr > 40.0) & (fr < SUBF0_HI * f0s)).to(hdb.dtype)
        self._subf0_r = ((fr >= SUBF0_REF[0] * f0s)
                         & (fr < SUBF0_REF[1] * f0s)).to(hdb.dtype)
        hl = hdb.mean(-2)
        lv = (hl > float(hl.max()) - 45.0) & ok & (self._subf0_m.sum(-2) > 0)
        self._voiced_long = lv
        self.tgt_subf0 = self._subf0_rel(hdb)
        self.tgt_harm = self._contrast(hdb, k, HARM_WIN_HZ, HARM_LO_HZ, HARM_HI_HZ)

    def continuity_loss(self, mel_err: torch.Tensor) -> torch.Tensor:
        """창별 오차가 **직전 창보다 나빠진 만큼**을 문다. 좋아진 것은 공짜다."""
        L = max(1, int(CONT_LAG))
        if mel_err.shape[-1] <= L:
            return torch.zeros((), dtype=mel_err.dtype, device=mel_err.device)
        d = mel_err[..., L:] - mel_err[..., :-L]
        over = self._soft_over(d - CONT_KNEE_DB, 0.3 * CONT_KNEE_DB)
        live = self._voiced_short
        if live is not None and live.shape[-1] >= over.shape[-1] + L:
            w = (live[..., L:] & live[..., :-L])[..., :over.shape[-1]].to(over.dtype)
            return (over * w).sum() / w.sum().clamp_min(1.0) / 20.0
        return over.mean() / 20.0

    def sharpness_loss(self, mel_raw_db: torch.Tensor,
                       harm_db: torch.Tensor | None) -> torch.Tensor:
        """유성 구간이 목표보다 **뭉툭한 만큼**을 문다. 날카로운 것은 공짜다."""
        z = torch.zeros((), dtype=mel_raw_db.dtype, device=mel_raw_db.device)
        tot, n = z, 0
        c = self._contrast(mel_raw_db, MEL_FFT, SHARP_WIN_HZ, SHARP_LO_HZ, SHARP_HI_HZ)
        m = min(c.shape[-1], self.tgt_sharp.shape[-1])
        d = self._soft_over(self.tgt_sharp[..., :m] - c[..., :m], 0.05)
        w = self._voiced_short[..., :m].to(d.dtype)
        tot, n = tot + (d * w).sum() / w.sum().clamp_min(1.0), n + 1
        if harm_db is not None and self.tgt_harm is not None:
            c = self._contrast(harm_db, self._sharp_k, HARM_WIN_HZ, HARM_LO_HZ, HARM_HI_HZ)
            m = min(c.shape[-1], self.tgt_harm.shape[-1])
            d = self._soft_over(self.tgt_harm[..., :m] - c[..., :m], 0.05)
            w = self._voiced_long[..., :m].to(d.dtype)
            tot, n = tot + (d * w).sum() / w.sum().clamp_min(1.0), n + 1
        return tot / max(n, 1) / 20.0

    def subf0_loss(self, harm_db: torch.Tensor) -> torch.Tensor:
        """F0 아래가 **목표보다** 시끄러운 만큼을 문다. 목표의 프라이는 벌하지 않는다."""
        r = self._subf0_rel(harm_db)
        m = min(r.shape[-1], self.tgt_subf0.shape[-1])
        d = self._soft_over(r[..., :m] - self.tgt_subf0[..., :m] - SUBF0_KNEE_DB, 0.3)
        w = self._voiced_long[..., :m].to(d.dtype)
        return (d * w).sum() / w.sum().clamp_min(1.0) / 20.0

    def phase_loss(self, y: torch.Tensor) -> torch.Tensor:
        """**단위 크기** 복소 잔차 — 크기와 직교한 순수 위상 거리, 조화 우세부에만.

        왜 복소 잔차를 그냥 쓰면 안 되는가
        ----------------------------------
        예전 형태는 `|S_t − S_p|` 였다. 위상이 상관된 곳(하모닉)에서는 옳게 작동하지만,
        **위상이 무상관인 곳(모든 난류)에서는 |S_p| = 0 이 최소다.** 두 위상이 독립이면
        E|S_t − S_p|² = |S_t|² + |S_p|² 이므로 합성을 **끄는 것**이 가장 좋은 해가 된다.

        실측 (같은 스펙트럼의 두 독립 실현, 합성 진폭 g 를 훑는다):

            | g   | 예전 위상항 | 크기항 SC |
            |-----|-------------|-----------|
            | 0.0 |  0.999 ←최소 |     1.000 |
            | 0.8 |  1.277      |     0.627 |
            | 1.0 |  1.411      |     0.665 |

        `fit_staged` 는 이 항을 기본으로 800 회 돌린다. 즉 마찰 구간은 800 회 내내
        "소리를 꺼라" 는 기울기를 받고 있었다.

        고친 형태
        ---------
        두 복소수를 단위 크기로 정규화한 뒤 거리를 재고, 목표 크기로 가중한다:

            d(t,f) = w(t,f) · |S_t/|S_t| − S_p/|S_p||

        `S_p` 의 크기에 대한 편미분이 항등적으로 0 이므로 **어떤 구간에서도 소리를
        끄지 않는다.** 크기는 크기 항이, 위상은 위상 항이 맡는 깨끗한 분해다.

        가중 w 는 **목표의 조화 우세도**다 (`_harmonic_weight`). 난류 빈에서는 0 이라
        아예 안 본다 — 재도 뜻이 없는 양이기 때문이다.

        여러 창을 쓰는 이유: 창이 길수록 하모닉이 분해되어 위상이 정밀해지지만 손실면이
        톱니가 된다. 짧은 창이 큰 틀을 잡고 긴 창이 다듬도록 겹친다.
        """
        out = 0.0
        for k in (256, 512, 1024):
            Sp, St = _stft(y, k, self.wins[k]), _stft(self.target, k, self.wins[k])
            m = min(Sp.shape[-1], St.shape[-1])
            b = self.bin_max[k]
            St, Sp = St[:, :b, :m], Sp[:, :b, :m]
            at = self._cabs(St)
            w = self._harm_w[k][:, :, :m] * at
            if PHASE_FMAX > 0.0:
                fb = torch.arange(b, device=w.device, dtype=w.dtype) * (self.fs / k)
                w = w * torch.sigmoid((PHASE_FMAX - fb) / 100.0).view(1, -1, 1)
            # 단위 크기로 정규화. δ 는 크기가 0 에 가까운 빈에서 방향이 폭주하는 것을
            # 막는다 (그런 빈은 w 도 작아 어차피 기여가 없다).
            dt = 1e-4 * at.mean().detach() + 1e-12
            ut = St / torch.sqrt(St.real ** 2 + St.imag ** 2 + dt ** 2)
            up = Sp / torch.sqrt(Sp.real ** 2 + Sp.imag ** 2 + dt ** 2)
            d = ut - up
            # √(|z|²+δ²)−δ — |z| 는 0 에서 곡률이 발산한다. 적합이 좋아질수록 그리로
            # 가므로 바닥을 깔아 둔다 (relu 제거와 같은 부류, C² 유지).
            e = torch.sqrt(d.real ** 2 + d.imag ** 2 + 1e-6) - 1e-3
            out = out + (w * e).sum() / (w.sum() + 1e-9)
        return out / 3.0

    def _decompose_target(self) -> tuple[np.ndarray, np.ndarray] | None:
        """목표를 조화 성분과 잔차로 가른다 (`waveform.decompose`).

        성문 펄스에서 나온 국소 F0 로 3 주기 창에서 최소제곱을 푼다. 무성 구간에서는
        F0 가 없어 조화 성분이 0 이 되고 전부 잔차로 간다 — 따로 마스크가 필요 없다.
        """
        from .waveform import decompose
        tgt = self.target[0].detach().cpu().numpy().astype(np.float64)
        f0 = np.asarray(self.track["f0_target"], dtype=np.float64)
        voi = np.asarray(getattr(self.track, "voiced", np.zeros(0, dtype=bool)))
        if voi.shape[0] == f0.shape[0]:
            f0 = np.where(voi, f0, 0.0)
        try:
            _, har, res = decompose(tgt, self.fs, f0, self.hop)
        except Exception:
            return None
        return har, res

    def _harmonic_weight(self, k: int) -> torch.Tensor:
        """(1, F, T) 조화 우세도 — 이 시간·주파수 빈이 얼마나 '주기적'인가.

            w = |STFT(조화)| / (|STFT(조화)| + |STFT(잔차)|)

        모음의 낮은 하모닉에서 1 에 가깝고, 마찰 잡음과 모음 고역의 기식에서 0 에
        가깝다. 목표에서 한 번만 계산하는 상수다.

        두 곳이 이 값을 쓴다. 위상 항은 **w 로 가중**해 조화부만 보고(난류의 위상은
        재도 뜻이 없다), 크기 항은 **1−w 로 가중**해 난류부에서 기대 스펙트럼을
        비교한다(실현을 맞추라고 하면 기울기가 잡음이다).
        """
        b = self.bin_max[k]
        if self._har_res is None:
            return torch.ones(1, b, 1, device=self.device)
        har, res = self._har_res
        H = self._cabs(_stft(torch.as_tensor(har, dtype=torch.float32,
                                             device=self.device).unsqueeze(0),
                             k, self.wins[k]))[:, :b]
        R = self._cabs(_stft(torch.as_tensor(res, dtype=torch.float32,
                                             device=self.device).unsqueeze(0),
                             k, self.wins[k]))[:, :b]
        return (H / (H + R + 1e-9)).detach()

    def _time_smooth(self, S: torch.Tensor, k: int) -> torch.Tensor:
        """파워 영역에서 시간축 이동평균 -> 크기. 난류의 **기대** 스펙트럼을 만든다.

        파워가 가법적이므로 평균은 파워에서 낸다. 폭은 `NOISE_EXPECT_MS` 이고 창
        크기별로 프레임 수가 다르다 (긴 창은 이미 시간 폭이 커서 평활이 거의 없다).
        """
        n = self._sm_frames.get(k, 1)
        if n <= 1 or S.shape[-1] < 2:
            return S
        n = min(n, S.shape[-1] // 2 * 2 + 1)
        pad = n // 2
        p = torch.nn.functional.pad(S ** 2, (pad, n - 1 - pad), mode="replicate")
        return torch.sqrt(torch.nn.functional.avg_pool1d(p, n, stride=1) + 1e-20)

    def _expect(self, S: torch.Tensor, k: int) -> torch.Tensor:
        """난류 우세 빈만 기대 스펙트럼으로 갈아 끼운다.

        `(1−w)` 로 섞으므로 조화부는 프레임별 실현 그대로 남는다. 목표와 합성에
        **똑같이** 걸리므로 편향은 생기지 않고 기울기의 분산만 줄어든다.
        """
        if NOISE_EXPECT_MS <= 0 or self._har_res is None:
            return S
        wn = self._noise_w.get(k)
        if wn is None:
            return S
        m = min(S.shape[-1], wn.shape[-1])
        w = wn[..., :m]
        s = S[..., :m]
        return s + w * (self._time_smooth(s, k) - s)

    @staticmethod
    def _soft_over(x: torch.Tensor, width: float) -> torch.Tensor:
        """relu 의 부드러운 대체. width 만큼의 폭으로 무릎을 뭉갠다.

        **relu 를 쓰면 안 된다.** 꺾임점에서 2 차 도함수가 정의되지 않아 손실이 C² 가
        아니게 되고, 그러면 헤시안 기반 진단(유효 파라미터 수 γ 등)이 통째로 막힌다.
        실측으로 확인했다: 유한차분 헤시안의 대칭성(uᵀHv = vᵀHu)이 ε 을 4 자릿수
        쓸어도 41.9 / 47.5 / 178.2 / 153.5 % 로 깨졌다 (docs/MEASUREMENTS.md §8.7).
        손실은 결정적이었으므로 원인은 무작위성이 아니라 꺾임이다.

        width → 0 이면 relu 로 수렴한다. 벌점으로서의 성질(문턱 아래는 거의 0, 위는
        선형)은 그대로 두고 미분만 매끄럽게 만든다.
        """
        return width * torch.nn.functional.softplus(x / width)

    @staticmethod
    def _soft_abs(x: torch.Tensor, width: float) -> torch.Tensor:
        """|x| 의 부드러운 대체 (의사 후버). 작은 x 에서 x²/2width, 큰 x 에서 |x|.

        포락 오차의 L1 이 손실의 최대 꺾임원이었다 — 멜 빈 수천 개마다 a=b 에서
        꺾인다. width 는 "이 정도 차이는 오차로 안 친다" 는 뜻이고, dB 단위에서
        0.5 dB 면 계측 잡음보다 작아 거동이 사실상 안 바뀐다.
        """
        return width * (torch.sqrt(1.0 + (x / width) ** 2) - 1.0)

    def _band_floor(self, mdb: torch.Tensor):
        """밴드별 바닥 (…, n_mels, 1). `BAND_FLOOR_DB` 주석이 근거다."""
        glob = float(mdb.max())
        if BAND_FLOOR_DB <= 0.0:
            return glob - DB_RANGE
        own = mdb.amax(-1, keepdim=True) - BAND_FLOOR_DB
        return own.clamp(min=glob - BAND_FLOOR_MIN, max=glob - DB_RANGE)

    @staticmethod
    def _soft_floor(x: torch.Tensor, floor, width: float) -> torch.Tensor:
        """clamp_min 의 부드러운 대체. floor + softplus(x − floor)."""
        return floor + width * torch.nn.functional.softplus((x - floor) / width)

    def _report_nonfinite(self, it: int, ps, names) -> None:
        """비유한 기울기가 **어느 파라미터의 어느 프레임**에서 나는지 찍는다.

        `w` 는 (Tc, P_fit) 이므로 열 = 파라미터, 행 = 격자점이다. 열 이름을 붙여
        주면 원인 파라미터가 바로 보인다. `FORMANT_ML_DEBUG_NONFINITE=1` 로 켠다.
        """
        for nm, p in zip(names, ps):
            if p.grad is None or torch.isfinite(p.grad).all():
                continue
            bad = ~torch.isfinite(p.grad)
            if p.grad.dim() == 2:
                cols = bad.any(0).nonzero().flatten().tolist()
                rows = bad.any(1).nonzero().flatten().tolist()
                who = [self.names[c] for c in cols if c < len(self.names)]
                print(f"    [{it}] 비유한 {nm}: 파라미터 {who}  "
                      f"격자점 {rows[:8]}{'...' if len(rows) > 8 else ''} "
                      f"({len(rows)}/{p.grad.shape[0]})", flush=True)
            else:
                idx = bad.nonzero().flatten().tolist()
                who = [self.names[c] for c in idx if c < len(self.names)] if nm == "d" else idx
                print(f"    [{it}] 비유한 {nm}: {who}", flush=True)

    @staticmethod
    def _pseudo_huber(r: torch.Tensor) -> torch.Tensor:
        """후버의 매끄러운 형태. torch.where 는 1 차 도함수만 이어져 C² 가 아니다.

        √(1+r²) − 1 은 작은 r 에서 r²/2, 큰 r 에서 r−1 로 후버와 같은 모양이면서
        무한히 미분 가능하다.
        """
        return torch.sqrt(1.0 + r * r) - 1.0

    def penalty(self) -> torch.Tensor:
        """물리적으로 성립하지 않는 해를 막는다.

        (1) 포먼트 순서. F4 가 F1 아래로 내려가는 해가 실제로 나왔다.
        (2) 대역폭이 중심 주파수를 넘지 않을 것 — 넘으면 그건 극이 아니라 기울기다.
        (3) 조음 속도. 계측된 생리적 상한(`VEL_LIMIT_HZ_PER_MS`)을 넘는 이동은 문다.
        """
        u = self._u()
        pen = torch.zeros((), dtype=torch.float64, device=self.device)
        if len(self.f_idx) >= 2:
            f = torch.stack([self._to_val(u[:, i], self.specs[i]) for i in self.f_idx], 1)
            gap = self._soft_over(f[:, :-1] + F_MARGIN_HZ - f[:, 1:], 30.0) / 1000.0
            pen = pen + 10.0 * (gap * gap).mean()
            for j, i in enumerate(self.f_idx):
                nb = self.names[i].replace("f", "bw")
                if nb in self.names:
                    k = self.names.index(nb)
                    bw = self._to_val(u[:, k], self.specs[k])
                    over = self._soft_over(bw - 0.8 * f[:, j], 30.0) / 1000.0
                    pen = pen + 2.0 * (over * over).mean()
        if VEL_W > 0 and VEL_MODE != "off" and u.shape[0] > 1:
            dt = max(self.track.frame_ms, 1e-6)
            tab = {"hinge": VEL_LIMIT_HZ_PER_MS, "huber": VEL_KNEE_HZ_PER_MS,
                   "accel": ACC_KNEE_HZ_PER_MS2}[VEL_MODE]
            for nm, lim in tab.items():
                if nm not in self.names or u.shape[0] < 3:
                    continue
                k = self.names.index(nm)
                v = self._to_val(u[:, k], self.specs[k])
                if VEL_MODE == "accel":
                    # 2 차 차분. 한 방향으로 꾸준히 가는 급전은 여기서 0 에 가깝고,
                    # 부호가 뒤집히는 난동만 크게 나온다.
                    rate = (v[2:] - 2.0 * v[1:-1] + v[:-2]).abs() / (dt * dt)
                else:
                    rate = (v[1:] - v[:-1]).abs() / dt
                if VEL_MODE == "hinge":
                    over = self._soft_over(rate - lim, 0.05 * lim) / 100.0
                    pen = pen + VEL_W * (over * over).mean()
                else:
                    # 후버. lim 으로 나눠 무차원으로 만든다 (파라미터끼리 같은 저울).
                    r = rate / lim
                    h = self._pseudo_huber(r)
                    pen = pen + VEL_W * h.mean()
        if ARTIC_VEL_W > 0 and u.shape[0] > 1:
            # **1 차 차분(속도)이다.** 포먼트에는 2 차(가속도)를 쓰지만(빠르게
            # 움직이는 것은 허용하고 방향을 홱 바꾸는 것만 문다), 여기서 막을 것은
            # 방향 전환이 아니라 **불가능한 속도** 자체다.
            dt = max(self.track.frame_ms, 1e-6)
            for nm, knee in ARTIC_VEL_KNEE.items():
                if nm not in self.names:
                    continue
                k = self.names.index(nm)
                lg = torch.log(self._to_val(u[:, k], self.specs[k]).clamp_min(1e-4))
                rate = (lg[1:] - lg[:-1]).abs() / dt
                # **문턱 위만 문다.** 처음에는 `pseudo_huber(rate/knee)` 를 썼는데
                # 그건 무릎 **아래**에서도 이차로 벌하므로 실제 제스처까지 눌린다.
                # 실측 (yang_00000040 0.18~0.55 s, ARTIC_VEL_W=1.0):
                #     a_c 속도 95 분위 0.449 -> **0.015** neper/ms
                # 가장 빠른 실제 제스처가 0.172 인데 그 11 배 **아래**로 얼렸다.
                # 대가는 포락 90.67 -> 87.85 %, 무게중심 r 0.983 -> 0.880 이었다
                # (§27.3). 스파이크만 깎아야 하는데 조음 자체를 죽인 것이다.
                #
                # softplus 로 초과분만 취하면 한계 아래는 사실상 공짜다. 폭은
                # 무릎의 20 % — relu 에 충분히 가깝고 C² 는 지킨다.
                # (정확히 0 은 아니다: 속도 0 에서 softplus(−5) = 6.7e-3 이 새고
                #  의사후버를 지나 프레임당 ~9e-7 로 남는다. 손실이 ~1.0 이므로
                #  무시할 양이고, 대신 미분이 어디서나 살아 있다.)
                over = self._soft_over(rate - knee, 0.2 * knee) / knee
                pen = pen + ARTIC_VEL_W * self._pseudo_huber(over).mean()
        if BW_LAW_W > 0.0 and len(self.f_idx) >= 1:
            # **대역폭을 손실 법칙 쪽으로 당긴다.** 포먼트 주파수는 그대로 자유다 —
            # 조음이 정하는 양이기 때문이다. 묶는 것은 "그 주파수의 극이 얼마나
            # 감쇠하는가" 뿐이고, 그건 기하가 아니라 손실이 정한다.
            bw_floor = float(getattr(self.eng.tract, "bw_floor", 40.0))
            bw_slope = float(getattr(self.eng.tract, "bw_slope", 0.05))
            for i in self.f_idx:
                nb = self.names[i].replace("f", "bw")
                if nb not in self.names:
                    continue
                k = self.names.index(nb)
                f = self._to_val(u[:, i], self.specs[i]).clamp_min(50.0)
                b = self._to_val(u[:, k], self.specs[k]).clamp_min(1.0)
                law = bw_floor + bw_slope * f
                r = torch.log(b / law) / BW_LAW_TOL
                pen = pen + BW_LAW_W * self._pseudo_huber(r).mean()
        if RIPPLE_W > 0 and self.w.shape[0] >= 3:
            # 격자 간격은 **stride 를 곱한 실제 시간**이다. 성긴 격자에서 같은
            # 증분은 훨씬 느린 변화이므로 벌점도 그만큼 작아야 한다.
            dt = max(self.track.frame_ms, 1e-6) * max(self.stride, 1)
            acc = (self.w[2:] - 2.0 * self.w[1:-1] + self.w[:-2]).abs() / (dt * dt)
            pen = pen + RIPPLE_W * self._pseudo_huber(acc / RIPPLE_KNEE).mean()
        return pen

    def loss(self):
        want = self.pulse_weight > 0 and self.pulse_idx.numel() > 0 and not PULSE_LOCK
        out = self.synth(want_phase=want)
        y, phase = out if want else (out, None)
        sc, env_db, env_sc, per = self.spectral_loss(y)
        env = 4.0 * env_db + env_sc + 0.5 * sc    # 포락(dB)이 주 목적, SC 는 보조
        l = env
        T = {"env": env} if self._collect else None
        self._last_db = float(env_db.detach()) * 20.0

        def add(name, base, term):
            nonlocal l
            if T is not None:
                T[name] = term
            l = l + self._bal.get(name, base) * term

        if want:
            pl = self.pulse_loss(phase)
            self._last_pulse = float(pl.detach())
            add("pulse", self.pulse_weight, pl)
        if self.phase_weight > 0:
            pr = getattr(self, "_phase_ramp", 1.0)
            if T is not None:
                T["phase"] = self.phase_loss(y)
                l = l + self._bal.get("phase", self.phase_weight) * pr * T["phase"]
            else:
                l = l + self._bal.get("phase", self.phase_weight) * pr * self.phase_loss(y)
        if FLUX_W > 0.0 and getattr(self, "_flux_db", None) is not None:
            fl = self.flux_loss(self._flux_db)
            self._last_flux = float(fl.detach()) * 20.0
            add("flux", FLUX_W, fl)
        if CORR_W > 0.0:
            cl = self.corr_loss(y)
            self._last_corr = float(cl.detach())
            add("corr", CORR_W, cl)
        if HNR_W > 0.0:
            add("hnr", HNR_W, self.hnr_loss(y))
        if UNVOICED_W > 0.0 and getattr(self, "_amp", None) is not None:
            add("unvoiced", UNVOICED_W, self.unvoiced_loss(self._amp))
        if ATTACK_W > 0.0:
            add("attack", ATTACK_W, self.attack_loss(y))
        if PROM_W > 0.0:
            add("prom", PROM_W, self.prom_loss())
        if HFRIP_W > 0.0 and getattr(self, "_hfrip_raw", None) is not None:
            add("hfrip", HFRIP_W, self.hfrip_loss(y))
        if EVENT_W > 0.0 and getattr(self, "w_ev", None) is not None:
            add("event", EVENT_PEN_W, self.event_loss())
        if DELTA_W > 0.0:
            add("delta", DELTA_W, self.delta_loss())
        if FORMANT_W > 0.0:
            add("formant", FORMANT_W, self.formant_loss())
        if QUIET_W > 0.0:
            add("quiet", QUIET_W, self.quiet_loss(y))
        if MOVE_W > 0.0:
            add("move", MOVE_W, self.move_loss(self._last_ctrl))
        if RE_LEAD_W > 0.0 and getattr(self, "_re", None) is not None:
            add("relead", RE_LEAD_W, self.re_lead_loss(self._re))
        if CONT_W > 0.0 and self._mel_err is not None:
            add("cont", CONT_W, self.continuity_loss(self._mel_err))
        if SHARP_W > 0.0 and self._mel_raw_db is not None:
            add("sharp", SHARP_W, self.sharpness_loss(self._mel_raw_db, self._harm_db))
        if SUBF0_W > 0.0 and self._harm_db is not None and self.tgt_subf0 is not None:
            add("subf0", SUBF0_W, self.subf0_loss(self._harm_db))
        if self.lam_l1 > 0:
            # **L1 — 희소한 제어열.** 사용자 제안. 지금 정칙화가 전부 L2(`lam_smooth`,
            # `lam_prior`)라 적합기가 40×1420 격자에 자잘한 보정을 흩뿌린다. 그
            # 흩뿌림이 국소 위상 미끄러짐(§38.2c)의 유력한 후보이고, 학습 데이터로
            # 나가는 제어열의 해석 가능성도 떨어뜨린다.
            #
            # **|w| 는 0 에서 미분이 없다.** √(w²+δ²)−δ 로 깐다 (§31 과 같은 부류).
            # δ 는 한 걸음 크기의 1/10 — 그보다 작은 증분은 사실상 0 으로 본다.
            d = self.w * self.scale
            dl = 0.1 * self.scale
            l = l + self.lam_l1 * (torch.sqrt(d * d + dl * dl) - dl).mean()
        if BOUND_W > 0.0:
            over = self._soft_over(self._u().abs() - BOUND_U, 0.3)
            l = l + BOUND_W * (over * over).mean()
        if MOTION and MOTION_L2 > 0.0:
            layers = [self.w] + list(self.w_coarse)
            base = min(float(q.shape[0]) for q in layers)
            for q in layers:
                l = l + MOTION_L2 * (float(q.shape[0]) / base) * (q * q).mean()
        if GESTURE_W > 0.0 and MOTION:
            # 층마다 이웃 목표의 차에 L1 (유사 후버로 깐다 — 0 에서 미분이 없으므로).
            gs = 0.0
            for q in [self.w] + list(self.w_coarse):
                if q.shape[0] > 1:
                    d = (q[1:] - q[:-1]) * self.scale
                    dl = 0.05 * self.scale
                    gs = gs + (torch.sqrt(d * d + dl * dl) - dl).mean()
            if torch.is_tensor(gs):
                add("gesture", GESTURE_W, gs)
        if self.lam_smooth > 0:
            # **층마다 건다.** 층을 쌓는 동안 거친 층에도 계속 기울기가 가므로,
            # 고운 층만 매끄럽게 해서는 난동을 거친 층으로 밀어낼 수 있다.
            sm = sum(((q[1:] - q[:-1]) ** 2).mean()
                     for q in ([self.w] + list(self.w_coarse)) if q.shape[0] > 1)
            if torch.is_tensor(sm):
                add("smooth", self.lam_smooth, sm)
        if self.lam_prior > 0:
            d = (self._u() - self.u0) * self.prior_w
            add("prior", self.lam_prior, (d * d).mean())
        if T is not None:
            self._terms = T
        return l + self.penalty(), sc, env_sc, per

    def balance(self, params=None, verbose: bool = False) -> None:
        """항마다 기울기 규범을 재서 유효 가중을 맞춘다 (`BALANCE` 주석 참조).

        **눈금은 한 번만 맞추고 굳힌다.** 처음에는 단계마다 다시 쟀는데, 그러면 이미
        만족된 항은 기울기가 0 에 가까워져 가중이 무한히 커진다 — 실측(`out/L16`)에서
        `smooth` 가 4.81 → 18.3 → 52.7 → **219** 로 폭증해 제어열을 얼렸고(포락
        88.6 → 80.3), 반대로 `corr` 는 1.07 → **0.05** 로 사실상 꺼져 끊김이
        21 → 47 로 돌아왔다. 다중 해상도는 층마다 0 에서 새로 시작하므로 매끄러움
        항의 기울기가 특히 작아 이 병이 크게 드러났다. 목적은 **단위를 맞추는 것**
        이지 진척을 똑같이 만드는 것(GradNorm 원형)이 아니다.

        그래서 각 항은 **처음 기울기가 잡힌 단계에서 한 번** 재고 그 뒤로 굳힌다.
        `phase` 처럼 뒤 단계에서 처음 나타나는 항만 그때 잰다. 전역 스칼라 단계(1 단계,
        `w` 가 최적화 대상이 아니다)에서는 균형하지 않는다.
        """
        if not BALANCE:
            self._bal = {}
            return
        ps = [q for q in (params or self.opt_params()) if q.requires_grad]
        if not any(q is self.w for q in ps):
            return
        self._collect = True
        try:
            self.loss()
            T = dict(self._terms)
        finally:
            self._collect = False

        def gnorm(t):
            # 이 단계가 최적화하지 않는 파라미터에만 걸린 항(예: 1 단계의 prior·smooth 는
            # `w` 에만 걸린다)은 기울기가 비어 있다 — 0 으로 두고 균형에서 뺀다.
            g = torch.autograd.grad(t, ps, retain_graph=True, allow_unused=True)
            s2 = [(x * x).sum() for x in g if x is not None]
            return float(torch.sqrt(torch.stack(s2).sum())) if s2 else 0.0

        missing = [k for k, t in T.items()
                   if k != "env" and k not in self._bal and BAL_W.get(k, 0.0) > 0.0
                   and k not in BAL_FIXED
                   and torch.is_tensor(t) and t.requires_grad]
        if not missing:
            return
        g_env = gnorm(T["env"])
        if not (g_env > 0.0):
            return
        out = []
        for k in missing:
            t = T[k]
            gk = gnorm(t)
            if gk > 0.0 and BAL_W.get(k, 0.0) > 0.0:
                self._bal[k] = BAL_W[k] * g_env / gk
                out.append(f"{k} {self._bal[k]:.3g}")
        if verbose:
            print("      균형 가중: " + ", ".join(out), flush=True)

    # ------------------------------------------------------------- 적합
    def fit(self, iters: int = 200, lr: float = 0.05, log_every: int = 25,
            verbose: bool = True, params: list | None = None,
            sizes: tuple[int, ...] | None = None,
            patience: int = 0, tol: float = 3e-4,
            ramp: tuple | None = None) -> FitReport:
        """`patience` 회 동안 손실이 `tol`(상대) 만큼도 안 줄면 멈춘다.

        **코퍼스 규모로 가려면 수렴 판정이 필요하다.** 지금까지는 고정 반복 뒤 최저손실
        스냅샷을 취할 뿐이라 이미 수렴한 구간에서도 예산을 다 썼다 (docs/HANDOFF.md §5:
        "구간당 5 -> 25 분이 됐다"). 45 분 코퍼스를 200 ms 구간으로 쪼개면 수천 개다.

        `patience=0` 이면 끈다 — 예전 거동 그대로다. 코사인 스케줄러가 lr 을 줄이는
        중이므로 문턱을 너무 크게 잡으면 아직 내려갈 수 있는데 멈춘다. 3e-4 는 실측에서
        마지막 20 % 구간의 회차당 개선폭보다 작다.
        """
        if sizes is not None:
            self.sizes = list(sizes)
        # **목적함수를 계단으로 바꾸지 않는다** (연속변형 / graduated non-convexity).
        # 위상 단계는 `phase_weight` 를 0 에서 3.0 으로 한 번에 올려 왔고, 그래서 §27 이
        # 적은 대로 **항상 한 번 꺾였다가 회복**한다 — 200 회로는 회복을 못 끝내
        # 기준선이 ±4 점 흔들린다(§46.1). 매끄럽게 켜면 해를 따라가며 변형하게 된다.
        # `ramp = (목표가중, 올리는 회차)`.
        if BALANCE:
            self.balance(params, verbose)
        opt = torch.optim.Adam(params or self.opt_params(), lr=lr)
        cyc_T = 0
        if SCHED == "sgdr" and iters >= 2 * SGDR_CYCLES:
            cyc_T = max(1, iters // SGDR_CYCLES)

            def _lam(i, T=cyc_T, n=iters):
                c = min(i // T, SGDR_CYCLES - 1)          # 나머지 회차는 마지막 주기에 붙인다
                t = i - c * T
                Tc = T if c < SGDR_CYCLES - 1 else max(n - c * T, 1)
                peak = SGDR_DECAY ** c
                return 0.05 + (peak - 0.05) * 0.5 * (1.0 + math.cos(math.pi * min(t, Tc) / Tc))
            sch = torch.optim.lr_scheduler.LambdaLR(opt, _lam)
        else:
            sch = torch.optim.lr_scheduler.CosineAnnealingLR(opt, max(iters, 1), eta_min=lr * 0.05)
        best = (-1e18, None, None, None)
        hist: list[tuple[float, float]] = []
        bad_grads = 0
        stall = 0
        seed0 = self.eng.cfg.seed
        ema = None
        for it in range(iters):
            if RESEED:
                self._reseed_n = getattr(self, "_reseed_n", 0) + 1
                self.eng.cfg.seed = 1000 + self._reseed_n
            if ramp is not None:
                # **가중을 직접 바꾸지 말고 배율로 건다.** `--balance` 는 위상 가중을
                # 굳힌 값(`_bal["phase"]`)으로 덮어쓰므로, `phase_weight` 를 바꾸면 램프가
                # 조용히 무시된다 — 실측(`out/L20`)이 `out/L18` 과 0 회차 손실부터 소수점까지
                # 같았다. 배율은 균형 여부와 상관없이 최종 가중에 곱해진다.
                tgt, ri = ramp
                self.phase_weight = float(tgt)
                self._phase_ramp = min(1.0, (it + 1) / max(int(ri), 1))
                ramping = self._phase_ramp < 1.0
            else:
                self._phase_ramp = 1.0
                ramping = False
            if cyc_T and it > 0 and it % cyc_T == 0 and it // cyc_T < SGDR_CYCLES:
                stall = 0             # 재시동 직후의 오르막은 정체가 아니다
                if verbose:
                    print(f"    [{it:4d}] 웜 리스타트 (lr ×{SGDR_DECAY ** (it // cyc_T):.2f})",
                          flush=True)
            opt.zero_grad(set_to_none=True)
            l, sc, env_sc, per = self.loss()
            if not torch.isfinite(l):
                if verbose:
                    print(f"    [{it}] 손실 비유한 — 중단")
                break
            env = float(100.0 * (1.0 - env_sc.detach()))
            fine = float(100.0 * (1.0 - sc.detach()))
            hist.append((env, fine))
            # **파라미터 사본은 걸음을 딛기 전에 뜬다.** step() 뒤에 뜨면 n 회차의 손실과
            # n+1 회차의 파라미터가 짝지어져, 복원해도 그 손실이 안 나온다(실측: 최선
            # 1.3185 로 기록해 놓고 복원하니 1.4828).
            score = -float(l.detach())
            if RESEED:
                # 잡음 실현마다 손실이 흔들린다 — 운 좋은 씨앗을 최선으로 뽑지 않게 평균한다.
                ema = -score if ema is None else (1 - RESEED_EMA) * ema + RESEED_EMA * (-score)
                score = -ema
            # 스냅샷은 조금이라도 나아지면 뜬다. 멈춤 판정만 **의미 있는** 개선을 센다.
            gain = score - best[0]
            # **램프 중에는 최선 상태를 뜨지 않는다.** 가중이 회차마다 다르니 손실끼리
            # 비교가 성립하지 않는다 — 초반의 작은 위상 가중에서 손실이 낮게 계산되어
            # 위상 단계 시작 상태가 "최선" 으로 뽑히고 단계 전체가 헛돈다.
            if ramping:
                gain = 0.0
            elif score > best[0]:
                best = (score, self._snapshot(), None,
                        (env, fine, self._last_db, float(l.detach()), per))
            stall_before = stall          # 증가 **전** 값 — 건너뛴 회차에 되돌린다
            if patience > 0 and not ramping:     # 램프 중의 "정체" 는 수렴의 증거가 아니다
                stall = 0 if gain > tol * abs(score) else stall + 1
                if stall >= patience:
                    if verbose:
                        print(f"    [{it}] 수렴 ({patience} 회 정체) — 조기 종료")
                    break
            l.backward()
            ps = self.opt_params()
            # **비유한 기울기로 걸음을 딛으면 안 된다.** Adam 의 모멘트가 NaN 으로
            # 오염되면 그 뒤 모든 파라미터가 NaN 이 되고, 손실을 보기 전에 엔진 안에서
            # 터진다(실측: 탄음 구간에서 int(NaN)).
            if any(p.grad is not None and not torch.isfinite(p.grad).all() for p in ps):
                bad_grads += 1
                if DEBUG_NONFINITE:
                    self._report_nonfinite(it, ps, ["w", "d", "log_gain", "pulse_phi0"]
                                           + ["hf%d" % i for i in range(len(self.hf))])
                # **건너뛰지 말고 성분만 지운다.** 예전에는 회차 전체를 버렸는데,
                # 그러면 파라미터가 안 변하므로 **다음 회차도 똑같이 비유한**이다.
                # 결정적으로 갇힌다 — 실측(out/sw/r000, yang_00000040 전체):
                # 모든 단계가 "[30] 수렴 + 비유한 29 회 건너뜀" 으로 끝났다. 즉
                # 단계당 실제 걸음이 **1 번**뿐이었고 포락이 82.7 % 에 머물렀다
                # (같은 파일의 정상 적합은 93.1 %).
                #
                # 비유한 성분을 0 으로 두면 나머지 성분으로 걸음을 딛으므로 상태가
                # 바뀌고, 다음 회차에는 그 자리를 벗어난다. Adam 의 모멘트도
                # 오염되지 않는다 (0 은 유한하다).
                for p in ps:
                    if p.grad is not None:
                        torch.nan_to_num_(p.grad, nan=0.0, posinf=0.0, neginf=0.0)
                # 정체 계수는 되돌린다 — 기울기 일부를 버린 회차의 정체는 수렴의
                # 증거가 아니다 (§27: 위상 단계가 골짜기 한복판에서 멈췄다).
                stall = stall_before
            torch.nn.utils.clip_grad_norm_(ps, 5.0)
            opt.step(); sch.step()
            if verbose and (it % log_every == 0 or it == iters - 1):
                print(f"    [{it:4d}] 포락 {env:6.2f}%  정밀 {fine:6.2f}%  "
                      f"오차 {self._last_db:5.2f} dB  펄스 {self._last_pulse:.3f}  "
                      f"손실 {float(l.detach()):.4f}", flush=True)
        self.eng.cfg.seed = seed0
        if verbose and bad_grads:
            print(f"    (기울기 비유한 {bad_grads} 회 건너뜀)")
        if best[1] is not None:
            self._restore(best[1])
            env, fine, db, lv, per = best[3]
        else:
            env = fine = db = lv = float("nan"); per = {}
        return FitReport(env, fine, db, per, lv, len(hist), hist)

    def opt_params(self) -> list:
        """최적화 대상 전부. **한 군데서만 정의한다** — 예전에는 이 목록이 네 곳에
        따로 적혀 있었고(옵티마이저·기울기 가드·스냅샷·최선 복원) 그래서 고역
        파라미터를 어디에도 못 넣은 채로 남았다."""
        if getattr(self, "_hf_focus", False):
            return list(self.hf) or [self.log_gain]     # 고역 단계: 전역 손잡이만
        out = ([self.w] + list(self.w_coarse)
               + [self.d, self.log_gain, self.pulse_phi0] + self.hf)
        if getattr(self, "w_ev", None) is not None:
            out.append(self.w_ev)
        return out

    def _snapshot(self):
        return tuple(q.detach().clone() for q in self.opt_params())

    def _restore(self, snap) -> None:
        with torch.no_grad():
            for q, v in zip(self.opt_params(), snap):
                q.copy_(v)

    def pick_lr_global(self, candidates, iters: int, verbose: bool = True) -> float:
        """짧은 탐침으로 전역 단계의 lr 을 고른다.

        **구간마다 맞는 걸음 크기가 다르다.** 40 ms 짜리 탄음은 lr 0.05 에서 포락
        60.3 % 인데 0.02 면 90.9 % 다 (실측, lr 에 단조). 반대로 소스 기울기가 7 dB/oct
        틀린 합성 모음은 0.02 로는 못 돌아오고 0.05 가 필요하다. 하나로 못 정하므로
        짧게 재 보고 손실이 가장 낮은 것을 쓴다.
        """
        start = self._snapshot()
        best = (float("inf"), float(candidates[0]))
        for lr in candidates:
            self._restore(start)
            rep = self.fit(iters, float(lr), 10 ** 9, False,
                           params=[self.d, self.log_gain, self.pulse_phi0] + self.hf,
                           sizes=STAGES[0])
            if np.isfinite(rep.loss) and rep.loss < best[0]:
                best = (rep.loss, float(lr))
            if verbose:
                print(f"      lr {float(lr):5.3f} -> 손실 {rep.loss:.4f}")
        self._restore(start)
        return best[1]

    def pick_lr_phase(self, candidates, iters: int, verbose: bool = True) -> float:
        """위상 단계의 lr 도 짧은 탐침으로 고른다.

        전역 단계와 같은 이유이고, 실측으로도 구간마다 최적이 다르다 (위상 800 반복):
        정상 모음 0.05~0.12, 설측 0.12 이상, 탄음 0.05. 하나로 못 정한다.
        """
        start = self._snapshot()
        best = (float("inf"), float(candidates[0]))
        for lr in candidates:
            self._restore(start)
            rep = self.fit(iters, float(lr), 10 ** 9, False, sizes=FFT_SIZES)
            if np.isfinite(rep.loss) and rep.loss < best[0]:
                best = (rep.loss, float(lr))
            if verbose:
                print(f"      lr {float(lr):5.3f} -> 손실 {rep.loss:.4f}")
        self._restore(start)
        return best[1]

    def fit_staged(self, global_iters: int = 200, stage_iters: int = 150,
                   lr_global=(0.015, 0.03, 0.05, 0.09), lr_frame: float = 0.04,
                   phase_iters: int = 800, phase_w: float = 3.0,
                   lr_phase=(0.05, 0.12, 0.25),
                   verbose: bool = True, log_every: int = 50,
                   patience: int = 0) -> FitReport:
        """전역 스칼라 -> 제어 격자를 성기게에서 촘촘하게, 창도 함께 늘려 가며.

        `patience` 는 각 단계의 수렴 판정에 그대로 넘어간다. 코퍼스를 통째로 돌릴 때
        쓴다 (`scripts/parametrize_corpus.py`). 기본 0 = 예전 거동.
        """
        if PULSE_LOCK and getattr(self, "_pulse_phase", None) is not None:
            # **위상 오프셋을 먼저 훑는다.** 잠금을 켜면 `pulse_loss` 가 항등적으로 0 이라 `pulse_phi0` 를
            # 끌어 줄 항이 스펙트럼 손실뿐인데, 그 손실은 위상에 대해 다봉이다 — 실측(`L53/s040`)에서
            # 최적 오프셋이 −2.62 rad 로 초기값에서 멀었다. 12 등분해 한 번 재고 가장 나은 자리에서 시작한다.
            best = None
            with torch.no_grad():
                for k in range(12):
                    phi = -math.pi + 2.0 * math.pi * k / 12.0
                    self.pulse_phi0.fill_(phi)
                    lv = float(self.loss()[0])
                    if best is None or lv < best[0]:
                        best = (lv, phi)
                self.pulse_phi0.fill_(best[1])
            if verbose:
                print(f"  펄스 잠금: 위상 오프셋 {best[1]:+.2f} rad ({best[1] / (2 * math.pi):+.3f} 주기), "
                      f"손실 {best[0]:.4f}", flush=True)
        if verbose:
            print(f"  1 단계 전역 {len(self.names)} 스칼라 (이득 {self.gain_db():+.1f} dB)")
        if isinstance(lr_global, (int, float)):
            lr_global = (float(lr_global),)
        if len(lr_global) > 1:
            probe = max(20, global_iters // 4)
            lr_g = self.pick_lr_global(lr_global, probe, verbose)
            if verbose:
                print(f"      -> lr {lr_g:.3f} 선택")
        else:
            lr_g = float(lr_global[0])
        rep = self.fit(global_iters, lr_g, log_every, verbose,
                       params=[self.d, self.log_gain, self.pulse_phi0] + self.hf,
                       sizes=STAGES[0], patience=patience)
        for si, (grid, sizes) in enumerate(zip(GRID_MS, STAGES)):
            tc = self.set_grid(grid)
            if verbose:
                print(f"  2.{si + 1} 단계  격자 {grid:g} ms ({tc} 점)  창 {sizes}", flush=True)
            rep = self.fit(stage_iters, lr_frame * (0.75 ** si), log_every, verbose,
                           sizes=sizes, patience=patience)
        # **위상 단계는 기본이다.** 크기만 맞추면 위상은 물리가 강제하는 곳에서만 맞는다.
        # 실측(코퍼스 150 ms 창 4 개): 조화 SNR +3.3/+1.0/+3.5/−2.6 -> +24.7/+16.5/+12.9/+16.2,
        # 위상 모양 오차 7.6/29.9/65.5/25.4° -> 3.4/17.8/32.6/16.4°. 포락은 안 나빠졌다.
        # **위상 단계는 예산이 모자랐다.** 예전 값(lr = lr_frame·0.4 = 0.016, 200 반복)은
        # 어느 구간에서도 바닥에 못 닿았다. 학습률과 반복만 올린 실측(무제약):
        #
        #   | 구간      | 예전 (lr 0.016 / 200) | lr 0.12 / 800 |
        #   |-----------|-----------------------|---------------|
        #   | 정상 모음 |                  3.4° |      **0.1°** |
        #   | 설측 닐   |                 96.5° |     **36.4°** |
        #   | 탄음 이리 |                 50.9° |     **18.3°** |
        #
        # 기구를 하나도 안 더하고 설측 2.6 배, 탄음 2.8 배다. 크기 적합 단계는 설측의
        # 위상을 아예 못 건드렸고(94.8 -> 96.5°) 오직 이 단계만 움직였다.
        if HF_STAGE_ITERS > 0 and self.hf:
            if verbose:
                print(f"  2.5 단계  고역만 ({HF_STAGE_FMIN:.0f} Hz 위, 전역 {len(self.hf)} 개, "
                      f"{HF_STAGE_ITERS} 반복)", flush=True)
            self._hf_focus = True
            try:
                self.fit(HF_STAGE_ITERS, 0.05, log_every, verbose, sizes=FFT_SIZES,
                         patience=max(20, HF_STAGE_ITERS // 4))
            finally:
                self._hf_focus = False
        if phase_iters > 0:
            self.phase_weight = phase_w
            if verbose:
                print(f"  3 단계  위상 (가중 {phase_w}, {phase_iters} 반복)", flush=True)
            if isinstance(lr_phase, (int, float)):
                lr_p = float(lr_phase)
            else:
                lr_p = self.pick_lr_phase(lr_phase, max(30, phase_iters // 8), verbose)
                if verbose:
                    print(f"      -> lr {lr_p:.3f} 선택")
            rep = self.fit(phase_iters, lr_p, log_every, verbose, sizes=FFT_SIZES,
                           patience=(PHASE_PATIENCE if PHASE_PATIENCE > 0 else patience),
                           ramp=((phase_w, max(1, int(phase_iters * PHASE_RAMP)))
                                 if PHASE_RAMP > 0.0 else None))
            self.phase_weight = 0.0
        self.sizes = list(FFT_SIZES)
        return rep

    # ------------------------------------------------------------- 결과
    def result_track(self) -> ControlTrack:
        with torch.no_grad():
            v = self.control()[0].double().cpu().numpy()
        return ControlTrack(v, self.track.frame_ms, list(self.track.events))

    def render(self, seed: int | None = None) -> np.ndarray:
        """적합된 파라미터로 합성. `seed` 를 주면 **난류의 실현만** 바꾼다.

        같은 파라미터를 시드만 바꾼 합성 간 거리는 실현 분산을 추정하는 대조군이다.
        `turbulence.corrected_sc` 의 보정은 목표 분산 추정에도 의존하므로,
        여러 시드의 대역·시간 구조와 함께 읽는다. 시드별로 다시 적합하지 않는다.

        시드는 **`cfg.seed` 로** 바꾼다. `eng.noise` 에 직접 대입하면 조용히 무시된다 —
        `synth()` 가 부르는 `VoiceEngine.reset()` 이 `NoiseBank(self.cfg.seed)` 로
        덮어쓰기 때문이다.
        """
        with torch.no_grad():
            if seed is None:
                return self.synth()[0].cpu().numpy()
            old = self.eng.cfg.seed
            try:
                self.eng.cfg.seed = int(seed)
                return self.synth()[0].cpu().numpy()
            finally:
                self.eng.cfg.seed = old

    def fidelity(self, seed_b: int = 991) -> dict:
        """실현 분산을 추정해 차감한 진단용 성적표.

        `fine_corr` 은 분산 추정에 의존한다. `resolved=False` 이면 편향을
        판별하지 못했으며, 보정값은 신뢰구간의 하한이 아니다. `trust` 는
        잔여거리 비율이고 `floor` 는 실현 기준 추정값이지 보편적 상한이 아니다.
        여러 시드·대역·시간 구조와 청취를 함께 평가해야 한다.
        """
        from . import turbulence as tb
        tgt = self.target[0].detach().cpu().numpy().astype(np.float64)
        a = self.render()
        b = self.render(seed_b)
        out = tb.spectral_fidelity(tgt, a, b, self.fs, tuple(FFT_SIZES), self.f_max)
        out["spectrum_match"] = tb.spectrum_match(tgt, a, self.fs)
        return out

    def gain_db(self) -> float:
        return float(20.0 * self.log_gain.detach().item() / math.log(10))

    def moved(self) -> list[tuple[str, float, float]]:
        """(이름, 초기 중앙값, 적합 중앙값). 어떤 파라미터가 얼마나 움직였는지."""
        out = []
        with torch.no_grad():
            u = self._u()
            for i, sp in enumerate(self.specs):
                a = float(self._to_val(self.u0[:, i], sp).median())
                b = float(self._to_val(u[:, i], sp).median())
                out.append((sp.name, a, b))
        return out
