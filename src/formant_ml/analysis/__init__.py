"""실제 음성 -> 물리 파라미터 역추정.

학습(train.py)이 프레임 단위 제어를 맞추는 일이라면, 여기 있는 것들은 학습 없이
**한 화자의 고정된 성질**을 몇 개의 숫자로 뽑아내는 일이다.

* `registers` : 성대 진동 모드 — 개방지수(H1-H2)->Rd, 서브하모닉, 성구/파사지오
* `sibilant`  : 치찰음 극-영점 지문 (경사하강 적합)
* `phase`     : 하모닉 상대위상(RPS) -> 위상차 올패스 파라미터 적합
* `extract`   : 위 셋을 묶어 VoiceProfile(JSON) 로 저장하는 CLI
"""
