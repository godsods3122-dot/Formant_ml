# Changelog

형식: [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/). 버전 규칙: `docs/VERSIONING.md`.

## [0.3.1] — 2026-09-07

사용자 지시("원래 목소리를 직접 합성하고 각 포인트별로 물리 파라미터를 매칭해라. 노이즈를
제거하고 스펙트럼이 95 % 이상 일치할 때까지 ms 간격으로 파라미터를 추적해라")에 따라
**복사합성**으로 전환했다. 손으로 작곡하는 대신 녹음을 정답으로 두고 파라미터를 역추정한다.
그 과정에서 엔진 결함 세 개가 드러났고 그게 이 판의 핵심이다 (ADR 0012, MEASUREMENTS §7).

### Fixed
- **고차 극 종속이 10 kHz 에서 +97 dB, 22 kHz 에서 +151 dB 였다.** 이산 공명기는 θ→π 에서
  극쌍이 z=−1 의 이중 실극으로 붙어 DC 정규화 이득이 `(1+r)²/(1−r)²` 로 뛴다. 0.98·fs/2 까지
  채운 것이 원인. `0.60·fs/2` 까지, 대역폭 Q≈1 로 바꿔 Fant 의 원래 모양(DC 0 → 10 kHz +13 →
  22 kHz −0.4 dB)으로 되돌렸다. 이 하나로 여성 /아/ 평균 스펙트럼 오차가 9.10 → 4.16 dB.
- **포먼트 분석에 프리엠퍼시스가 빠져 있었다.** 소스의 −12 dB/oct 기울기가 LPC 극 하나를
  저역에서 잡아먹어 배정이 한 칸씩 밀렸다(여성 /아/ 가 F1 376 / F2 962). 넣은 뒤 기준 모음
  다섯 개가 전부 계측 범위 안.
- **프레임별 독립 추정의 흔들림이 광대역 잡음이 됐다.** 25 ms 창을 1 ms 마다 독립으로 재면
  계수가 kHz 로 변조된다(12~24 kHz 가 −38 dB 여야 하는데 −5 dB, 소스를 다 꺼도 남았다).
  분석 뒤 중앙값+이동평균 평활.
- `sqrt(0)` 에서 나오던 비유한 기울기 (`glottis.py`, `noise.py`).

### Added
- `engine/denoise.py` — 위너 스펙트럼 차감. 방 잡음이 목표에 남으면 엔진이 그것까지 만든다.
- `engine/analyze.py` — 녹음 → 1 ms 제어열 초기값 (Praat F0·HNR + 참 포락선 LPC 포먼트).
- `engine/fit.py` — 미분가능 엔진으로 제어열 역추정. 전역 스칼라 → 격자 20/10/5/1 ms,
  창 256 → 4096. 포락(멜 dB)·정밀(선형 STFT) 두 일치율과 평균 |오차| dB 를 보고한다.
- `scripts/copyfit.py` — 잡음 제거 → 분석 → 적합 → wav·제어열·보고서.
- `tviir.py` 에 numba 수반(adjoint) 역전파. 엔진 순전파+역전파가 초당 오디오 **42 → 1.9 s**.
- `tests/engine/test_analyze_fit.py` — 참 포락선·포먼트 복원·프리엠퍼시스 회귀·평활·잡음 제거·
  적합기 기울기·전역 오프셋 격리·포먼트 순서 벌점.

### Changed
- `glottis.py` 의 LF 파형표 색인이 (B,N,K) 슬라이스 → (B,N) 색인. 역전파에서 37 MB 영텐서를
  5937 회 만들던 것이 사라졌다(역전파 시간의 68 %).
- 지터·시머가 적합 대상에 들어갔다. 둘이 광대역 잡음 바닥을 33 dB 올린다 — 고역 바닥은
  사실상 지터가 정한다.

### Fixed (2차 — 위상)
- **`f0_target` 위에 폐압-F0 결합이 곱해지고 있었다.** ParamSpec 은 "0 이 아니면 tension
  대신 F0 직접 지정" 인데 `f0 = f0 * (1 + 0.04·(Ps−Pth))` 가 그 위에 또 걸렸다. p_sub 6.7 ·
  Pth 2.2 에서 ×1.18 — 238 Hz 를 지정하면 281.6 Hz 가 울렸다. 200 ms 동안 성문 펄스가
  **8.42 주기** 밀렸다. 결합은 긴장으로 F0 를 정할 때만 건다. 고친 뒤 누적 어긋남 0.033 주기.
- **최선 파라미터 사본을 `opt.step()` 뒤에 뜨고 있었다.** n 회차의 손실과 n+1 회차의
  파라미터가 짝지어져, 되돌려도 기록한 손실이 안 나왔다(1.3185 로 기록 → 복원 1.4828).
- 전역 단계 lr 0.15 는 발산한다. 0.05 로 낮췄다 — 소스 기울기가 7 dB/oct 틀린 출발점에서
  0.2 는 한 번도 못 내려왔고, 0.05 는 tilt 9.0 → 2.04, 오차 0.12 dB 로 복원했다.
- **제스처 세기 보정이 F0 버그에 기대고 있었다.** 설측 유지부가 "모음보다 조용"해야 하는데,
  F0 를 고치자 1.4 dB 더 커졌다 — 이전에는 하모닉이 F1 을 비껴가 우연히 조용했던 것이다.
  `scripts/calibrate_levels.py` 로 설측·탄음·비음 gain 을 프로파일의 실측 목표에 다시 맞췄다
  (여 1.50/0.80/1.00 → 1.32/0.74/0.75, 남 1.05/0.75/1.00 → 0.93/0.61/0.78, 기본 → 0.65/0.66/0.75).
  테스트도 프레임 번호 대신 **제어열에서 창을 읽도록** 바꿨다.

### Added (2차)
- `analyze.glottal_pulses` / `f0_from_pulses` — Praat 의 상호상관 PointProcess 로 성문 폐쇄
  시각을 받아 F0 를 **주기별로** 만든다. 피치 궤적은 프레임마다 독립이라 0.5 % 씩 흔들리고
  위상은 그 누적합이다.
- `fit.pulse_loss` — `1 − cos(φ(t_k) − φ0)`. 파형 위상이 아니라 **펄스 시각**을 걸어 손실면을
  매끈하게 유지한다. φ0 는 학습되는 상수 하나(LF 폐쇄 시점과 Praat 정의의 차이를 흡수).
- `VoiceEngine.forward` 가 성문 위상을 돌려준다.
- `scripts/calibrate_levels.py` — 제스처 세기 자동 보정(이분법).

### 결과 (여성 긴 /아/ 200 ms, 1 ms 격자)
| | 0-0.5 k | 0.5-1 k | 1-2 k | 2-3 k | 3-5 k | 5-8 k | 8-12 k | 12-24 k |
|---|---|---|---|---|---|---|---|---|
| 목표 | −2.7 | −5.2 | −8.1 | −35.5 | −29.5 | −31.2 | −48.5 | −53.4 |
| 적합 | −2.7 | −5.1 | −8.2 | −34.9 | −29.8 | −31.0 | −52.9 | −54.8 |

**포락 일치 94.5 %, 정밀 일치 88.1 %, 멜 평균 오차 1.67 dB.**
(고차 극 수정 전 9.10 dB → 수정 후 4.16 → F0·펄스 고정 후 1.67 dB.)

## [0.2.3] — 2026-09-06

사용자 지적("비강↔구강이 뚝뚝 끊긴다 / 치찰음이 성대 진동과 따로 논다")을 새 여성 녹음 2 개와
기존 녹음의 **ms 단위 전수 분해**로 확인하고 구조를 바꿨다. 계측은 `docs/MEASUREMENTS.md` §6.

### Added
- `scripts/segment_analyze.py` (5 ms 프레임 전수 계측), `scripts/transitions.py` (전사 정렬 없이
  비음·마찰 전이를 찾아 궤적 출력).
- 병렬 비강 분기 (ADR 0010): `oral_open`·`nasal_f2`·`nasal_f3`·`nasal_damp`·`nasal_gain` 파라미터,
  비강 꼬리 극(나이퀴스트까지), 유량 분배.
- `lat_mix` — 측지 영점의 깊이를 연속으로 여닫는다.

### Changed
- 마찰 소스가 대역통과 → **광대역**(셸프 −9 dB + 정점 1.4 배 저역통과). 정점은 앞공동 극이 만든다.
- 성문 제스처가 협착보다 35 ms 먼저 열리고 55~110 ms 늦게 닫힌다.
- 비음 제스처: 연구개(선행 60 / 후행 90 ms)와 구강 폐쇄(35 / 18 ms)가 다른 시간축.
- `scripts/ab_male.py` 가 비교 창을 신호에서 자동 검출한다.
- `fric_gain` 상한 4 → 64. 마찰 세기 기준점을 `phones.SIB_REF_DB` 한 곳으로.

### Fixed
- 비음 머머의 2 kHz 위가 −60 dB 로 무너지던 것 (극 3 개로 잘라서). 꼬리 극으로 rms 11 → 2.3 dB.
- 구강·비강 게이트를 출력에 걸어 해제 순간 저장 에너지가 12 배로 튀던 클릭.
- 측지 영점을 주파수 0 으로 껐다 켜며 생기던 8 배 스파이크 (로그 파라미터의 영차 유지).
- 마찰음 1~3 kHz 가 15 dB 낮던 것. 파찰음이 `fric_gain` 상한에 10 dB 잘리던 것.

## [0.2.2] — 2026-09-06

### Fixed (같은 화자 A/B 로 찾은 장치 결함, ADR 0009)
- **모음 4~8 kHz 가 35~60 dB 어두웠다** — DC 정규화 공명기 11 개의 곱이 6 kHz −27 / 10 kHz −85 dB.
  나이퀴스트까지 극을 채워 완결(표본화된 관). 실측 남성 /아/ 1/3 oct 오차 4.1 dB.
- 비음 머머 중 마찰·버스트 (연구개 열림을 무시한 구강 유량), 3 kHz 저역통과가 머머 고역을 −80 dB 로 죽인 것.
- /s/ 중 성문 기식이 1~3 kHz 를 10 dB 채운 것 (기식 ∝ ΔPg 선형).
- 마찰 소스 혹(Strouhal 0.2 → 장애물 0.5·v/d, Q 0.5), 앞공동 대역폭.

### Added
- `profiles/user_male.json`, `scripts/ab_male.py` — 같은 화자 A/B (녹음 조각 / 합성 wav 짝 + 계측표).
- 성도 float64 경로.

## [0.2.1] — 2026-09-06

### Added
- `engine/profile.py` `SpeakerProfile` + `profiles/yang_female.json` — 여성 화자 문장 녹음 계측값 (ADR 0008).
- `docs/MEASUREMENTS.md`, `scripts/measure_ref.py` — 여성 문장 / 사용자 비음·치찰음 녹음 계측표.
- 구강압 저장·방전 버스트 (파열/파찰 개시 물리), `phones.affricate` (ㅈ/ㅊ/ㅉ), `phones.nasal(coda=)`,
  구 "이리닐씨리래" 데모, 청취 세트 9 종 (`scripts/v2_listen.py --profile`).
- `tviir.peak_coeffs` (극-영점 봉우리), 비강 3 kHz 저역통과.

### Changed
- 음절 길이 300 → 120~140 ms (계측). 탄음 골 깊이·길이는 프로파일. 탄음 과도음 템플릿 기본 0.
- 성문 기식은 성문 양단 압력 강하 √ΔPg 에 비례 (협착 중 기식이 1~6 kHz 를 채우던 것).
- `front_len` 은 0=끔 규약(영차 유지). 마찰 세기 보정: `fric_gain=1` ⇒ 모음 대비 −11 dB.

### Fixed
- 스트리밍에서 구강압 상태가 프레임 성문 면적 대신 첫 샘플을 쓰던 것.
- 경음/파찰음의 내전이 발성 게이트를 넘어 유성음이 되던 것.

## [0.2.0] — 2026-09-06

### Added
- `formant_ml.engine` — v2 엔진. 시간영역 시변 IIR(결합 스캔·numba), 압력 구동 성문 소스,
  마찰/기식/과도음 템플릿 세 생성기, 포먼트+올패스 → 비강 → 측지 성도, 잔차 보정망
  (shifted softplus TCN), 감정 토큰 레지스트리, `VoiceEngine` (오프라인 = 스트리밍),
  한국어 음소 제스처 `phones.py` (여성 기본값: 아라·라·사·나).
- `tests/engine/` 성질 테스트 40 여 종.
- 문서: `docs/AUDIT_v1.md`, `docs/ARCHITECTURE.md`(Mermaid UML), `docs/VERSIONING.md`, `docs/adr/0001~0006`.
- `scripts/v2_listen.py` 청취 세트 + 측정표.

### Changed
- v1 (`dsp/`, `models/`, `score.py`, `liquid.py`, `aeroacoustic.py`) 은 **동결**. 테스트는 유지.

### Fixed (v2 개발 중 측정으로 잡은 것)
- 대역폭이 0 을 지나 극이 단위원에 붙던 규약 문제 (ADR 0006).
- DC 정규화 영점쌍의 원거리 이득 (+25~40 dB) → 극-영점 노치 (ADR 0005).
- 나이퀴스트 근방 이산 공명기 이득 폭주 → 고차 극 상한 0.7·fs/2.
- 큰 대역폭 공명기를 저역통과로 쓰던 것 → RBJ 2차 저역통과.

## [0.1.0] — 2026-09-05
- v1: LF 가산합성, 포먼트 캐스케이드/KL 도파관(주파수영역), 치찰음 극영점, 유음 다질량 혀끝,
  복사합성 학습 루프. 인수인계 `docs/HANDOFF.md`, `docs/RIEUL.md`.
