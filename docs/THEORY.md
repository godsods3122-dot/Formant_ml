# 이론 노트 — 구현된 방정식

기호: `fs` 샘플레이트, `T0 = 1/F0` 기본주기, `z⁻¹` 한 샘플 지연.

## 1. 성문 소스: LF (Liljencrants-Fant) 모델

유량미분 `E(t) = dU_g/dt` 를 두 구간으로 정의한다.

```
개방기  0 ≤ t ≤ te :   E(t) = E0 · e^{αt} · sin(ω_g t),      ω_g = π/tp
복귀기  te < t ≤ tc :   E(t) = −(Ee / (ε·ta)) · [ e^{−ε(t−te)} − e^{−ε(tc−te)} ]
```

Fant 의 **Rd** 한 개로 형상을 요약한다 (`Rd` 작음 = pressed, 큼 = breathy):

```
Ra = (−1 + 4.8·Rd)/100
Rk = (22.4 + 11.8·Rd)/100
Rg = (Rk/4)·(0.5 + 1.2·Rk) / (0.11·Rd − Ra·(0.5 + 1.2·Rk))
tp = 1/(2·Rg),   te = tp·(1+Rk),   ta = Ra,   tc = 1
```

두 개의 제약으로 `ε`, `α` 가 결정된다.

```
(i)  ε·ta = 1 − e^{−ε(tc−te)}                     → 고정점 반복
(ii) ∫₀^{tc} E(t) dt = 0  (유량이 다시 0으로 닫힘) → α 에 대해 이분법
```

구현: `dsp/glottal.py::_lf_waveform`. Rd 그리드에서 파형을 미리 만들고 FFT 해
**하모닉 복소계수**로 저장한다. 학습 시에는 인접 두 테이블 계수를 선형보간하므로
Rd 에 대해 미분가능하다.

### 대역제한 가산합성

웨이브테이블을 그냥 재생하면 에일리어싱이 생긴다. 대신

```
x(t) = Σ_k 2·|c_k|·m_k·cos(k·φ(t) + arg c_k),   φ(t) = Σ_n 2π f0(n)/fs
m_k = σ((0.95·fs/2 − k·f0)/(0.02·fs/2))          ← 나이퀴스트 부근 소프트 마스킹
```

`φ` 를 **샘플 단위로 누적**하므로 프레임 경계에서 위상 불연속이 없다.
이것이 "합성 잡음이 안 생기는" 첫 번째 이유다.

## 2. 성대 자가진동: 비대칭 2질량 모델

```
m₁ ẍ₁ = −c₁ẋ₁ − k₁x₁ − k_c(x₁−x₂) − Θ(−a₁)·k_col·a₁/(2L) + L·d₁·P₁
m₂ ẍ₂ = −c₂ẋ₂ − k₂x₂ − k_c(x₂−x₁) − Θ(−a₂)·k_col·a₂/(2L) + L·d₂·P₂
a_i = a_{0i} + 2L·x_i,   a_min = min(a₁,a₂)
P₁  = Ps·[1 − Θ(a_min)·(a_min/a₁)²]·Θ(a₁),   P₂ = 0        (베르누이)
U   = a_min⁺ · √(2Ps/ρ)
```

- 긴장도 `q`: `k_i ← q·k_i`, `m_i ← m_i/q` → `F0 ∝ q` (구현에서 확인: q=1→167 Hz, q=2→320 Hz)
- 비대칭 `asym`: 한쪽 성대 강성만 스케일 → 1:1 모드락이 깨지며 서브하모닉/이중음
- 적분: semi-implicit(symplectic) Euler, 4배 오버샘플

구현: `dsp/vocalfold.py`. **충돌항이 비미분**이라 학습 본선에는 쓰지 않는다
(PINN 계열은 이 지점을 미분가능 근사로 우회한다 — arXiv:2511.00428).

## 3. 성도 (a) 포먼트 캐스케이드

Klatt(1980) 관례. 극점 반지름과 각도:

```
r = e^{−π·BW/fs},   θ = 2π·F/fs
D(z) = 1 − 2r cosθ·z⁻¹ + r²·z⁻²
H(z) = D(1)/D(z) = (1 − 2r cosθ + r²)/D(z)        ← DC 이득 = 1
```

**DC 정규화가 중요하다.** 피크 이득 1로 정규화하면 각 단이 대역통과가 되어
캐스케이드 전체가 0으로 죽는다(실제로 구현 중 겪은 버그). `r<1` 이 항상
성립하므로 필터는 설계상 안정하다.

반공명(비음/마찰음의 골): `H(z) = D(z)/D(1)`.

## 4. 성도 (b) Kelly-Lochbaum 도파관

길이 `Δ = c/(2fs)` 인 원통관 N개. `fs=24 kHz, c=350 m/s` 면 `Δ=0.729 cm`,
성도 17.5 cm → **N=24**. 반사계수와 래티스 스텝업 재귀:

```
k_m = (A_m − A_{m+1})/(A_m + A_{m+1}),        k_N = 입술 종단(≈0.9)
A₀(z) = 1
A_m(z) = A_{m−1}(z) + k_m·z^{−m}·A_{m−1}(1/z)
H(ω) = g / A_N(e^{jω})
```

- 시간영역 산란 루프가 없으므로 **주파수축 병렬**, 학습에 적합하다.
- `|k_m| < 1` (면적이 양수면 자동) ⇒ `A_N(z)` 최소위상 ⇒ **항상 안정**.
- 손실(대역폭): `a_j ← a_j·ρ^j` 로 극점 반지름을 ρ배 축소.
- 배열 규약: `area[..., 0]`=성문, `[..., -1]`=입술 (내부에서 뒤집어 재귀).

검증: 균일관 → 498/1500/2502 Hz (이론 500/1500/2500), /a/형 → F1 상승,
/i/·/u/형 → F1 하강.

## 5. 난류 노이즈 (학습되는 소스)

```
n(t) = w(t)·m(t)·[(1−d) + 2d·sin²(π·frac(φ/2π)/0.6)]   ← 변조 × 성문 개방기 동기 AM
m(t) = irfft( W(f) · (1 + (f/f_knee)²)^{−β/2} ),  평균 1 / 표준편차 1 로 정규화
y_n  = LTV_filter(n, P·H_bands · H_sib · Π_{i≥c}(성도 단_i))
```

`β`(변조 스펙트럼 기울기), `f_knee`, `P`(난류 스펙트럼 사전, 기하평균 1 로 정규화)
는 **학습 파라미터**다. 실제 난류는 정상적이지 않고 협착부 제트가 스스로 요동해
진폭이 계속 흔들린다. 그 변조 스펙트럼이 대략 1/f^β 다. 이게 없으면 합성음이
'테이프 히스' 같은 죽은 소리가 되고, 반대로 모델이 크기 스펙트럼만 맞추다 보면
프레임마다 같은 벡터를 반복해 미세하게 주기적인 텍스처가 생긴다.

- `d`(am_depth): 기식성. Klatt & Klatt(1990) — 이 변조가 없으면 하모닉과
  노이즈가 "두 개의 층"으로 분리되어 들린다.
- `c`(noise_entry): 협착 위치. 노이즈는 **협착 하류(앞공동)만** 통과한다.
  소프트 게이트 `w_i = σ((i − c)/0.7)`, 단 응답은 `(1−w_i) + w_i·H_i` 로 보간
  (복소 로그의 분지 문제를 피한다).

## 5.5 치찰음 극-영점 필터

마찰음 스펙트럼을 결정하는 것은 몇 개 안 된다.

```
H_sib(z) = [ D_z(1)/D_z(z) 의 역 ]·[ 극쌍 ]·[ 기울기 ]
         = (D_p(1)/D_p(z)) · (D_z(z)/D_z(1)) · 10^{(t·log2(f/1k))/20}
```

* 극 `(F_p, B_p)` — 협착 **앞공동**의 1/4 파장 공진. /s/ 는 앞공동이 1.5 cm
  안팎이라 5~8 kHz, /ʃ/ 는 2.5~4 kHz.
* 영점 `(F_z, B_z)` — 뒤공동·설하공의 반공진.
* `t` — 난류 소스 자체의 기울기 [dB/oct].

전체를 주파수축 RMS 로 정규화하고 `mix` 로 항등응답과 보간한다(마찰음이 아닌
프레임에서는 아무 일도 일어나지 않는다). 구현 `dsp/sibilant.py`.

이 6 개가 화자의 치찰음 지문이다. 자유로운 대역게인 대신 이걸 쓰는 이유는
[`PLAN.md`](PLAN.md) §0 의 5번 항목에 적었다.

## 5.6 부분 캐스케이드 (노이즈가 협착 하류만 통과)

단별 가중치 `w_i` 로 캐스케이드를 부분 적용해야 한다. 소박한 방법인
`Π (1 − w_i + w_i·H_i)` 는 **쓰면 안 된다**: `w` 가 중간값일 때 감쇠하는 단이
1 로 밀려 올라가 정규화가 깨진다(12 단이 각각 2~3 배면 곱이 10^5). 로그 영역에서
보간한다.

```
H(w) = exp( Σ_i w_i · ( log D_i(1) + log g_i − log|D_i(e^{jω})| − j∠D_i(e^{jω}) ) )
```

`w=0` 에서 정확히 1, `w=1` 에서 정확히 캐스케이드다. 극이 단위원 안에 있으므로
(`r<1`, `D(1)>0`, `D(−1)>0`) `∠D` 의 주치는 연속이라 복소 로그의 분지 문제가 없다.

여기에 더해 **곱 전체를 peak 정규화**한다. DC 정규화 규약에서 고역 극 하나는
나이퀴스트 이득이 1 을 넘고(F=11.2 kHz, BW=620 → 27 배), 앞쪽 저역 극의 저역통과가
사라지면 그게 다 곱해진다. 부분 캐스케이드에 DC 정규화는 맞는 규약이 아니다.
노이즈 경로의 절대 레벨은 `noise_bands` 가 정하므로 모양만 남기면 된다.

## 6. 위상 정형 (올패스) 과 위상 손실

2차 올패스는 크기응답이 정확히 1이고 군지연만 바꾼다:

```
H_ap(z) = (r² − 2r cosθ·z⁻¹ + z⁻²) / (1 − 2r cosθ·z⁻¹ + r²·z⁻²),  |H_ap| ≡ 1
```

위상은 순환량이므로 손실은 **미분량**에 걸고 anti-wrapping 을 적용한다:

```
⟨x⟩ = x − 2π·round(x/2π)
L_IF = E[ |⟨Δ_t∠X − Δ_t∠Y⟩| · w ]        (순시주파수)
L_GD = E[ |⟨Δ_f∠X − Δ_f∠Y⟩| · w ]        (군지연)
w    = |Y| / max|Y|                        (작은 성분의 위상은 무의미)
```

### 6.1 하모닉 상대위상 (위상차 파라미터)

성도 올패스와 **별개로**, 성문 소스의 하모닉 간 위상 관계를 조절한다. 올패스
체인의 위상응답을 하모닉 주파수에서 평가해 위상 오프셋으로 쓴다.

```
φ_k ← φ_k + Σ_s arg H_ap(k·f0 ; F_s, r_s)
```

측정량은 **상대위상**(RPS, relative phase shift)

```
RPS_k = ∠X_k − k·∠X_1
```

으로, 분석창의 시작 위치에 무관한 화자 고유량이다. 프레임 간 원형 표준편차가
위상왜곡편차(PDD)이고 거칠기/기식성에 대응한다.

측정 주의: **STFT 격자의 가장 가까운 빈을 쓰면 안 된다.** 창 누설 계수가
복소수이고 그 위상이 (빈 − 실제 주파수)에 비례하므로, 반올림 오차가 하모닉마다
다른 위상 오차로 들어온다(측정: 상대위상에 1 rad 넘는 편향). 각 프레임의 창
신호에 대해 `f_k = k·f0` 에서 DFT 를 직접 계산한다.

## 6.2 소스 스펙트럼 기울기

```
g_k = 10^{ t·log2(k·f0 / 1000 Hz) / 20 },   t ∈ [−12, +12] dB/oct
```

하모닉 계수에 직접 곱한다. 로그주파수에 대한 직선이라 6~12 kHz 를 원하는 만큼
정확히 올리고 내린다. LF 의 `Rd` 는 고역 감쇠의 *모양*이 고정이라 이걸 못 한다.

## 6.3 지터 / 시머

```
f0 ← f0·(1 + J·z_t),   amp ← amp·(1 + S·z'_t)
```

`z` 는 프레임률에서 만든 저역통과 단위분산 난수. 없으면 완전 주기성이 남아
'부저 같은' 소리가 된다.

## 7. 시변(LTV) 필터 적용

```
프레임별 복소응답 H_t(ω) → irfft → ir_size/2 만큼 회전 + 해닝 창 → 유한 IR
x 를 hop 길이 비중첩 프레임으로 분할 → 각 프레임을 해당 IR 과 FFT 컨볼루션
→ overlap-add → ir_size/2 지연 보정
```

`H ≡ 1` 일 때 출력 = 입력 (오차 7e-7) 로 검증. 재귀가 없어 길이에 대해 병렬이다.
한계: `BW < 40 Hz` 처럼 울림이 `ir_size` 보다 길면 근사 오차가 생긴다.
